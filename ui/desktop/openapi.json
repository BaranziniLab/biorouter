{
  "openapi": "3.0.3",
  "info": {
    "title": "biorouter-server",
    "description": "An AI-powered integrated research environment for biomedical discovery",
    "contact": {
      "name": "Wanjun Gu",
      "email": "wanjun.gu@ucsf.edu"
    },
    "license": {
      "name": "Apache-2.0"
    },
    "version": "1.90.3"
  },
  "paths": {
    "/action-required/secrets": {
      "post": {
        "tags": [
          "super::routes::action_required"
        ],
        "summary": "Store the credentials an extension install is parked on, and release it.",
        "description": "The response reports **names only**: `configuredKeys` on success, `missing`\nwhen a required field came back empty. There is nowhere in it a value can\nsit, which is what lets the parked install — and therefore the model — be\ntold the truth about what happened without being told what it was.\n\nRequires the DR-16 proof-of-user header. The model reaches this daemon over\nthe same HTTP with the same secret key, so without the proof it could satisfy\nits own credential card with a value it invented and drive the install past\nthe one step that exists to involve a person.",
        "operationId": "submit_secrets",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SubmitSecretsRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Names of the keys configured, or which required ones are still missing",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "The request carried no proof it came from the user"
          }
        }
      }
    },
    "/action-required/tool-confirmation": {
      "post": {
        "tags": [
          "super::routes::action_required"
        ],
        "operationId": "confirm_tool_action",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ConfirmToolActionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Decision processed; `status` is `delivered` or `unknown`",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "Refused: `reason` is `unproven` (this request carried no proof it came from the user) or `noKeyInstalled` (this daemon can never obtain that proof)",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/active_work": {
      "get": {
        "tags": [
          "active_work"
        ],
        "operationId": "list_active_work",
        "responses": {
          "200": {
            "description": "Current background jobs, subagents, and in-flight scheduled runs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActiveWorkResponse"
                }
              }
            }
          }
        }
      }
    },
    "/active_work/{id}/cancel": {
      "post": {
        "tags": [
          "active_work"
        ],
        "operationId": "cancel_active_work",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Active-work item id from GET /active_work",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Cancel requested",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CancelActiveWorkResponse"
                }
              }
            }
          },
          "404": {
            "description": "No such active-work item"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/add_extension": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "agent_add_extension",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddExtensionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Extension added. The body is DR-26's cross-institutional warning for this chat once the extension is attached: the statement the user is shown before proceeding, warnings separated by a blank line, and is EMPTY when nothing in the chat crosses an institutional boundary, which is the normal case.",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "Refused by a privacy or subagent-control boundary: the named chat is private/absent, or is a subagent, and the request carried no proof it came from the user"
          },
          "409": {
            "description": "Refused by a privacy boundary (issue #56, DR-16): a private extension cannot be attached to a chat running on a public model"
          },
          "424": {
            "description": "Agent not initialized"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/call_tool": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "call_tool",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CallToolRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Resource read successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CallToolResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "404": {
            "description": "Resource not found"
          },
          "424": {
            "description": "Agent not initialized"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/callable_tool_count": {
      "get": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "get_callable_tool_count",
        "parameters": [
          {
            "name": "session_id",
            "in": "query",
            "description": "Active session whose model-visible tools should be counted",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Model-visible callable tool count",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CallableToolCountResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "424": {
            "description": "Agent not initialized"
          }
        }
      }
    },
    "/agent/cancel": {
      "post": {
        "tags": [
          "workspace"
        ],
        "summary": "Hard cancel: trip the cancellation token of the turn in flight for this",
        "description": "session (BR-62).\n\nBefore this route, cancelling meant dropping the SSE socket — the only thing\nthat closed `tx` and tripped the token — so a turn could not be stopped by a\nsecond client, the CLI, or a script, and `/agent/stop` merely evicted the\nagent from the LRU while the in-flight reply task ran on happily against its\nown `Arc<Agent>`. Tripping the token unwinds the agent loop at its next\nboundary and unblocks a tool-permission prompt it may be parked on.\n\nDeliberately **idempotent**: cancelling a session with no turn in flight (a\ndouble-clicked Stop button, a cancel that raced the turn's own completion) is\na 200 with `cancelled: false`, never an error. A cancel that reports failure\nbecause the thing was already stopped is exactly the unreliability this BR\nexists to remove. When `expected_turn_id` names an older generation and a\nsuccessor is active, the route instead fails closed with 409 and leaves that\nsuccessor untouched.",
        "operationId": "cancel_turn",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CancelTurnRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Cancel processed; `cancelled` reports whether a turn was running",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CancelTurnResponse"
                }
              }
            }
          },
          "400": {
            "description": "Stop-and-Send requires an exact turn id and a valid stable continuation owner id"
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "The request was not proven to come from the user"
          },
          "409": {
            "description": "A different turn generation is active, another client owns the continuation, or its admission is still settling",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CancelTurnConflict"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error"
          },
          "504": {
            "description": "The cancelled turn did not release the session lock before the safety bound"
          }
        }
      }
    },
    "/agent/continuation/abandon": {
      "post": {
        "tags": [
          "workspace"
        ],
        "operationId": "abandon_continuation_lease",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AbandonContinuationLeaseRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The continuation lease is abandoned or was already resolved",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AbandonContinuationLeaseResponse"
                }
              }
            }
          },
          "403": {
            "description": "The request was not proven to come from the user"
          },
          "409": {
            "description": "The lease is invalid or belongs to another session",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ContinuationLeaseErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/agent/continuation/recover": {
      "post": {
        "tags": [
          "workspace"
        ],
        "operationId": "recover_continuation",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecoverContinuationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The exact pending continuation was taken over or its whole claim group was abandoned",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecoverContinuationResponse"
                }
              }
            }
          },
          "400": {
            "description": "The continuation owner id is missing or invalid"
          },
          "403": {
            "description": "The session is out of reach or the request was not proven to come from the user"
          },
          "409": {
            "description": "The exact continuation generation was already resolved",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ContinuationLeaseErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/agent/cross_affiliation_grant": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "summary": "Record the user's acceptance of one cross-institutional data flow (issue #56,",
        "description": "DR-26 / Task 49).\n\n⚠ **This route is a reversal of `/agent/add_extension`'s posture and the\ndifference is the ruling, not an inconsistency.** That route refuses a private\nextension on a public session *outright*, with no user-proof branch, because\nattaching one is a raise the user cannot authorize either. A Private↔Private\ncross-institution flow is the opposite case: both endpoints are already\nprivate, the tier boundary is not being crossed, and DR-26 states explicitly\nthat legitimate cross-institutional work under a real DUA exists and that the\nuser may accept the stated risk. Blocking it outright is the design\nresearchers route around by turning the feature off. So this route exists, and\nthe tier refusal beside it is untouched.\n\nThe grant is keyed on the **triple** (session, extension, model affiliation),\nwhere the affiliation is read by the daemon from the same sample that produced\nthe warning — never from the request. Re-binding to a different institution's\nmodel changes the triple, so the acceptance does not carry over.\n\n⚠ **The desktop surface is the accept control on the refusal itself** (Task\n57): `CrossAffiliationAcceptCard` renders inside the failed tool call that\ncarries `privacy::refusal::CROSS_AFFILIATION_ACCEPT_MARKER`, and posts here\nwith the `X-User-Action` header this route requires. The generated client\nattaches no header of its own, so every caller must supply one — which is the\npoint: a call without it is refused, and only a surface the user gestured at\nhas one to send.",
        "operationId": "agent_cross_affiliation_grant",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrossAffiliationGrantRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The user's acceptance was recorded",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrossAffiliationGrantResponse"
                }
              }
            }
          },
          "400": {
            "description": "There is no cross-institutional mismatch to accept"
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "Refused (issue #56, DR-26): only the user may accept a cross-institutional data flow, or (DR-27) this machine's mixing policy is 'strict' and the operating system did not confirm the user"
          },
          "424": {
            "description": "That chat is not loaded in this daemon, so the model it is bound to cannot be read"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/read_resource": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "read_resource",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReadResourceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Resource read successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadResourceResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "Refused: this entry reaches extensions as a public caller, and the named extension is private"
          },
          "404": {
            "description": "Resource or extension not found"
          },
          "424": {
            "description": "Agent not initialized"
          },
          "500": {
            "description": "Internal server error"
          },
          "502": {
            "description": "The extension failed the read"
          }
        }
      }
    },
    "/agent/remove_extension": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "agent_remove_extension",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveExtensionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Extension removed",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "The session is out of reach, or the target is a subagent and the request lacks user-action proof"
          },
          "409": {
            "description": "Subagent extension grants are immutable for the lifetime of the delegated child"
          },
          "424": {
            "description": "Agent not initialized"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/restart": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "restart_agent",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RestartAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Agent restarted successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestartAgentResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "The session is out of reach, or the target is a subagent and the request lacks user-action proof"
          },
          "404": {
            "description": "Session not found"
          },
          "424": {
            "description": "The delegated child is still initializing"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/resume": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "resume_agent",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResumeAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Agent started successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResumeAgentResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request - invalid working directory"
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "The session is out of reach, or the target is a subagent and the request lacks user-action proof"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/start": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "start_agent",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StartAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Agent started successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Session"
                }
              }
            }
          },
          "400": {
            "description": "Bad request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "409": {
            "description": "The selected private provider requires user-action proof",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/agent/stop": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "stop_agent",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StopAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Agent stopped successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "The session is out of reach, or the target is a subagent and the request lacks user-action proof"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/tools": {
      "get": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "get_tools",
        "parameters": [
          {
            "name": "extension_name",
            "in": "query",
            "description": "Optional extension name to filter tools",
            "required": false,
            "schema": {
              "type": "string",
              "nullable": true
            }
          },
          {
            "name": "session_id",
            "in": "query",
            "description": "Session ID used to inspect active tools; pass an empty string to inspect one globally enabled extension from settings",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Tools retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ToolInfo"
                  }
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "408": {
            "description": "Extension timed out while loading for settings"
          },
          "424": {
            "description": "Agent not initialized"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/update_from_session": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "update_from_session",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateFromSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Update agent from session data successfully"
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "The session is out of reach, or the target is a subagent and the request lacks user-action proof"
          },
          "424": {
            "description": "Agent not initialized"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/update_provider": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "update_agent_provider",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateProviderRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Provider updated. The body is DR-26's cross-institutional warning for this chat on the model just bound: the statement the user is shown before proceeding, warnings separated by a blank line, and is EMPTY when the bind crosses no institutional boundary, which is the normal case.",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "400": {
            "description": "Bad request - missing or invalid parameters"
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "The session is out of reach, or the target is a subagent and the request lacks user-action proof"
          },
          "409": {
            "description": "Refused by a privacy boundary (issue #56). Gate A: a public model cannot be bound to a private chat (body = PrivacyBarrierBody). DR-16: the bind raises this chat's capability to Private and the request carried no proof it came from the user (body = plain text)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PrivacyBarrierBody"
                }
              }
            }
          },
          "424": {
            "description": "Agent not initialized"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/agent/update_working_dir": {
      "post": {
        "tags": [
          "super::routes::agent"
        ],
        "operationId": "update_working_dir",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateWorkingDirRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Working directory updated and agent restarted successfully"
          },
          "400": {
            "description": "Bad request - invalid directory path"
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "Refused by a privacy boundary (issue #56 Task 58 / #47): the named chat is private (or absent, and an unproven caller is told the same thing for both) and the request carried no proof it came from the user"
          },
          "404": {
            "description": "Session not found"
          },
          "409": {
            "description": "Conflict - the working directory is fixed once a chat has messages, or a turn is in flight"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/catalog/changes": {
      "get": {
        "tags": [
          "super::routes::catalog"
        ],
        "summary": "Wait for the extension/skill catalogue to change, then say what changed.",
        "description": "Returns immediately when the caller is already behind. A timeout is not an\nerror: the body carries the current revision and the caller polls again.\n\n⚠ **`truncated` is not advisory.** It means the caller fell further behind\nthan the daemon's buffer holds, so `changes` is a partial history. Applying\nit and believing yourself current is the stale-inventory bug this endpoint\nexists to end, one layer down. Refetch instead.",
        "operationId": "catalog_changes",
        "parameters": [
          {
            "name": "since",
            "in": "query",
            "description": "The last revision this client applied. `0` (or absent) means \"tell me\nthe current revision\", which is how a fresh client establishes a\nbaseline without a refetch.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int64",
              "minimum": 0
            }
          },
          {
            "name": "timeout_ms",
            "in": "query",
            "description": "How long to park, in milliseconds. Clamped to [`MAX_WAIT`].",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int64",
              "nullable": true,
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The catalogue delta since `since`",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogDelta"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          }
        }
      }
    },
    "/catalog/revision": {
      "get": {
        "tags": [
          "super::routes::catalog"
        ],
        "summary": "The current catalogue revision, without waiting.",
        "operationId": "catalog_revision",
        "responses": {
          "200": {
            "description": "The current revision",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogDelta"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          }
        }
      }
    },
    "/coding_agents/status": {
      "get": {
        "tags": [
          "super::routes::coding_agents"
        ],
        "operationId": "coding_agents_status",
        "responses": {
          "200": {
            "description": "Install and sign-in state for each coding-agent CLI",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CodingAgentStatusResponse"
                }
              }
            }
          }
        }
      }
    },
    "/config": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "read_all_config",
        "responses": {
          "200": {
            "description": "All configuration values retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConfigResponse"
                }
              }
            }
          }
        }
      }
    },
    "/config/backup": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "backup_config",
        "responses": {
          "200": {
            "description": "Config file backed up",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/check_provider": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "check_provider",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CheckProviderRequest"
              }
            }
          },
          "required": true
        },
        "responses": {}
      }
    },
    "/config/custom-providers": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "create_custom_provider",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateCustomProviderRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Custom provider created successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/custom-providers/{id}": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "get_custom_provider",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Custom provider retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LoadedProvider"
                }
              }
            }
          },
          "404": {
            "description": "Provider not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      },
      "put": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "update_custom_provider",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateCustomProviderRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Custom provider updated successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "Provider not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      },
      "delete": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "remove_custom_provider",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Custom provider removed successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "Provider not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/detect-provider": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "detect_provider",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DetectProviderRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Detection result (provider_name is null with a reason on failure)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DetectProviderResponse"
                }
              }
            }
          }
        }
      }
    },
    "/config/detectable-providers": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "get_detectable_providers",
        "responses": {
          "200": {
            "description": "Providers supported by API-key auto-detection",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DetectableProvidersResponse"
                }
              }
            }
          }
        }
      }
    },
    "/config/extensions": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "get_extensions",
        "responses": {
          "200": {
            "description": "All extensions retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ExtensionResponse"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error"
          }
        }
      },
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "add_extension",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ExtensionQuery"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Extension added or updated successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request"
          },
          "422": {
            "description": "Could not serialize config.yaml"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/extensions/{name}": {
      "delete": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "remove_extension",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Extension removed successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "Extension not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/init": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "init_config",
        "responses": {
          "200": {
            "description": "Config initialization check completed",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/permissions": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "upsert_permissions",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpsertPermissionsQuery"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Permission update completed",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "400": {
            "description": "Invalid request"
          }
        }
      }
    },
    "/config/pricing": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "get_pricing",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PricingQuery"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Model pricing data retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PricingResponse"
                }
              }
            }
          }
        }
      }
    },
    "/config/providers": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "providers",
        "responses": {
          "200": {
            "description": "All configuration values retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ProviderDetails"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/config/providers/{name}/models": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "get_provider_models",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Provider name (e.g., openai)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Models fetched successfully",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Unknown provider, provider not configured, or authentication error"
          },
          "429": {
            "description": "Rate limit exceeded"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/read": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "read_config",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ConfigKeyQuery"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Configuration value retrieved successfully",
            "content": {
              "application/json": {
                "schema": {}
              }
            }
          },
          "500": {
            "description": "Unable to get the configuration value"
          }
        }
      }
    },
    "/config/recover": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "recover_config",
        "responses": {
          "200": {
            "description": "Config recovery attempted",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/remove": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "remove_config",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ConfigKeyQuery"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Configuration value removed successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "description": "Refused: `BIOROUTER_PRIVACY_TIERS` is the master privacy switch and may only be changed from Settings > Privacy, never removed, and (issue #56, DR-27) `BIOROUTER_PRIVACY_MIXING_POLICY` is set, never deleted"
          },
          "404": {
            "description": "Configuration key not found"
          },
          "409": {
            "description": "Refused by a privacy boundary (issue #56, DR-16): the key decides what privacy capability new chats start at, and a delete restores its default, so it requires proof the request came from the user"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/set_provider": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "set_config_provider",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetProviderRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Default provider and model set"
          },
          "400": {
            "description": "The provider could not be constructed"
          },
          "409": {
            "description": "Refused by a privacy boundary (issue #56, DR-16): this route writes BIOROUTER_PROVIDER, which decides what privacy capability new chats start at, so it requires proof the request came from the user"
          }
        }
      }
    },
    "/config/slash_commands": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "get_slash_commands",
        "responses": {
          "200": {
            "description": "Slash commands retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SlashCommandsResponse"
                }
              }
            }
          }
        }
      }
    },
    "/config/upsert": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "upsert_config",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpsertConfigQuery"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Configuration value upserted successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "400": {
            "description": "Refused (issue #56, DR-27): `BIOROUTER_PRIVACY_MIXING_POLICY` is one of 'open', 'standard' or 'strict'"
          },
          "403": {
            "description": "Refused: `BIOROUTER_PRIVACY_TIERS` is the master privacy switch and may only be written from Settings > Privacy, with its typed confirmation, or (issue #56, DR-27) relaxing `BIOROUTER_PRIVACY_MIXING_POLICY` needed a system authentication that did not happen"
          },
          "409": {
            "description": "Refused by a privacy boundary (issue #56, DR-16): the key decides what privacy capability new chats start at, so writing it requires proof the request came from the user. Also (DR-27) `BIOROUTER_PRIVACY_MIXING_POLICY`, which is user-only in every mode"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/config/validate": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "operationId": "validate_config",
        "responses": {
          "200": {
            "description": "Config validation result",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "422": {
            "description": "Config file is corrupted"
          }
        }
      }
    },
    "/diagnostics/{session_id}": {
      "get": {
        "tags": [
          "super::routes::status"
        ],
        "operationId": "diagnostics",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Diagnostics zip file",
            "content": {
              "application/zip": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "403": {
            "description": "Out of reach - a private or unreadable session named without the user-action proof"
          },
          "500": {
            "description": "Failed to generate diagnostics"
          }
        }
      }
    },
    "/handle_openrouter": {
      "post": {
        "tags": [
          "super::routes::setup"
        ],
        "operationId": "start_openrouter_setup",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetupResponse"
                }
              }
            }
          }
        }
      }
    },
    "/handle_tetrate": {
      "post": {
        "tags": [
          "super::routes::setup"
        ],
        "operationId": "start_tetrate_setup",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SetupResponse"
                }
              }
            }
          }
        }
      }
    },
    "/interrupt": {
      "post": {
        "tags": [
          "workspace"
        ],
        "summary": "Soft interrupt: queue a user message to be injected into the session's",
        "description": "running turn at the next safe loop boundary, instead of cancelling the turn\nand re-sending the whole context. Returns 202 Accepted; the message surfaces\nas a normal user message in the active reply stream on the next loop step.\n\nBR-61: rejected with 409 when the session has no turn in flight — with no\nrunning loop to drain the queue the text would sit on the agent until some\nunrelated later turn injected it. Clients treat 409 as \"just send it as a\nnormal message\".\n\n#69: the 409 is now decided by the *agent's own queue*, not by the turn-lock\ncheck above it. Checking the lock and then queueing are two steps against\nstate the reply loop changes underneath them: a steer could be accepted (202)\nafter the loop had already performed its final empty-queue check, and the text\nthen surfaced in an unrelated later turn. `try_queue_soft_interrupt` decides\nacceptance and enqueues in one critical section, and reports back *which* turn\ntook the message — so a 202 names something real.",
        "operationId": "interrupt",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InterruptRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "Message queued for injection into the running turn",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InterruptAccepted"
                }
              }
            }
          },
          "400": {
            "description": "Empty message text"
          },
          "403": {
            "description": "The request was not proven to come from the user"
          },
          "409": {
            "description": "No turn is accepting interrupts for this session"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/knowledge/active": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "get_active",
        "parameters": [
          {
            "name": "session_id",
            "in": "query",
            "description": "Optional chat session id for the session-scoped selection",
            "required": false,
            "schema": {
              "type": "string",
              "nullable": true
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The session's knowledge bases and its primary",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActiveKbResponse"
                }
              }
            }
          },
          "403": {
            "description": "The named session is outside the caller's privacy reach"
          }
        }
      },
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "set_active",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetActiveBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The resulting selection",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActiveKbResponse"
                }
              }
            }
          },
          "400": {
            "description": "Unknown kb id, a primary outside the resulting set, or conflicting primary-KB fields"
          },
          "403": {
            "description": "Refused by a privacy boundary (issue #56 Task 58 / #47): `session_id` names a private chat (or an absent one, and an unproven caller is told the same thing for both) and the request carried no proof it came from the user (body = plain text)"
          }
        }
      }
    },
    "/knowledge/bases": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "list_bases",
        "responses": {
          "200": {
            "description": "List of knowledge bases",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/KbListEntry"
                  }
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "create_base",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateBaseBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Created knowledge base",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Manifest"
                }
              }
            }
          },
          "400": {
            "description": "Duplicate id, invalid id, or unknown format"
          }
        }
      }
    },
    "/knowledge/bases/import": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "import_brkb",
        "requestBody": {
          "description": "multipart/form-data with a 'file' field containing the .brkb archive",
          "content": {
            "application/json": {
              "schema": {}
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Imported knowledge base ID"
          },
          "400": {
            "description": "Bad request"
          },
          "500": {
            "description": "Internal error"
          }
        }
      }
    },
    "/knowledge/bases/{id}": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "get_base",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Knowledge base manifest",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Manifest"
                }
              }
            }
          },
          "404": {
            "description": "Not found"
          }
        }
      },
      "delete": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "delete_base",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Deleted"
          },
          "404": {
            "description": "Not found"
          }
        }
      }
    },
    "/knowledge/bases/{id}/default-model": {
      "put": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "set_default_model",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetDefaultModelBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Updated knowledge base default model",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Manifest"
                }
              }
            }
          },
          "400": {
            "description": "Bad request"
          },
          "404": {
            "description": "Not found"
          }
        }
      }
    },
    "/knowledge/bases/{id}/export": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "export_brkb",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Binary .brkb archive"
          },
          "404": {
            "description": "Not found"
          },
          "500": {
            "description": "Internal error"
          }
        }
      }
    },
    "/knowledge/bases/{id}/graph": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "get_graph",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Knowledge graph",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Graph"
                }
              }
            }
          },
          "404": {
            "description": "Not found"
          }
        }
      }
    },
    "/knowledge/bases/{id}/history": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "list_history",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Maximum entries (default 50)",
            "required": false,
            "schema": {
              "type": "integer",
              "nullable": true,
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Commit history"
          }
        }
      }
    },
    "/knowledge/bases/{id}/ingest": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "ingest",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/IngestBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "SSE stream of sub-agent events (text/event-stream)"
          },
          "400": {
            "description": "Invalid model or source"
          }
        }
      }
    },
    "/knowledge/bases/{id}/ingest-conversation": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "ingest_conversation",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/IngestConversationBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "SSE stream of sub-agent events (text/event-stream)"
          },
          "400": {
            "description": "Invalid model or no sessions"
          }
        }
      }
    },
    "/knowledge/bases/{id}/lint": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "summary": "Lint a knowledge base and stream the result.",
        "description": "The stream's terminal `event: done` frame carries a `LintResult`: the\nautofix commit, if any, wrapped around a `LintReport` — the four hygiene\nlists this route has always returned, plus `diagnostics`, the typed findings.\nEach of those is a stable rule id (`kb.` for hygiene, `okf.` and `biookf.`\nfor the format layers), a severity, the subject it is about and a message;\na consumer matches on `rule` and never on `message`, which is prose.\n\nBoth schemas are published under `components.schemas` rather than as this\nresponse's body, because the body is an event stream and typing it as JSON\nwould be a false statement the generated client believes.\n\nRetired pre-OKF bases are refused before the macro starts. Startup removes\nthem, and every remaining base receives its current OKF or BioOKF format\nlayer in addition to the shared hygiene rules.",
        "operationId": "lint",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LintBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "SSE stream of sub-agent events (text/event-stream); the terminal `event: done` frame's data is a LintResult"
          },
          "400": {
            "description": "Invalid model"
          }
        }
      }
    },
    "/knowledge/bases/{id}/location": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "get_location",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Knowledge base on-disk location",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LocationResponse"
                }
              }
            }
          },
          "404": {
            "description": "Not found"
          }
        }
      }
    },
    "/knowledge/bases/{id}/merge": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "summary": "⚠ **No caller barrier here, and it is the same position `GET /export` takes.**",
        "description": "This route is the USER's own path: they can already read both bases through\n`GET /bases/{id}/page` and download either as a `.brkb`, and DR-14 governs\nwhat a MODEL can reach. Passing a public-model identity instead would refuse\nthe user every merge involving a private base of their own — the feature's\nmain case — and passing a private one would be inventing a model that is not\nthere.\n\nWhat still runs, and is the part that matters, is the classification fold:\nthe destination takes `max` over the tier axis and the union over owning\ninstitutions. That is what keeps the model side honest after a merge the user\nperformed.\n\nThe proof-of-user is therefore load-bearing rather than ceremonial — it is\nthe whole of what separates this branch from the tool channel.",
        "operationId": "merge_bases",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Destination knowledge base ID — canonical; never modified",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MergeBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "What the merge did, or (dry run) would do",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MergeReport"
                }
              }
            }
          },
          "400": {
            "description": "Bad request"
          },
          "403": {
            "description": "Refused: merging is the user's decision and the request carried no proof it came from them, or this daemon holds no user-action key at all (body = plain text)"
          },
          "404": {
            "description": "Not found"
          },
          "500": {
            "description": "Internal error"
          }
        }
      }
    },
    "/knowledge/bases/{id}/page": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "get_page_body",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "path",
            "in": "query",
            "description": "Page path under the KB root (knowledge/*.md, raw/*/source.md, or index.md/schema.md/log.md)",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Page content",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadPageResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid kb id or path"
          },
          "404": {
            "description": "Page not found"
          }
        }
      }
    },
    "/knowledge/bases/{id}/pages": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "list_pages",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "path_prefix",
            "in": "query",
            "description": "Optional path prefix filter",
            "required": false,
            "schema": {
              "type": "string",
              "nullable": true
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Page list"
          }
        }
      }
    },
    "/knowledge/bases/{id}/pages/{page_path}": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "read_page",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "page_path",
            "in": "path",
            "description": "Page path within KB",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Page content"
          },
          "404": {
            "description": "Page not found"
          }
        }
      },
      "put": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "write_page",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "page_path",
            "in": "path",
            "description": "Page path within KB",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WritePageBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Written",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CommitResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request"
          },
          "500": {
            "description": "Write outcome uncertain or post-commit cache refresh failed"
          }
        }
      }
    },
    "/knowledge/bases/{id}/preview": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "preview_state",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PreviewBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "File content at commit",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreviewResponse"
                }
              }
            }
          },
          "500": {
            "description": "Error"
          }
        }
      }
    },
    "/knowledge/bases/{id}/query": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "query_kb",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueryBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "SSE stream of sub-agent events (text/event-stream)"
          },
          "400": {
            "description": "Invalid model"
          }
        }
      }
    },
    "/knowledge/bases/{id}/raw": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "add_raw_source",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "description": "One of: multipart/form-data with 'file' field, JSON {url}, or JSON {text, title?}",
          "content": {
            "application/json": {
              "schema": {}
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Source ingested",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RawSourceResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request"
          },
          "500": {
            "description": "Internal error"
          }
        }
      }
    },
    "/knowledge/bases/{id}/restore": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "restore_state",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RestoreBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Restored; returns new commit SHA",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestoreResponse"
                }
              }
            }
          },
          "500": {
            "description": "Error"
          }
        }
      }
    },
    "/knowledge/bases/{id}/sources/{sid}/credibility": {
      "put": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "override_credibility",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sid",
            "in": "path",
            "description": "Source ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Credibility"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Credibility overridden",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CredibilityResponse"
                }
              }
            }
          },
          "404": {
            "description": "Source not found"
          },
          "500": {
            "description": "Internal error"
          }
        }
      }
    },
    "/knowledge/bases/{id}/sources/{sid}/reclassify": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "reclassify",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sid",
            "in": "path",
            "description": "Source ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Reclassified credibility",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CredibilityResponse"
                }
              }
            }
          },
          "404": {
            "description": "Source not found"
          },
          "500": {
            "description": "Internal error"
          }
        }
      }
    },
    "/knowledge/bases/{id}/tier": {
      "get": {
        "tags": [
          "super::routes::knowledge"
        ],
        "summary": "Read a base's tier, its provenance, and what publicizing it would release.",
        "description": "A plain read: it is the Knowledge view asking about the user's own base, and\nthe barrier Task 10C installs is for model callers. No proof-of-user, because\nnothing is changed.",
        "operationId": "get_kb_tier",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The base's tier and what a publicize would release",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/KbTierResponse"
                }
              }
            }
          },
          "404": {
            "description": "Not found"
          }
        }
      },
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "summary": "Move a knowledge base's privacy tier, on the user's behalf (issue #56 DR-18).",
        "description": "The ONLY route in the tree that can LOWER one. It is user-only (DR-16's\n`X-User-Action`, the same header and the same key Task 18A installs — not a\nsecond one), it works in both directions, and the change carries its\nprovenance into `.kb-tiers` so a released base stays distinguishable from one\nthat was always public.\n\n⚠ **There is no `kb_set_tier` tool and there must never be one.** A model\nraises a tier as a side effect of writing (Task 10B, raise-only) and can do\nnothing else.\n\n⚠ **The typed confirmation is not enforced here, and that is deliberate**,\nunlike `POST /sessions/{id}/declassify` where the daemon re-derives the grade.\nA session's grade depends on server state (its stored provenance), so a client\ncould otherwise claim the weak control for a chat that no longer qualifies.\nA base's grade depends only on the DIRECTION, which the request itself states:\na publicize with no phrase is exactly what the request says it wants, and the\nphrase's job — making the user check *which* base — is a property of the\ndialog, not a claim about server state. What the daemon enforces is the thing\na client cannot fake: the proof that a human asked at all.",
        "operationId": "set_kb_tier",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Knowledge base ID",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetKbTierBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The base's tier after the change",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/KbTierResponse"
                }
              }
            }
          },
          "403": {
            "description": "Refused by a privacy boundary: changing a knowledge base's privacy is the user's decision and the request carried no proof it came from them, or this daemon holds no user-action key at all (body = plain text)"
          },
          "404": {
            "description": "Not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/knowledge/check-model": {
      "post": {
        "tags": [
          "super::routes::knowledge"
        ],
        "operationId": "check_model",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CheckModelBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Model responded OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CheckModelResponse"
                }
              }
            }
          },
          "502": {
            "description": "Model is unreachable / invalid",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CheckModelResponse"
                }
              }
            }
          }
        }
      }
    },
    "/llamacpp/delete": {
      "post": {
        "tags": [
          "super::routes::llamacpp"
        ],
        "operationId": "llamacpp_delete",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LlamaCppDeleteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Cached Llama Server fallback model removed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LlamaCppDeleteResponse"
                }
              }
            }
          },
          "400": {
            "description": "Unknown model name"
          }
        }
      }
    },
    "/llamacpp/ensure": {
      "post": {
        "tags": [
          "super::routes::llamacpp"
        ],
        "operationId": "llamacpp_ensure",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LlamaCppEnsureRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Sidecar start initiated (poll /llamacpp/status)",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LlamaCppStatusResponse"
                }
              }
            }
          },
          "400": {
            "description": "Unknown model name"
          }
        }
      }
    },
    "/llamacpp/status": {
      "get": {
        "tags": [
          "super::routes::llamacpp"
        ],
        "operationId": "llamacpp_status",
        "responses": {
          "200": {
            "description": "Sidecar status and curated model catalog",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LlamaCppStatusResponse"
                }
              }
            }
          }
        }
      }
    },
    "/llamacpp/stop": {
      "post": {
        "tags": [
          "super::routes::llamacpp"
        ],
        "operationId": "llamacpp_stop",
        "responses": {
          "200": {
            "description": "Sidecar stopped",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LlamaCppStatusResponse"
                }
              }
            }
          }
        }
      }
    },
    "/llamacpp/warmup": {
      "post": {
        "tags": [
          "super::routes::llamacpp"
        ],
        "operationId": "llamacpp_warmup",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LlamaCppWarmupRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Model loaded and produced a test completion",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LlamaCppWarmupResponse"
                }
              }
            }
          },
          "400": {
            "description": "Unknown model name"
          },
          "502": {
            "description": "Model failed to produce a warm-up completion"
          }
        }
      }
    },
    "/memory/delete_category": {
      "post": {
        "tags": [
          "Memory"
        ],
        "operationId": "memory_delete_category",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MemoryDeleteCategoryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The category was deleted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryDeleteCategoryResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid category, or a local scope with no working_dir"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "404": {
            "description": "No such category"
          },
          "409": {
            "description": "The category changed since it was listed"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/memory/delete_entry": {
      "post": {
        "tags": [
          "Memory"
        ],
        "operationId": "memory_delete_entry",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MemoryDeleteEntryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The memory was deleted",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryDeleteEntryResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid category, or a local scope with no working_dir"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "404": {
            "description": "No memory at that position"
          },
          "409": {
            "description": "The category changed since it was listed"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/memory/inventory": {
      "get": {
        "tags": [
          "Memory"
        ],
        "operationId": "memory_inventory",
        "parameters": [
          {
            "name": "working_dir",
            "in": "query",
            "description": "The project directory whose local store to list. Omit for global only.",
            "required": false,
            "schema": {
              "type": "string",
              "nullable": true
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Everything both memory stores hold",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MemoryInventoryResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "A store could not be read"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/privacy/disclosure": {
      "get": {
        "tags": [
          "super::routes::config_management"
        ],
        "summary": "The disclosure copy, and whether it has been acknowledged.",
        "description": "⚠ **Deliberately does NOT consult the master privacy switch.** DR-15 turns\noff gates, the ratchet and refusals; it does not turn off the truth, and with\nenforcement off the exposure is *larger*. Every other privacy route in this\nfile reads the switch, which is exactly why wiring this one the same way is\nthe plausible mistake.",
        "operationId": "get_privacy_disclosure",
        "responses": {
          "200": {
            "description": "The one copy of the non-private-model disclosure, plus whether this install has acknowledged it",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PrivacyDisclosureResponse"
                }
              }
            }
          }
        }
      }
    },
    "/privacy/disclosure/ack": {
      "post": {
        "tags": [
          "super::routes::config_management"
        ],
        "summary": "Record that the user has read the disclosure.",
        "description": "⚠ **DR-16's proof-of-user, unconditionally.** This is the one thing making\nDR-17's accepted risks acceptable, so a caller holding nothing but the daemon\nsecret — which AR-11 measured to be recoverable from inside the daemon, i.e.\nthe model — must not be able to acknowledge on the user's behalf. Unlike\n`upsert_config`'s guard this is NOT additionally gated on the master privacy\nswitch: turning enforcement off must not hand the model the dismiss button.",
        "operationId": "ack_privacy_disclosure",
        "responses": {
          "200": {
            "description": "Acknowledged"
          },
          "403": {
            "description": "Refused: acknowledging the disclosure is a user act, and this request carried no proof it came from the user"
          },
          "500": {
            "description": "The acknowledgement could not be written"
          }
        }
      }
    },
    "/reply": {
      "post": {
        "tags": [
          "workspace"
        ],
        "operationId": "reply",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChatRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Streaming response initiated: either a NEW turn, or an attachment to the turn this `turn_id` already named, replayed from `from_seq` and then followed live",
            "content": {
              "text/event-stream": {
                "schema": {
                  "$ref": "#/components/schemas/MessageEvent"
                }
              }
            }
          },
          "202": {
            "description": "A proven user reply was retained as steering for a delegated child that is still initializing"
          },
          "403": {
            "description": "Refused by a privacy boundary (issue #56 Task 58 / #47): the named chat is private (or absent, and an unproven caller is told the same thing for both) and the request carried no proof it came from the user (body = plain text)"
          },
          "409": {
            "description": "A DIFFERENT turn is already in flight for this session, or the supplied `conversation_so_far` is missing messages the server holds (nothing was written; re-read the session and retry)"
          },
          "424": {
            "description": "Agent not initialized"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/reset": {
      "post": {
        "tags": [
          "App Reset"
        ],
        "operationId": "reset_app_data",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Selected app data was reset",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResetResponse"
                }
              }
            }
          },
          "400": {
            "description": "No reset category was selected",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResetErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "Reset is blocked by active work",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResetErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Reset failed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResetErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/reset/preview": {
      "get": {
        "tags": [
          "App Reset"
        ],
        "operationId": "preview_reset",
        "responses": {
          "200": {
            "description": "Counts of data affected by each reset category",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResetPreviewResponse"
                }
              }
            }
          },
          "500": {
            "description": "Could not inspect reset data",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResetErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/schedule/create": {
      "post": {
        "tags": [
          "schedule"
        ],
        "operationId": "create_schedule",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateScheduleRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Scheduled job created successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduledJob"
                }
              }
            }
          },
          "400": {
            "description": "Invalid schedule name, cron expression or workflow file",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "409": {
            "description": "Job ID already exists",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/schedule/delete/{id}": {
      "delete": {
        "tags": [
          "schedule"
        ],
        "operationId": "delete_schedule",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "ID of the schedule to delete",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Scheduled job deleted successfully"
          },
          "404": {
            "description": "Scheduled job not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/schedule/list": {
      "get": {
        "tags": [
          "schedule"
        ],
        "operationId": "list_schedules",
        "responses": {
          "200": {
            "description": "A list of scheduled jobs",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListSchedulesResponse"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/schedule/{id}": {
      "put": {
        "tags": [
          "schedule"
        ],
        "operationId": "update_schedule",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "ID of the schedule to update",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateScheduleRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Scheduled job updated successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduledJob"
                }
              }
            }
          },
          "400": {
            "description": "Cannot update a currently running job or invalid request"
          },
          "404": {
            "description": "Scheduled job not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/schedule/{id}/inspect": {
      "get": {
        "tags": [
          "schedule"
        ],
        "operationId": "inspect_running_job",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "ID of the schedule to inspect",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Running job information",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InspectJobResponse"
                }
              }
            }
          },
          "404": {
            "description": "Scheduled job not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/schedule/{id}/kill": {
      "post": {
        "tags": [
          "schedule"
        ],
        "operationId": "kill_running_job",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Running job killed successfully"
          }
        }
      }
    },
    "/schedule/{id}/pause": {
      "post": {
        "tags": [
          "schedule"
        ],
        "operationId": "pause_schedule",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "ID of the schedule to pause",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Scheduled job paused successfully"
          },
          "400": {
            "description": "Cannot pause a currently running job"
          },
          "404": {
            "description": "Scheduled job not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/schedule/{id}/run_now": {
      "post": {
        "tags": [
          "schedule"
        ],
        "operationId": "run_now_handler",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "ID of the schedule to run",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Scheduled job triggered successfully, returns new session ID",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunNowResponse"
                }
              }
            }
          },
          "404": {
            "description": "Scheduled job not found"
          },
          "500": {
            "description": "Internal server error when trying to run the job"
          }
        }
      }
    },
    "/schedule/{id}/sessions": {
      "get": {
        "tags": [
          "schedule"
        ],
        "operationId": "sessions_handler",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "ID of the schedule",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": true,
            "schema": {
              "type": "integer",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A list of session display info",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/SessionDisplayInfo"
                  }
                }
              }
            }
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/schedule/{id}/unpause": {
      "post": {
        "tags": [
          "schedule"
        ],
        "operationId": "unpause_schedule",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "ID of the schedule to unpause",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Scheduled job unpaused successfully"
          },
          "404": {
            "description": "Scheduled job not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/sessions": {
      "get": {
        "tags": [
          "Session Management"
        ],
        "operationId": "list_sessions",
        "parameters": [
          {
            "name": "include_subagents",
            "in": "query",
            "description": "Include sub_agent sessions (grouped under parent_session_id); default false",
            "required": false,
            "schema": {
              "type": "boolean",
              "nullable": true
            }
          }
        ],
        "responses": {
          "200": {
            "description": "List of available sessions retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionListResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/activity": {
      "get": {
        "tags": [
          "Session Management"
        ],
        "operationId": "get_session_activity",
        "parameters": [
          {
            "name": "days",
            "in": "query",
            "description": "Calendar days to report (default 155, clamped to 1..=371)",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int64",
              "nullable": true
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Per-day usage for the Home heatmap",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivityWindow"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/changes": {
      "get": {
        "tags": [
          "super::routes::session_meta"
        ],
        "summary": "Wait for one of `ids`' rows to change, then say what changed.",
        "description": "Returns immediately when the caller is already behind. A timeout is not an\nerror: the body carries the current revision and the caller polls again,\nwhich is also how it survives a daemon restart — the revision resets to 0,\nthe client sees a LOWER number than it holds, and refetches.\n\n⚠ **`truncated` is not advisory.** It means the caller fell further behind\nthan the ring holds, so `changes` is a partial history. Applying it and\nbelieving yourself current is the stale-row bug this endpoint exists to end.",
        "operationId": "session_changes",
        "parameters": [
          {
            "name": "since",
            "in": "query",
            "description": "The last revision this client applied. `0` (or absent) means \"tell me\nthe current revision\", which is how a fresh client establishes a\nbaseline without a refetch.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int64",
              "minimum": 0
            }
          },
          {
            "name": "ids",
            "in": "query",
            "description": "Comma-separated session ids this client has open. Only these rows are\nread, so a client watching nothing costs nothing.\n\n⚠ It scopes what can be DETECTED, not what is returned: the ring is\nprocess-wide, so a delta may name a chat this caller did not list. The\ncaller ignores those, exactly as `CatalogChanged.session_id` is ignored\nby clients it does not concern.",
            "required": false,
            "schema": {
              "type": "string",
              "nullable": true
            }
          },
          {
            "name": "timeout_ms",
            "in": "query",
            "description": "How long to park, in milliseconds. Clamped to [`MAX_WAIT`].",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int64",
              "nullable": true,
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The session-row delta since `since`",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionMetaDelta"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          }
        }
      }
    },
    "/sessions/import": {
      "post": {
        "tags": [
          "Session Management"
        ],
        "operationId": "import_session",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Session imported successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Session"
                }
              }
            }
          },
          "400": {
            "description": "Bad request - Invalid JSON"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/insights": {
      "get": {
        "tags": [
          "Session Management"
        ],
        "operationId": "get_session_insights",
        "responses": {
          "200": {
            "description": "Session insights retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionInsights"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/running": {
      "get": {
        "tags": [
          "workspace"
        ],
        "operationId": "running_sessions",
        "responses": {
          "200": {
            "description": "Sessions with a turn in flight",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunningSessionsResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          }
        }
      }
    },
    "/sessions/sidebar": {
      "get": {
        "tags": [
          "Session Management"
        ],
        "operationId": "list_sidebar_sessions",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "description": "Session summaries per page (default 10, clamped to 1..=50)",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "nullable": true,
              "minimum": 0
            }
          },
          {
            "name": "offset",
            "in": "query",
            "description": "Number of session summaries to skip",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "nullable": true,
              "minimum": 0
            }
          },
          {
            "name": "include_subagents",
            "in": "query",
            "description": "Include sub_agent sessions (grouped under parent_session_id); default false",
            "required": false,
            "schema": {
              "type": "boolean",
              "nullable": true
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Paginated lightweight session summaries for the sidebar",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SidebarSessionListResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}": {
      "get": {
        "tags": [
          "Session Management"
        ],
        "operationId": "get_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "metadata_only",
            "in": "query",
            "description": "Omit conversation history when only session metadata is needed",
            "required": false,
            "schema": {
              "type": "boolean",
              "nullable": true
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Session history retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Session"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "403": {
            "description": "Refused by a privacy boundary (issue #56 Task 58 / #47): the named chat is private (or absent, and an unproven caller is told the same thing for both) and the request carried no proof it came from the user (body = plain text)"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      },
      "delete": {
        "tags": [
          "Session Management"
        ],
        "operationId": "delete_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Session deleted successfully"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}/declassify": {
      "post": {
        "tags": [
          "Session Management"
        ],
        "summary": "Mark a private chat public (issue #56 §12.4/§12.5).",
        "description": "The ONLY route in the tree that lowers a session's classification. It is\nuser-only (DR-16's `X-User-Action`), it is graded on the chat's provenance,\nand every use leaves a row in `classification_audit` — a declassified session\nmust never become indistinguishable from one that was always public.",
        "operationId": "declassify_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session to declassify",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeclassifySessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "The chat is public",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeclassifySessionResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request - invalid session id, or the typed confirmation did not match (body = plain text)"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "403": {
            "description": "Refused by a privacy boundary (issue #56 §12.4): lowering a chat's classification is the user's decision, and the request carried no proof it came from them (body = plain text)"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}/diverge": {
      "post": {
        "tags": [
          "Session Management"
        ],
        "summary": "Diverge (branch) a session into a brand-new session that inherits the full",
        "description": "conversation history and metadata of the original, while leaving the\noriginal completely untouched. Unlike `edit_message` with `Diverge`, no\ntruncation is performed — the entire history is carried over so the user can\ncontinue a fresh conversation from exactly where they left off.",
        "operationId": "diverge_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session to diverge from",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DivergeSessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Session diverged successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DivergeSessionResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request - Invalid session id or name too long"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "403": {
            "description": "Refused by a privacy boundary (issue #56 DR-19): the source chat is private, so the branch would inherit its private model, and the request carried no proof it came from the user (body = plain text)"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}/edit_message": {
      "post": {
        "tags": [
          "Session Management"
        ],
        "summary": "Prepare a session for an edited message.",
        "description": "`diverge` (the default) copies the history into a NEW session, trimmed at the\nedited message, and returns that session's id. The original is untouched.\n\n`edit` truncates THIS session in place, dropping every message from\n`timestamp` onwards. Because that destroys history a concurrent writer may\nalready have been told was saved, the cut is refused — with nothing deleted —\nwhile a turn is in flight, and it never reaches past the rows the server read\nwhen it took the request. You may additionally send `expectedMessageIds`, the\nids of every message your view of the session holds: the cut is then also\nrefused if the session holds a message your view does not name, and the 409\nbody's `missing_message_ids` says which, so you can re-read and retry.",
        "operationId": "edit_message",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EditMessageRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Session prepared for editing - frontend should submit the edited message",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EditMessageResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request - invalid session id"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "403": {
            "description": "Refused by a privacy boundary (issue #56 DR-19): `editType: diverge` on a private chat branches it into a new chat that inherits its private model, and the request carried no proof it came from the user (body = plain text)"
          },
          "404": {
            "description": "Session or message not found"
          },
          "409": {
            "description": "A turn is in flight for this session, or a supplied `expectedMessageIds` is missing messages the server holds (nothing was deleted; re-read the session and retry)"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}/events": {
      "get": {
        "tags": [
          "workspace"
        ],
        "operationId": "observe_session_events",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Session to observe",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Read-only observer stream of the session's live events",
            "content": {
              "text/event-stream": {
                "schema": {
                  "$ref": "#/components/schemas/MessageEvent"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid secret key"
          },
          "403": {
            "description": "Out of reach - a private or unreadable session named without the user-action proof"
          },
          "404": {
            "description": "No such session"
          }
        }
      }
    },
    "/sessions/{session_id}/export": {
      "get": {
        "tags": [
          "Session Management"
        ],
        "operationId": "export_session",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Session exported successfully",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "403": {
            "description": "Out of reach - a private or unreadable session named without the user-action proof"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}/extensions": {
      "get": {
        "tags": [
          "Session Management"
        ],
        "operationId": "get_session_extensions",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Session extensions retrieved successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionExtensionsResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}/name": {
      "put": {
        "tags": [
          "Session Management"
        ],
        "operationId": "update_session_name",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateSessionNameRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Session name updated successfully"
          },
          "400": {
            "description": "Bad request - Name too long (max 200 characters)"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}/usage": {
      "get": {
        "tags": [
          "Session Management"
        ],
        "operationId": "get_session_usage",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Per-model usage for the session",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionModelUsageResponse"
                }
              }
            }
          },
          "400": {
            "description": "Invalid session id"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "404": {
            "description": "Session not found"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/sessions/{session_id}/user_workflow_values": {
      "put": {
        "tags": [
          "Session Management"
        ],
        "operationId": "update_session_user_workflow_values",
        "parameters": [
          {
            "name": "session_id",
            "in": "path",
            "description": "Unique identifier for the session",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateSessionUserWorkflowValuesRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Session user workflow values updated successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UpdateSessionUserWorkflowValuesResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "404": {
            "description": "Session not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/skills/catalog": {
      "get": {
        "tags": [
          "skills"
        ],
        "summary": "The canonical catalog: every root, every skill, every bundle, and the",
        "description": "composed enablement for one conversation.",
        "operationId": "skill_catalog_handler",
        "parameters": [
          {
            "name": "session_id",
            "in": "query",
            "description": "Compose the machine-wide catalog with this conversation's override.\nOmit for the machine-wide view — what a new chat would start with.",
            "required": false,
            "schema": {
              "type": "string",
              "nullable": true
            }
          },
          {
            "name": "refresh",
            "in": "query",
            "description": "Rescan the filesystem before answering, instead of reusing the cached\nsnapshot. The interface sets this after an install it did not perform\nitself (a marketplace click, a `.brxt` drop) and after a `CatalogChanged`\nnotice, since a change made by another process may land inside the\nsnapshot's one-second mtime window.",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The catalog",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogView"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid or missing secret key"
          },
          "500": {
            "description": "This conversation's skill state is unreadable"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/skills/packages/install": {
      "post": {
        "tags": [
          "skills"
        ],
        "summary": "Import a skill package, atomically, and refresh the catalog.",
        "operationId": "install_skill_package",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Installed, or a question to answer",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportResult"
                }
              }
            }
          },
          "400": {
            "description": "The source could not be read or installed"
          },
          "401": {
            "description": "Unauthorized - invalid or missing secret key"
          },
          "410": {
            "description": "That preview has expired"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/skills/packages/preview": {
      "post": {
        "tags": [
          "skills"
        ],
        "summary": "Look at a source without installing anything.",
        "operationId": "preview_skill_package",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ImportRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "What an install would do",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportResult"
                }
              }
            }
          },
          "400": {
            "description": "The source could not be read"
          },
          "401": {
            "description": "Unauthorized - invalid or missing secret key"
          },
          "410": {
            "description": "That preview has expired"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/skills/packages/remove": {
      "post": {
        "tags": [
          "skills"
        ],
        "summary": "Remove an installed package or single skill, and every component with it.",
        "operationId": "remove_skill_package",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemovePackageRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Removed",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PackageSummary"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid or missing secret key"
          },
          "404": {
            "description": "No such package"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/skills/refresh": {
      "post": {
        "tags": [
          "skills"
        ],
        "summary": "Rescan the filesystem and publish a new catalog to every live conversation.",
        "description": "The endpoint an install calls — a marketplace skill, a dropped `.zip`, a\n`.brxt` extension whose `skills/` subdirectory becomes a new root, or\nworktree 4's `CatalogChanged` notice. It rescans unconditionally rather than\nconsulting mtimes, because the caller already knows something changed.",
        "operationId": "refresh_skill_catalog",
        "parameters": [
          {
            "name": "session_id",
            "in": "query",
            "description": "Compose the machine-wide catalog with this conversation's override.\nOmit for the machine-wide view — what a new chat would start with.",
            "required": false,
            "schema": {
              "type": "string",
              "nullable": true
            }
          },
          {
            "name": "refresh",
            "in": "query",
            "description": "Rescan the filesystem before answering, instead of reusing the cached\nsnapshot. The interface sets this after an install it did not perform\nitself (a marketplace click, a `.brxt` drop) and after a `CatalogChanged`\nnotice, since a change made by another process may land inside the\nsnapshot's one-second mtime window.",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The freshly scanned catalog",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogView"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid or missing secret key"
          },
          "500": {
            "description": "This conversation's skill state is unreadable"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/skills/session": {
      "post": {
        "tags": [
          "skills"
        ],
        "summary": "Enable or disable skills for one conversation, and hand back what the model",
        "description": "will see on its next turn.",
        "operationId": "set_session_skills",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SessionSkillsRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Applied",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionSkillsResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - invalid or missing secret key"
          },
          "404": {
            "description": "No such conversation"
          },
          "500": {
            "description": "The override could not be persisted"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/status": {
      "get": {
        "tags": [
          "super::routes::status"
        ],
        "operationId": "status",
        "responses": {
          "200": {
            "description": "ok",
            "content": {
              "text/plain": {
                "schema": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "/system_info": {
      "get": {
        "tags": [
          "super::routes::status"
        ],
        "operationId": "system_info",
        "responses": {
          "200": {
            "description": "System information",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SystemInfo"
                }
              }
            }
          }
        }
      }
    },
    "/tunnel/start": {
      "post": {
        "tags": [
          "super::routes::tunnel"
        ],
        "summary": "Start the tunnel",
        "operationId": "start_tunnel",
        "responses": {
          "200": {
            "description": "Tunnel started successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TunnelInfo"
                }
              }
            }
          },
          "400": {
            "description": "Bad request",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/tunnel/status": {
      "get": {
        "tags": [
          "super::routes::tunnel"
        ],
        "summary": "Get tunnel info",
        "operationId": "get_tunnel_status",
        "responses": {
          "200": {
            "description": "Tunnel info",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TunnelInfo"
                }
              }
            }
          }
        }
      }
    },
    "/tunnel/stop": {
      "post": {
        "tags": [
          "super::routes::tunnel"
        ],
        "summary": "Stop the tunnel",
        "operationId": "stop_tunnel",
        "responses": {
          "200": {
            "description": "Tunnel stopped successfully"
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/usage/report": {
      "get": {
        "tags": [
          "Usage"
        ],
        "operationId": "get_usage_report",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "description": "Inclusive window start, unix seconds (default: 30 days ago)",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int64",
              "nullable": true
            }
          },
          {
            "name": "to",
            "in": "query",
            "description": "Inclusive window end, unix seconds (default: now)",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int64",
              "nullable": true
            }
          },
          {
            "name": "group",
            "in": "query",
            "description": "Bucketing: day (default), model, or day_model",
            "required": false,
            "schema": {
              "allOf": [
                {
                  "$ref": "#/components/schemas/UsageGroup"
                }
              ],
              "nullable": true
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Usage report",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UsageReportResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/usage/summary": {
      "get": {
        "tags": [
          "Usage"
        ],
        "operationId": "get_usage_summary",
        "responses": {
          "200": {
            "description": "Month-to-date usage vs the configured budget",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UsageSummaryResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "Internal server error"
          }
        },
        "security": [
          {
            "api_key": []
          }
        ]
      }
    },
    "/workflows/create": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "create_workflow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateWorkflowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Workflow created successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CreateWorkflowResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request"
          },
          "412": {
            "description": "Precondition failed - Agent not available"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/workflows/decode": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "decode_workflow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DecodeWorkflowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Workflow decoded successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecodeWorkflowResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request"
          }
        }
      }
    },
    "/workflows/delete": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "delete_workflow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeleteWorkflowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Workflow deleted successfully"
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "404": {
            "description": "Workflow not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/workflows/encode": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "encode_workflow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EncodeWorkflowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Workflow encoded successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EncodeWorkflowResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request"
          }
        }
      }
    },
    "/workflows/list": {
      "get": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "list_workflows",
        "responses": {
          "200": {
            "description": "Get workflow list successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListWorkflowResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized - Invalid or missing API key"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/workflows/parse": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "parse_workflow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ParseWorkflowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Workflow parsed successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ParseWorkflowResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request - Invalid workflow format",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/workflows/save": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "save_workflow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SaveWorkflowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Workflow saved to file successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SaveWorkflowResponse"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "404": {
            "description": "Not found",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          },
          "500": {
            "description": "Internal server error",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    },
    "/workflows/scan": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "scan_workflow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ScanWorkflowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Workflow scanned successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScanWorkflowResponse"
                }
              }
            }
          }
        }
      }
    },
    "/workflows/schedule": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "schedule_workflow",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ScheduleWorkflowRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Workflow scheduled successfully"
          },
          "404": {
            "description": "Workflow not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/workflows/slash-command": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "set_workflow_slash_command",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetSlashCommandRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Slash command set successfully"
          },
          "404": {
            "description": "Workflow not found"
          },
          "500": {
            "description": "Internal server error"
          }
        }
      }
    },
    "/workflows/to-yaml": {
      "post": {
        "tags": [
          "Workflow Management"
        ],
        "operationId": "workflow_to_yaml",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WorkflowToYamlRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "Workflow converted to YAML successfully",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkflowToYamlResponse"
                }
              }
            }
          },
          "400": {
            "description": "Bad request - Failed to convert workflow to YAML",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "AbandonContinuationLeaseRequest": {
        "type": "object",
        "required": [
          "session_id",
          "continuation_lease"
        ],
        "properties": {
          "continuation_lease": {
            "type": "string"
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "AbandonContinuationLeaseResponse": {
        "type": "object",
        "required": [
          "resolution"
        ],
        "properties": {
          "resolution": {
            "type": "string"
          }
        }
      },
      "ActionRequired": {
        "type": "object",
        "required": [
          "data"
        ],
        "properties": {
          "data": {
            "$ref": "#/components/schemas/ActionRequiredData"
          }
        }
      },
      "ActionRequiredData": {
        "oneOf": [
          {
            "type": "object",
            "required": [
              "id",
              "toolName",
              "arguments",
              "actionType"
            ],
            "properties": {
              "actionType": {
                "type": "string",
                "enum": [
                  "toolConfirmation"
                ]
              },
              "arguments": {
                "$ref": "#/components/schemas/JsonObject"
              },
              "id": {
                "type": "string"
              },
              "preview": {
                "allOf": [
                  {
                    "$ref": "#/components/schemas/ToolPreview"
                  }
                ],
                "nullable": true
              },
              "prompt": {
                "type": "string",
                "nullable": true
              },
              "risk": {
                "allOf": [
                  {
                    "$ref": "#/components/schemas/ToolRisk"
                  }
                ],
                "nullable": true
              },
              "toolName": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "required": [
              "id",
              "message",
              "requested_schema",
              "actionType"
            ],
            "properties": {
              "actionType": {
                "type": "string",
                "enum": [
                  "elicitation"
                ]
              },
              "id": {
                "type": "string"
              },
              "message": {
                "type": "string"
              },
              "requested_schema": {}
            }
          },
          {
            "type": "object",
            "required": [
              "id",
              "user_data",
              "actionType"
            ],
            "properties": {
              "actionType": {
                "type": "string",
                "enum": [
                  "elicitationResponse"
                ]
              },
              "id": {
                "type": "string"
              },
              "user_data": {}
            }
          },
          {
            "type": "object",
            "description": "#107: credentials a *trusted surface* must collect and write straight to\ntheir destination.\n\nThe card carries key names, labels and a destination — and deliberately\nnothing a value could sit in. The answer never comes back through this\ntype: the surface stores the values itself and resolves the parked call\nwith `SecretsConfigured { configured_keys }`, so a credential is never\npersisted into a session row, replayed into a later prompt, or flattened\ninto a child agent's transcript. There is deliberately no\n`SecretResponse` sibling to `ElicitationResponse`; adding one would undo\nthe entire guarantee.",
            "required": [
              "id",
              "prompt",
              "keys",
              "destination",
              "actionType"
            ],
            "properties": {
              "actionType": {
                "type": "string",
                "enum": [
                  "secretRequest"
                ]
              },
              "destination": {
                "$ref": "#/components/schemas/SecretDestination"
              },
              "id": {
                "type": "string"
              },
              "keys": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/SecretKeyRequest"
                }
              },
              "prompt": {
                "type": "string"
              }
            }
          }
        ],
        "discriminator": {
          "propertyName": "actionType"
        }
      },
      "ActiveKbResponse": {
        "type": "object",
        "required": [
          "kb_ids",
          "hidden_kbs"
        ],
        "properties": {
          "active_kb": {
            "type": "string",
            "description": "Deprecated mirror of `primary_kb`.",
            "nullable": true
          },
          "hidden_kbs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "kb_ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The session's knowledge bases, sorted. Every one is searchable and\nreadable; there is no narrower \"active\" list."
          },
          "primary_kb": {
            "type": "string",
            "description": "The KB-less write target. Always a member of `kb_ids`, or `null`.",
            "nullable": true
          }
        }
      },
      "ActiveTurnRef": {
        "type": "object",
        "description": "A pointer to a live turn, handed to a client that needs to attach to it.",
        "required": [
          "turn_id"
        ],
        "properties": {
          "turn_id": {
            "type": "string",
            "description": "Post this as `ChatRequest.turn_id` to attach. `POST /reply` accepts\neither this server-assigned id or the idempotency key the turn's original\ncaller chose, so a client can use whichever it happens to hold."
          }
        }
      },
      "ActiveWorkItemDto": {
        "type": "object",
        "description": "One active unit of work, kind-tagged so the GUI can render/route uniformly.",
        "required": [
          "id",
          "kind",
          "title",
          "cancellable"
        ],
        "properties": {
          "cancellable": {
            "type": "boolean",
            "description": "Whether `POST /active_work/{id}/cancel` can stop it."
          },
          "detail": {
            "type": "string",
            "description": "Extra context (full command, child session id, workflow source).",
            "nullable": true
          },
          "id": {
            "type": "string",
            "description": "Unique id; also the handle for `POST /active_work/{id}/cancel`."
          },
          "kind": {
            "type": "string",
            "description": "`background_job`, `subagent`, `detached_turn`, or `scheduled_run`."
          },
          "runningForSeconds": {
            "type": "integer",
            "format": "int64",
            "description": "Wall-clock seconds this item has been running where known.",
            "nullable": true
          },
          "sessionId": {
            "type": "string",
            "description": "Owning/related session id where known.",
            "nullable": true
          },
          "startedAt": {
            "type": "string",
            "description": "RFC3339 start time where known.",
            "nullable": true
          },
          "title": {
            "type": "string",
            "description": "Short human-readable label (command, task prompt, or schedule id)."
          }
        }
      },
      "ActiveWorkResponse": {
        "type": "object",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActiveWorkItemDto"
            }
          }
        }
      },
      "ActivityWindow": {
        "type": "object",
        "description": "The Home heatmap payload.",
        "required": [
          "start",
          "end",
          "maxSessions",
          "maxTokens",
          "tokensComplete",
          "currentStreak",
          "longestStreak",
          "days"
        ],
        "properties": {
          "currentStreak": {
            "type": "integer",
            "format": "int64"
          },
          "days": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DailyActivity"
            },
            "description": "Only days with activity. The client fills the rest of the grid with level 0."
          },
          "end": {
            "type": "string"
          },
          "longestStreak": {
            "type": "integer",
            "format": "int64"
          },
          "maxSessions": {
            "type": "integer",
            "format": "int64"
          },
          "maxTokens": {
            "type": "integer",
            "format": "int64"
          },
          "start": {
            "type": "string"
          },
          "tokensComplete": {
            "type": "boolean",
            "description": "False when at least one event in the window lacks billed-token accounting.\nConsult each day's `tokens_complete` for display semantics."
          }
        }
      },
      "AddExtensionRequest": {
        "type": "object",
        "required": [
          "session_id",
          "config"
        ],
        "properties": {
          "config": {
            "$ref": "#/components/schemas/ExtensionConfig"
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "AffiliationInstitution": {
        "type": "object",
        "description": "One institution, as a surface that has to *print* it needs it (issue #56,\nDR-26).\n\nBoth halves travel, and neither is derivable from the other. The `id` is the\nnormalised slug — what `registry.json` publishes, what `baam.html`'s\n`data-affiliation` carries, what every cross-affiliation warning prints and\nwhat a user quotes in a support conversation. The `display_name` is what the\nregistry publishes for it, and is **absent** for an institution the registry\ndoes not know.\n\n⚠ **An absent display name is not an error and must never render as\nnothing.** Task 47 rules that an affiliation naming an unpublished\ninstitution is a *mismatch* whose raw id is surfaced; dropping the row\nbecause it has no pretty name would make a real constraint disappear from the\none surface a user can see it on. This mirrors `privacy::affiliation::label`,\nwhich falls back to the bare id for exactly that reason.",
        "required": [
          "id"
        ],
        "properties": {
          "display_name": {
            "type": "string",
            "description": "The registry's published display name (`UCSF`), or `None`.",
            "nullable": true
          },
          "id": {
            "type": "string",
            "description": "The normalised slug (`ucsf`)."
          }
        }
      },
      "AgentAvailability": {
        "type": "object",
        "description": "One CLI's full situation, as the settings card renders it.",
        "required": [
          "kind",
          "providerId",
          "displayName",
          "auth",
          "loginCommand",
          "installHint"
        ],
        "properties": {
          "auth": {
            "$ref": "#/components/schemas/AuthState"
          },
          "displayName": {
            "type": "string"
          },
          "installHint": {
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/CodingAgentKind"
          },
          "loginCommand": {
            "type": "string",
            "description": "The command to run when `auth` says the user must act."
          },
          "path": {
            "type": "string",
            "description": "Absolute path we resolved, when we found one.",
            "nullable": true
          },
          "providerId": {
            "type": "string"
          },
          "version": {
            "type": "string",
            "description": "Raw first line of `--version`.",
            "nullable": true
          }
        }
      },
      "AgentInitializationError": {
        "type": "object",
        "required": [
          "code",
          "message",
          "retryable"
        ],
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          },
          "retryable": {
            "type": "boolean"
          }
        }
      },
      "Ambiguity": {
        "type": "object",
        "description": "Why the caller has to choose, and what the choices are.",
        "required": [
          "reason",
          "components"
        ],
        "properties": {
          "components": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The component names the caller is choosing among."
          },
          "reason": {
            "type": "string",
            "description": "One sentence for the dialog."
          }
        }
      },
      "Annotations": {
        "type": "object",
        "properties": {
          "audience": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Role"
            }
          },
          "lastModified": {
            "type": "string",
            "format": "date-time"
          },
          "priority": {
            "type": "number"
          }
        }
      },
      "AuthState": {
        "oneOf": [
          {
            "type": "object",
            "required": [
              "state"
            ],
            "properties": {
              "state": {
                "type": "string",
                "enum": [
                  "not_installed"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "state"
            ],
            "properties": {
              "state": {
                "type": "string",
                "enum": [
                  "signed_out"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Signed in on a subscription. This is the state the feature requires.",
            "required": [
              "state"
            ],
            "properties": {
              "account": {
                "type": "string",
                "nullable": true
              },
              "plan": {
                "type": "string",
                "description": "`\"max\"`, `\"pro\"`, … as the vendor reports it. Advisory only.",
                "nullable": true
              },
              "state": {
                "type": "string",
                "enum": [
                  "signed_in_subscription"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "state"
            ],
            "properties": {
              "state": {
                "type": "string",
                "enum": [
                  "signed_in_with_api_key"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "The probe ran but its output could not be understood. Carries the reason\nrather than collapsing to \"signed out\", because telling a user to log in\nwhen they already are is worse than admitting we do not know.",
            "required": [
              "detail",
              "state"
            ],
            "properties": {
              "detail": {
                "type": "string"
              },
              "state": {
                "type": "string",
                "enum": [
                  "indeterminate"
                ]
              }
            }
          }
        ],
        "description": "What the vendor CLI's credential store says right now.\n\n`SignedInWithApiKey` is a distinct state rather than an error: the CLI works,\nbut the run bills a metered API account instead of the subscription, which is\nthe entire thing this feature exists to avoid. The user is told, rather than\nsilently getting a bill.",
        "discriminator": {
          "propertyName": "state"
        }
      },
      "Author": {
        "type": "object",
        "properties": {
          "contact": {
            "type": "string",
            "nullable": true
          },
          "metadata": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "AuthorRequest": {
        "type": "object",
        "properties": {
          "contact": {
            "type": "string",
            "nullable": true
          },
          "metadata": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "CallToolRequest": {
        "type": "object",
        "required": [
          "session_id",
          "name",
          "arguments"
        ],
        "properties": {
          "arguments": {},
          "name": {
            "type": "string"
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "CallToolResponse": {
        "type": "object",
        "required": [
          "content",
          "is_error"
        ],
        "properties": {
          "_meta": {
            "nullable": true
          },
          "content": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Content"
            }
          },
          "is_error": {
            "type": "boolean"
          },
          "structured_content": {
            "nullable": true
          }
        }
      },
      "CallableToolCountQuery": {
        "type": "object",
        "required": [
          "session_id"
        ],
        "properties": {
          "session_id": {
            "type": "string"
          }
        }
      },
      "CallableToolCountResponse": {
        "type": "object",
        "required": [
          "count"
        ],
        "properties": {
          "count": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "CancelActiveWorkResponse": {
        "type": "object",
        "required": [
          "message"
        ],
        "properties": {
          "message": {
            "type": "string"
          }
        }
      },
      "CancelTurnConflict": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/CancelTurnConflictResponse"
          },
          {
            "$ref": "#/components/schemas/ContinuationLeaseErrorResponse"
          }
        ],
        "description": "The two fail-closed 409 shapes returned by `/agent/cancel`."
      },
      "CancelTurnConflictResponse": {
        "type": "object",
        "description": "A generation-conditional cancel addressed a different active turn.",
        "required": [
          "mismatch",
          "expected_turn_id",
          "active_turn_id"
        ],
        "properties": {
          "active_turn_id": {
            "type": "string"
          },
          "expected_turn_id": {
            "type": "string"
          },
          "mismatch": {
            "type": "boolean"
          }
        }
      },
      "CancelTurnRequest": {
        "type": "object",
        "description": "Request body for the addressable cancel route.",
        "required": [
          "session_id"
        ],
        "properties": {
          "continuation_owner_id": {
            "type": "string",
            "description": "Stable, per-window identifier for recovering a Stop-and-Send admission\nafter a renderer reload. It is an ownership label, not a credential;\nuser-action proof and the opaque lease remain the authorities.",
            "nullable": true
          },
          "continuation_pending": {
            "type": "boolean",
            "description": "The caller will submit a replacement turn after cancellation settles.\nThis is distinct from ordinary Stop because a delegated child's parent\nmust keep supervising across the gap between the two turns."
          },
          "expected_turn_id": {
            "type": "string",
            "description": "Cancel only this turn generation. A stale Stop request must never cancel\na successor that started in the same session after the UI queued it.",
            "nullable": true
          },
          "session_id": {
            "type": "string"
          },
          "wait_for_idle": {
            "type": "boolean",
            "description": "Wait until the exact cancelled turn has released the session lock before\nacknowledging. Stop-and-Send callers use this as their submission\nbarrier; ordinary Stop callers retain the immediate acknowledgement."
          }
        }
      },
      "CancelTurnResponse": {
        "type": "object",
        "description": "Response body for the addressable cancel route.",
        "required": [
          "cancelled",
          "settled"
        ],
        "properties": {
          "cancelled": {
            "type": "boolean",
            "description": "True when a running turn was found and its cancellation token tripped.\nFalse means there was nothing to cancel — which is a success, not an error."
          },
          "continuation_lease": {
            "type": "string",
            "description": "Opaque exact-generation admission for the replacement `/reply`.\nPresent only for a settled Stop-and-Send request.",
            "nullable": true
          },
          "settled": {
            "type": "boolean",
            "description": "True only after the cancelled turn's guard has retired. An idle session\nis already settled. An immediate cancellation acknowledgement may be\n`false` while the agent loop is still unwinding."
          },
          "turn_id": {
            "type": "string",
            "description": "The id of the turn that was cancelled, when there was one.",
            "nullable": true
          }
        }
      },
      "CarriedPage": {
        "type": "object",
        "required": [
          "source_path",
          "destination_path"
        ],
        "properties": {
          "destination_path": {
            "type": "string"
          },
          "identifier": {
            "type": "string",
            "description": "The page's identifier as the source wrote it, when it has one.",
            "nullable": true
          },
          "renamed_identifier": {
            "type": "string",
            "description": "Set only when the identifier collided with the destination's and was\nrenamed.",
            "nullable": true
          },
          "source_path": {
            "type": "string"
          }
        }
      },
      "CatalogBundle": {
        "type": "object",
        "description": "A directory of skills installed and removed as one unit.",
        "required": [
          "name",
          "displayName",
          "directory",
          "sourceRoot",
          "source",
          "skills",
          "builtin",
          "state"
        ],
        "properties": {
          "builtin": {
            "type": "boolean",
            "description": "Shipped with Biorouter, so the interface offers no Delete for it.\n\n⚠ **The bundle needs its own answer**, because `CatalogSkill.builtin`\ngates the Delete on a *skill* row and a bundle row is a different\ncontrol over a different directory.\n\n⚠ **This is defence in depth, not the live fix, and saying otherwise\ninvites someone to delete the real one.** Today the only shipped bundle\nis a Context, and `pickerBundles` strips Contexts before `SkillsView`\nrenders a row at all, so this flag cannot fire on it. What actually\nclosed the \"delete succeeds, toast confirms, next startup rewrites it\"\nregression on every surface is `skill_package::refuse_shipped`, which\n`biorouter skill remove` bypassed entirely while the interface gate held.\nThis field earns its place for the case the filter does not cover: a\nbundle that is seeded but not a Context, should one ever ship."
          },
          "directory": {
            "type": "string"
          },
          "displayName": {
            "type": "string",
            "description": "The package's own display name when a manifest supplied one, else the\ndirectory name."
          },
          "name": {
            "type": "string",
            "description": "The bundle directory name. This is the identifier `skills-config.json`\nlists to disable the whole bundle."
          },
          "package": {
            "allOf": [
              {
                "$ref": "#/components/schemas/PackageSummary"
              }
            ],
            "nullable": true
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Member skill names, sorted."
          },
          "source": {
            "$ref": "#/components/schemas/SkillSource"
          },
          "sourceRoot": {
            "type": "string"
          },
          "state": {
            "$ref": "#/components/schemas/SkillState"
          }
        }
      },
      "CatalogChangeReason": {
        "type": "string",
        "enum": [
          "install",
          "uninstall",
          "update",
          "enable",
          "disable",
          "import"
        ]
      },
      "CatalogChanged": {
        "type": "object",
        "required": [
          "revision",
          "reason"
        ],
        "properties": {
          "extensions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CatalogExtensionChange"
            }
          },
          "reason": {
            "$ref": "#/components/schemas/CatalogChangeReason"
          },
          "revision": {
            "type": "integer",
            "format": "int64",
            "description": "Monotonic per-daemon-process counter. A consumer that has applied a\nrevision >= this may drop the event; one that missed a gap refetches.",
            "minimum": 0
          },
          "sessionId": {
            "type": "string",
            "description": "The session the change was made from, when it was made from one.\n\n⚠ Present so a surface can offer \"attach to this chat\" — **not** a\ndelivery scope. The change is machine-wide and every session's inventory\nis stale after it.",
            "nullable": true
          },
          "skills": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CatalogSkillChange"
            }
          }
        }
      },
      "CatalogDelta": {
        "type": "object",
        "description": "What a client gets back from a poll.",
        "required": [
          "revision"
        ],
        "properties": {
          "changes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CatalogChanged"
            }
          },
          "revision": {
            "type": "integer",
            "format": "int64",
            "description": "The current revision. A client stores this and passes it as `since`.",
            "minimum": 0
          },
          "truncated": {
            "type": "boolean",
            "description": "The client fell further behind than the buffer holds, so `changes` is\nnot a complete history. **Refetch the inventory** rather than applying\nit."
          }
        }
      },
      "CatalogEntryChange": {
        "type": "string",
        "enum": [
          "added",
          "removed",
          "updated",
          "enabled",
          "disabled"
        ]
      },
      "CatalogExtensionChange": {
        "type": "object",
        "required": [
          "key",
          "name",
          "change",
          "enabled"
        ],
        "properties": {
          "bundledSkillIds": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Skills that shipped inside this extension's bundle."
          },
          "change": {
            "$ref": "#/components/schemas/CatalogEntryChange"
          },
          "config": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ExtensionConfig"
              }
            ],
            "nullable": true
          },
          "displayName": {
            "type": "string",
            "nullable": true
          },
          "enabled": {
            "type": "boolean"
          },
          "key": {
            "type": "string",
            "description": "`name_to_key(name)` — the join every surface already uses, and the only\nidentifier that survives a display-name change."
          },
          "name": {
            "type": "string"
          }
        }
      },
      "CatalogSkill": {
        "type": "object",
        "description": "One skill, as the interface and the model both see it.",
        "required": [
          "name",
          "description",
          "slug",
          "directory",
          "sourceRoot",
          "source",
          "builtin",
          "state"
        ],
        "properties": {
          "builtin": {
            "type": "boolean",
            "description": "Shipped with Biorouter, so the interface offers no Delete for it."
          },
          "bundle": {
            "type": "string",
            "description": "The bundle directory this skill sits in, when it is a bundle member.",
            "nullable": true
          },
          "description": {
            "type": "string"
          },
          "directory": {
            "type": "string"
          },
          "name": {
            "type": "string",
            "description": "Frontmatter `name` — the identifier every enablement surface keys on."
          },
          "slug": {
            "type": "string",
            "description": "Root-relative logical path, `/`-separated on every platform\n(`superpowers/brainstorming`). What `biorouter skill list` prints."
          },
          "source": {
            "$ref": "#/components/schemas/SkillSource"
          },
          "sourceRoot": {
            "type": "string"
          },
          "state": {
            "$ref": "#/components/schemas/SkillState"
          }
        }
      },
      "CatalogSkillChange": {
        "type": "object",
        "required": [
          "id",
          "change"
        ],
        "properties": {
          "change": {
            "$ref": "#/components/schemas/CatalogEntryChange"
          },
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string",
            "nullable": true
          },
          "sourceExtensionKey": {
            "type": "string",
            "description": "The extension whose bundle carried it, when it came from one.",
            "nullable": true
          }
        }
      },
      "CatalogView": {
        "type": "object",
        "description": "The serialisable catalog: what `GET /skills/catalog` returns and what the\ndesktop picker renders. There is no second derivation of any of these\nfields on the interface side — that separation is what #113 removed.",
        "required": [
          "generation",
          "roots",
          "skills",
          "bundles"
        ],
        "properties": {
          "bundles": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CatalogBundle"
            }
          },
          "generation": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "roots": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SkillRoot"
            }
          },
          "skills": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CatalogSkill"
            }
          }
        }
      },
      "ChangeKind": {
        "type": "string",
        "enum": [
          "ingest",
          "link",
          "flag",
          "query",
          "lint",
          "restore",
          "manual"
        ]
      },
      "ChatRequest": {
        "type": "object",
        "required": [
          "user_message",
          "session_id"
        ],
        "properties": {
          "continuation_lease": {
            "type": "string",
            "description": "Opaque admission minted by an exact-generation Stop-and-Send. A live\nlease is required while that superseded child generation remains under\nparent supervision, and is consumed atomically with this successor's\nturn lock.",
            "nullable": true
          },
          "conversation_so_far": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Message"
            },
            "description": "The client's own copy of the session's history, which REPLACES what the\nserver has stored.\n\nDEPRECATED as an authoritative overwrite (#51 W5). It is now conditional:\nthe copy must already contain every message the server holds, and the\nrequest is refused with 409 if it does not. See\n[`apply_client_writeback`]. The desktop app has never sent this field.",
            "nullable": true
          },
          "from_seq": {
            "type": "integer",
            "format": "int64",
            "description": "Attach only from this per-turn sequence number, when `turn_id` names a\nturn already in flight.\n\nA pure OPTIMISATION and deliberately so: a client that already rendered\nframes `0..N` skips re-receiving them, but one that omits the field gets\nthe whole turn replayed and its own sequence gate makes that idempotent.\nNothing about correctness depends on the server honouring it.\n\nIt lives in the BODY rather than in `?from_seq=`, because `/reply` is\ngenerated with `query?: never` in `api/types.gen.ts` — a query parameter\ncould only be smuggled past the typed client, while a `ChatRequest` field\nappears properly the next time the OpenAPI spec is regenerated.",
            "nullable": true,
            "minimum": 0
          },
          "reasoning_effort": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ReasoningEffort"
              }
            ],
            "nullable": true
          },
          "session_id": {
            "type": "string"
          },
          "turn_id": {
            "type": "string",
            "description": "Client-generated idempotency key naming *this* turn (BR-62). Optional, but\na client that retries `/reply` — an SSE reconnect, a fetch retry on a\nflaky network — should send the same key it sent the first time. The retry\nthen comes back as a 409 with `duplicate: true`, meaning \"that turn is\nstill running\", instead of being mistaken for a genuine second turn.\n\nSince the live-turn-stream work this is also the ATTACH pointer: posting\na `turn_id` that names a turn already in flight is answered 200 with that\nturn's stream instead of 409. Either name works — the key the client\nchose, or the server-assigned `turn-N` it read off a frame's `turn_id` /\n`POST /agent/resume`'s `active_turn`.",
            "nullable": true
          },
          "user_message": {
            "$ref": "#/components/schemas/Message"
          },
          "workflow_name": {
            "type": "string",
            "nullable": true
          },
          "workflow_version": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "CheckModelBody": {
        "type": "object",
        "required": [
          "model"
        ],
        "properties": {
          "model": {
            "$ref": "#/components/schemas/ModelRef"
          }
        }
      },
      "CheckModelResponse": {
        "type": "object",
        "required": [
          "ok"
        ],
        "properties": {
          "error": {
            "type": "string",
            "nullable": true
          },
          "ok": {
            "type": "boolean"
          }
        }
      },
      "CheckProviderRequest": {
        "type": "object",
        "required": [
          "provider"
        ],
        "properties": {
          "provider": {
            "type": "string"
          }
        }
      },
      "CodingAgentKind": {
        "type": "string",
        "description": "Which vendor CLI. Kept as an enum rather than a string so the match arms that\ndiffer (and there are several — the auth probe especially) cannot silently\nfall through for a new variant.",
        "enum": [
          "claude_code",
          "codex"
        ]
      },
      "CodingAgentStatusResponse": {
        "type": "object",
        "required": [
          "agents"
        ],
        "properties": {
          "agents": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AgentAvailability"
            }
          }
        }
      },
      "CommandType": {
        "type": "string",
        "enum": [
          "Builtin",
          "Workflow"
        ]
      },
      "CommitResponse": {
        "type": "object",
        "required": [
          "commit_sha"
        ],
        "properties": {
          "commit_sha": {
            "type": "string"
          }
        }
      },
      "ConfigKey": {
        "type": "object",
        "description": "Configuration key metadata for provider setup",
        "required": [
          "name",
          "required",
          "secret",
          "oauth_flow"
        ],
        "properties": {
          "default": {
            "type": "string",
            "description": "Optional default value for the key",
            "nullable": true
          },
          "name": {
            "type": "string",
            "description": "The name of the configuration key (e.g., \"API_KEY\")"
          },
          "oauth_flow": {
            "type": "boolean",
            "description": "Whether this key should be configured using OAuth device code flow\nWhen true, the provider's configure_oauth() method will be called instead of prompting for manual input"
          },
          "required": {
            "type": "boolean",
            "description": "Whether this key is required for the provider to function"
          },
          "secret": {
            "type": "boolean",
            "description": "Whether this key should be stored securely (e.g., in keychain)"
          }
        }
      },
      "ConfigKeyQuery": {
        "type": "object",
        "required": [
          "key",
          "is_secret"
        ],
        "properties": {
          "is_secret": {
            "type": "boolean"
          },
          "key": {
            "type": "string"
          }
        }
      },
      "ConfigResponse": {
        "type": "object",
        "required": [
          "config"
        ],
        "properties": {
          "config": {
            "type": "object",
            "additionalProperties": {}
          }
        }
      },
      "ConfirmToolActionRequest": {
        "type": "object",
        "required": [
          "id",
          "action",
          "sessionId"
        ],
        "properties": {
          "action": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "principalType": {
            "$ref": "#/components/schemas/PrincipalType"
          },
          "sessionId": {
            "type": "string"
          }
        }
      },
      "Content": {
        "oneOf": [
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/RawTextContent"
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/RawImageContent"
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/RawEmbeddedResource"
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/RawAudioContent"
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/RawResource"
              }
            ]
          }
        ]
      },
      "ContinuationLeaseErrorResponse": {
        "type": "object",
        "required": [
          "error"
        ],
        "properties": {
          "error": {
            "type": "string"
          }
        }
      },
      "Conversation": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/Message"
        }
      },
      "CreateBaseBody": {
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "properties": {
          "color": {
            "type": "string",
            "nullable": true
          },
          "format": {
            "allOf": [
              {
                "$ref": "#/components/schemas/KbFormat"
              }
            ],
            "nullable": true
          },
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          }
        }
      },
      "CreateScheduleRequest": {
        "type": "object",
        "required": [
          "id",
          "workflow_source",
          "cron"
        ],
        "properties": {
          "cron": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "workflow_source": {
            "type": "string"
          }
        }
      },
      "CreateWorkflowRequest": {
        "type": "object",
        "required": [
          "session_id"
        ],
        "properties": {
          "author": {
            "allOf": [
              {
                "$ref": "#/components/schemas/AuthorRequest"
              }
            ],
            "nullable": true
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "CreateWorkflowResponse": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string",
            "nullable": true
          },
          "workflow": {
            "allOf": [
              {
                "$ref": "#/components/schemas/Workflow"
              }
            ],
            "nullable": true
          }
        }
      },
      "Credibility": {
        "type": "object",
        "required": [
          "tier",
          "confidence",
          "reasoning",
          "classifier_version"
        ],
        "properties": {
          "classifier_version": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "confidence": {
            "type": "number",
            "format": "float"
          },
          "doi": {
            "type": "string",
            "nullable": true
          },
          "publisher": {
            "type": "string",
            "nullable": true
          },
          "reasoning": {
            "type": "string"
          },
          "retracted": {
            "type": "boolean"
          },
          "tier": {
            "$ref": "#/components/schemas/CredibilityTier"
          },
          "venue": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "CredibilityResponse": {
        "type": "object",
        "required": [
          "credibility"
        ],
        "properties": {
          "credibility": {
            "$ref": "#/components/schemas/Credibility"
          }
        }
      },
      "CredibilityTier": {
        "type": "string",
        "enum": [
          "peer_reviewed",
          "preprint",
          "book",
          "gray_lit",
          "web",
          "personal"
        ]
      },
      "CrossAffiliationGrantRequest": {
        "type": "object",
        "description": "Issue #56 Task 49 (DR-26): the user accepting ONE cross-institutional data\nflow.\n\n⚠ **There is no `affiliation` field, and there must not be one.** The grant is\nkeyed on the affiliation of the model bound *right now*, read by the daemon\nfrom the same sample that produced the warning. A client-supplied institution\nwould let a caller record an acceptance for a triple the user was never shown\n— which is the one thing this control exists to prevent.",
        "required": [
          "session_id",
          "extension"
        ],
        "properties": {
          "extension": {
            "type": "string",
            "description": "The extension whose cross-institutional flow the user accepted. Any\nspelling the UI holds; it is `name_to_key`-normalised on both sides."
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "CrossAffiliationGrantResponse": {
        "type": "object",
        "description": "What was accepted, echoed back so the caller can record the exact sentence the\nuser was shown rather than a paraphrase of it.",
        "required": [
          "accepted"
        ],
        "properties": {
          "accepted": {
            "type": "string",
            "description": "The full statement, including the scope of the approval\n(`privacy::grant::GRANT_SCOPE_COPY`)."
          }
        }
      },
      "DailyActivity": {
        "type": "object",
        "description": "One calendar day of usage, for the Home heatmap.",
        "required": [
          "date",
          "sessions",
          "tokens",
          "tokensComplete",
          "inputTokens",
          "outputTokens",
          "messages",
          "level"
        ],
        "properties": {
          "date": {
            "type": "string",
            "description": "Local calendar day, `YYYY-MM-DD`."
          },
          "inputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "level": {
            "type": "integer",
            "format": "int32",
            "description": "1–4. Level 0 days are omitted from the response entirely.",
            "minimum": 0
          },
          "messages": {
            "type": "integer",
            "format": "int64",
            "description": "Assistant + user messages exchanged that day."
          },
          "outputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "sessions": {
            "type": "integer",
            "format": "int64",
            "description": "Sessions *started* that day (exact; keyed on the immutable `created_at`)."
          },
          "tokens": {
            "type": "integer",
            "format": "int64",
            "description": "Tokens processed that day, summed from per-turn `token_events`."
          },
          "tokensComplete": {
            "type": "boolean",
            "description": "False when at least one token event that day lacks billed-token\naccounting. `tokens` is then a known subtotal; zero is unavailable, not\na measured zero."
          }
        }
      },
      "DeclarativeProviderConfig": {
        "type": "object",
        "required": [
          "name",
          "engine",
          "display_name",
          "api_key_env",
          "base_url",
          "models"
        ],
        "properties": {
          "api_key_env": {
            "type": "string"
          },
          "base_url": {
            "type": "string"
          },
          "description": {
            "type": "string",
            "nullable": true
          },
          "display_name": {
            "type": "string"
          },
          "engine": {
            "$ref": "#/components/schemas/ProviderEngine"
          },
          "headers": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "nullable": true
          },
          "models": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ModelInfo"
            }
          },
          "name": {
            "type": "string"
          },
          "supports_streaming": {
            "type": "boolean",
            "nullable": true
          },
          "timeout_seconds": {
            "type": "integer",
            "format": "int64",
            "nullable": true,
            "minimum": 0
          }
        }
      },
      "DeclassifySessionRequest": {
        "type": "object",
        "properties": {
          "confirmation": {
            "type": "string",
            "description": "The last six characters of the session id, as the user typed them.\n\nRequired when the chat's provenance is anything other than `turn:*` —\nsee `biorouter::privacy::declassify::requires_typed_confirmation`, which\nis where the grading lives. The daemon re-derives the grade from the\nSTORED provenance rather than trusting the client to say which control it\nshowed, so a caller cannot claim the single-click path for a chat that\nreached a private data source.",
            "nullable": true
          }
        }
      },
      "DeclassifySessionResponse": {
        "type": "object",
        "required": [
          "sessionId",
          "privacyTier"
        ],
        "properties": {
          "privacyTier": {
            "$ref": "#/components/schemas/SessionClassification"
          },
          "sessionId": {
            "type": "string"
          }
        }
      },
      "DecodeWorkflowRequest": {
        "type": "object",
        "required": [
          "deeplink"
        ],
        "properties": {
          "deeplink": {
            "type": "string"
          }
        }
      },
      "DecodeWorkflowResponse": {
        "type": "object",
        "required": [
          "workflow"
        ],
        "properties": {
          "workflow": {
            "$ref": "#/components/schemas/Workflow"
          }
        }
      },
      "DeleteWorkflowRequest": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      },
      "DetectProviderRequest": {
        "type": "object",
        "required": [
          "api_key"
        ],
        "properties": {
          "api_key": {
            "type": "string"
          }
        }
      },
      "DetectProviderResponse": {
        "type": "object",
        "properties": {
          "api_key_config_key": {
            "type": "string",
            "description": "Exact secret config key used by the detected provider.",
            "nullable": true
          },
          "default_model": {
            "type": "string",
            "description": "A recommended default chat model, when one could be determined.",
            "nullable": true
          },
          "extra_config": {
            "type": "object",
            "description": "Non-secret config to persist alongside the key (e.g. a regional host).",
            "additionalProperties": {
              "type": "string"
            }
          },
          "models": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "All model ids the provider reported for the key (empty on failure)."
          },
          "provider_name": {
            "type": "string",
            "description": "The detected provider, or `null` when detection failed.",
            "nullable": true
          },
          "reason": {
            "type": "string",
            "description": "Machine-readable failure reason when `provider_name` is null:\n`\"timeout\" | \"network\" | \"invalid_key\" | \"no_match\"`.",
            "nullable": true
          }
        }
      },
      "DetectableProvider": {
        "type": "object",
        "required": [
          "name",
          "display_name"
        ],
        "properties": {
          "display_name": {
            "type": "string"
          },
          "name": {
            "type": "string"
          }
        }
      },
      "DetectableProvidersResponse": {
        "type": "object",
        "required": [
          "providers"
        ],
        "properties": {
          "providers": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DetectableProvider"
            }
          }
        }
      },
      "Diagnostic": {
        "type": "object",
        "description": "One finding, addressed to whoever has to fix it.",
        "required": [
          "rule",
          "severity",
          "subject",
          "message"
        ],
        "properties": {
          "message": {
            "type": "string"
          },
          "path": {
            "type": "string",
            "description": "Bundle-relative path of the page, when there is one. Absent for a\nbase-wide finding and for a draft that has not been given a path yet.",
            "nullable": true
          },
          "rule": {
            "type": "string",
            "description": "Stable across releases. Callers, tests and the UI match on this and\nnever on `message`, which is prose and will be reworded. Prefixed by the\nlayer that raised it: `okf.`, `biookf.`, or `kb.` for the deterministic\nhygiene scan that predates both."
          },
          "severity": {
            "$ref": "#/components/schemas/Severity"
          },
          "subject": {
            "type": "string",
            "description": "What the finding is *about*: a page's `identifier`, a bundle-relative\npath, or an edge rendered as `<subject> -<predicate>-> <object>`.\nNever empty — a diagnostic whose subject a caller has to reconstruct\nfrom the message is one nobody acts on."
          }
        }
      },
      "Diagnostics": {
        "type": "object",
        "description": "A capped list plus the count before the cap.\n\nThe pair, not the `Vec` alone: a truncated list that reports its own length\nas the answer is how \"3 errors\" gets rendered for a base with four hundred.",
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Diagnostic"
            },
            "description": "At most [`MAX_DIAGNOSTICS`], most severe first."
          },
          "total": {
            "type": "integer",
            "description": "How many were raised, which is `items.len()` unless the cap bit.",
            "minimum": 0
          }
        }
      },
      "DivergeSessionRequest": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Optional name for the diverged session. When omitted or blank, the\ndiverged session inherits the original session's name so it stays\nrecognizable in the session list.",
            "nullable": true
          },
          "truncateAfter": {
            "type": "integer",
            "format": "int64",
            "description": "Optional anchor: the `created` timestamp (ms) of the assistant message a\nper-message Diverge button was clicked on. The branch is trimmed to end\nat that answer. When omitted, the branch ends at the most recent\ncomplete assistant answer — an in-flight turn captured mid-generation is\nnever carried over.",
            "nullable": true
          },
          "truncateAfterId": {
            "type": "string",
            "description": "Optional anchor by durable message id (`Message.id`), the BR-45 divergence\npoint. Preferred over `truncate_after`: it is unambiguous when two\nmessages share a whole second and it records the branch's divergence point. It\ntakes precedence when both are supplied; `truncate_after` stays for\nback-compatibility with older clients.",
            "nullable": true
          }
        }
      },
      "DivergeSessionResponse": {
        "type": "object",
        "required": [
          "sessionId",
          "workingDir",
          "name"
        ],
        "properties": {
          "divergedFrom": {
            "type": "string",
            "description": "The id of the session this one was diverged from (its lineage parent).",
            "nullable": true
          },
          "name": {
            "type": "string",
            "description": "The diverged session's resolved name (e.g. \"Foo (branch 2)\")."
          },
          "sessionId": {
            "type": "string",
            "description": "The id of the newly-created diverged session."
          },
          "workingDir": {
            "type": "string",
            "description": "The working directory of the diverged session (identical to the\noriginal). Surfaced so the client can spawn the new window/backend in\nthe correct directory."
          }
        }
      },
      "EditMessageRequest": {
        "type": "object",
        "required": [
          "timestamp"
        ],
        "properties": {
          "editType": {
            "$ref": "#/components/schemas/EditType"
          },
          "expectedMessageIds": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Your view of this session: the durable ids (`Message.id`) of every\nmessage it holds, in any order.\n\nOPTIONAL, and enforced when you send it. For `edit`, which truncates the\nlive session, supplying it is how the server checks that you are deleting\nthe history you actually saw: a stored message your list does not name\nlanded after you rendered the conversation, and the cut is refused with\n409 `conversation_out_of_date` rather than destroying it. Omit it and the\ncut still runs under the turn lock and is still bounded to the rows the\nserver itself just read — safe, but blind to that wider race. Ignored for\n`diverge`, which writes only to a new session and cannot destroy anything.\n\nAn EMPTY list is not the same as omitting the field: it asserts your view\nholds nothing, and is checked as such (so a non-empty session refuses it).",
            "nullable": true
          },
          "timestamp": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "EditMessageResponse": {
        "type": "object",
        "required": [
          "sessionId"
        ],
        "properties": {
          "sessionId": {
            "type": "string"
          }
        }
      },
      "EditType": {
        "type": "string",
        "enum": [
          "diverge",
          "edit"
        ]
      },
      "EmbeddedResource": {
        "type": "object",
        "required": [
          "resource"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": true
          },
          "annotations": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Annotations"
              },
              {
                "type": "object"
              }
            ]
          },
          "resource": {
            "$ref": "#/components/schemas/ResourceContents"
          }
        }
      },
      "EncodeWorkflowRequest": {
        "type": "object",
        "required": [
          "workflow"
        ],
        "properties": {
          "workflow": {
            "$ref": "#/components/schemas/Workflow"
          }
        }
      },
      "EncodeWorkflowResponse": {
        "type": "object",
        "required": [
          "deeplink"
        ],
        "properties": {
          "deeplink": {
            "type": "string"
          }
        }
      },
      "Envs": {
        "type": "object",
        "additionalProperties": {
          "type": "string",
          "description": "A map of environment variables to set, e.g. API_KEY -> some_secret, HOST -> host"
        }
      },
      "ErrorResponse": {
        "type": "object",
        "required": [
          "message"
        ],
        "properties": {
          "message": {
            "type": "string"
          }
        }
      },
      "Evidence": {
        "type": "string",
        "description": "Which rung of the detection ladder decided the shape. Reported so a preview\ncan explain itself, and so a test can assert that the *manifest* decided\nrather than the structure happening to agree with it.",
        "enum": [
          "biorouterManifest",
          "codexPlugin",
          "claudePlugin",
          "skillsManifest",
          "skillsDirectory",
          "structuralInference",
          "singleSkill"
        ]
      },
      "ExtensionConfig": {
        "oneOf": [
          {
            "type": "object",
            "description": "SSE transport is no longer supported - kept only for config file compatibility",
            "required": [
              "name",
              "description",
              "type"
            ],
            "properties": {
              "description": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "type": {
                "type": "string",
                "enum": [
                  "sse"
                ]
              },
              "uri": {
                "type": "string",
                "nullable": true
              }
            }
          },
          {
            "type": "object",
            "description": "Standard I/O client with command and arguments",
            "required": [
              "name",
              "description",
              "cmd",
              "args",
              "type"
            ],
            "properties": {
              "args": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "available_tools": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "bundled": {
                "type": "boolean",
                "nullable": true
              },
              "cmd": {
                "type": "string"
              },
              "description": {
                "type": "string"
              },
              "env_keys": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "envs": {
                "$ref": "#/components/schemas/Envs"
              },
              "name": {
                "type": "string",
                "description": "The name used to identify this extension"
              },
              "timeout": {
                "type": "integer",
                "format": "int64",
                "nullable": true,
                "minimum": 0
              },
              "type": {
                "type": "string",
                "enum": [
                  "stdio"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Built-in extension that is part of the bundled biorouter MCP server",
            "required": [
              "name",
              "description",
              "type"
            ],
            "properties": {
              "available_tools": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "bundled": {
                "type": "boolean",
                "nullable": true
              },
              "description": {
                "type": "string"
              },
              "display_name": {
                "type": "string",
                "nullable": true
              },
              "name": {
                "type": "string",
                "description": "The name used to identify this extension"
              },
              "timeout": {
                "type": "integer",
                "format": "int64",
                "nullable": true,
                "minimum": 0
              },
              "type": {
                "type": "string",
                "enum": [
                  "builtin"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Platform extensions that have direct access to the agent etc and run in the agent process",
            "required": [
              "name",
              "description",
              "type"
            ],
            "properties": {
              "available_tools": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "bundled": {
                "type": "boolean",
                "nullable": true
              },
              "description": {
                "type": "string"
              },
              "name": {
                "type": "string",
                "description": "The name used to identify this extension"
              },
              "type": {
                "type": "string",
                "enum": [
                  "platform"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Streamable HTTP client with a URI endpoint using MCP Streamable HTTP specification",
            "required": [
              "name",
              "description",
              "uri",
              "type"
            ],
            "properties": {
              "available_tools": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "bundled": {
                "type": "boolean",
                "nullable": true
              },
              "description": {
                "type": "string"
              },
              "env_keys": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "envs": {
                "$ref": "#/components/schemas/Envs"
              },
              "headers": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                }
              },
              "name": {
                "type": "string",
                "description": "The name used to identify this extension"
              },
              "timeout": {
                "type": "integer",
                "format": "int64",
                "nullable": true,
                "minimum": 0
              },
              "type": {
                "type": "string",
                "enum": [
                  "streamable_http"
                ]
              },
              "uri": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "description": "Frontend-provided tools that will be called through the frontend",
            "required": [
              "name",
              "description",
              "tools",
              "type"
            ],
            "properties": {
              "available_tools": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "bundled": {
                "type": "boolean",
                "nullable": true
              },
              "description": {
                "type": "string"
              },
              "instructions": {
                "type": "string",
                "description": "Instructions for how to use these tools",
                "nullable": true
              },
              "name": {
                "type": "string",
                "description": "The name used to identify this extension"
              },
              "tools": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/Tool"
                },
                "description": "The tools provided by the frontend"
              },
              "type": {
                "type": "string",
                "enum": [
                  "frontend"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Inline Python code that will be executed using uvx",
            "required": [
              "name",
              "description",
              "code",
              "type"
            ],
            "properties": {
              "available_tools": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "code": {
                "type": "string",
                "description": "The Python code to execute"
              },
              "dependencies": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Python package dependencies required by this extension",
                "nullable": true
              },
              "description": {
                "type": "string"
              },
              "name": {
                "type": "string",
                "description": "The name used to identify this extension"
              },
              "timeout": {
                "type": "integer",
                "format": "int64",
                "description": "Timeout in seconds",
                "nullable": true,
                "minimum": 0
              },
              "type": {
                "type": "string",
                "enum": [
                  "inline_python"
                ]
              }
            }
          }
        ],
        "description": "Represents the different types of MCP extensions that can be added to the manager",
        "discriminator": {
          "propertyName": "type"
        }
      },
      "ExtensionData": {
        "type": "object",
        "description": "Extension data containing all extension states\nKeys are in format \"extension_name.version\" (e.g., \"todo.v0\")",
        "additionalProperties": {}
      },
      "ExtensionEntry": {
        "allOf": [
          {
            "$ref": "#/components/schemas/ExtensionConfig"
          },
          {
            "type": "object",
            "required": [
              "enabled"
            ],
            "properties": {
              "enabled": {
                "type": "boolean"
              }
            }
          }
        ],
        "description": "⚠ `PartialEq` is not derive-everything hygiene: `remove_extension_if_matches`\nalready compares the two fields by hand, and `remove_extension`'s\npost-approval re-validation compares whole entries — an approval is only\natomic against a tree that moved under it if \"the same entry\" has one\ndefinition."
      },
      "ExtensionLoadResult": {
        "type": "object",
        "required": [
          "name",
          "success"
        ],
        "properties": {
          "error": {
            "type": "string",
            "nullable": true
          },
          "name": {
            "type": "string"
          },
          "success": {
            "type": "boolean"
          }
        }
      },
      "ExtensionQuery": {
        "type": "object",
        "required": [
          "name",
          "config",
          "enabled"
        ],
        "properties": {
          "config": {
            "$ref": "#/components/schemas/ExtensionConfig"
          },
          "enabled": {
            "type": "boolean"
          },
          "name": {
            "type": "string"
          }
        }
      },
      "ExtensionResponse": {
        "type": "object",
        "required": [
          "extensions"
        ],
        "properties": {
          "extensions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExtensionEntry"
            }
          },
          "warnings": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "FrontendToolRequest": {
        "type": "object",
        "required": [
          "id",
          "toolCall"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "toolCall": {
            "type": "object"
          }
        }
      },
      "GetToolsQuery": {
        "type": "object",
        "required": [
          "session_id"
        ],
        "properties": {
          "extension_name": {
            "type": "string",
            "nullable": true
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "Graph": {
        "type": "object",
        "required": [
          "nodes",
          "edges"
        ],
        "properties": {
          "edges": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GraphEdge"
            }
          },
          "nodes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GraphNode"
            }
          }
        }
      },
      "GraphEdge": {
        "type": "object",
        "required": [
          "from",
          "to"
        ],
        "properties": {
          "agent_type": {
            "type": "string",
            "nullable": true
          },
          "from": {
            "type": "string"
          },
          "knowledge_level": {
            "type": "string",
            "description": "The BioOKF §8.1 provenance triplet.",
            "nullable": true
          },
          "negated": {
            "type": "boolean",
            "description": "SPEC §6.F polarity, emitted rather than left for the renderer to infer\nfrom a `not_` prefix — the prefix is one of two spellings (the other is\nthe legacy `negated: true` attribute) and a renderer that knows only the\nfirst draws a negative claim as a positive one."
          },
          "predicate": {
            "type": "string",
            "description": "The BioOKF §6 predicate. `None` for an untyped link, which is what makes\n\"this edge has no type\" answerable rather than inferred from an empty\nstring.",
            "nullable": true
          },
          "primary_source": {
            "type": "string",
            "description": "The source node this claim came from: a node id when the identifier\nresolves to a page in this bundle, and otherwise the identifier exactly\nas written, so an unresolved one is visible instead of vanishing.",
            "nullable": true
          },
          "publications": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "qualifiers": {
            "type": "object",
            "description": "BioOKF §7.2 context, plus every attribute this build does not recognise —\n`direction`, `aspect`, `species_context`, `sex`, `timepoint`, and\nanything BioOKF adds after this build shipped — as text, in key order.\n\nIt is the residue map as well as the context map, and that asymmetry is\ndeliberate: an attribute nobody has classified is *context* until someone\nsays otherwise. The alternative loses data outright — the attribute\nexists on disk, and a deriver that models only what it recognises drops\nthe rest on the floor where nothing can report it.",
            "additionalProperties": {
              "type": "string"
            }
          },
          "quantitative": {
            "type": "object",
            "description": "BioOKF §7.3: the quantitative bundle, keyed by the slot the producer\nwrote. `effect_size`, `p_value`, `ci_lower`, `ci_upper`, `sample_size`,\n`effect_metric`, `adjusted_p_value`, `standard_error`, `sensitivity`,\n`specificity`, `auc`, `frequency`, `clinical_phase`, `response_direction`,\n`unit`, … — see `graph::QUANTITATIVE_KEYS` for the set this build routes\nhere and for why an unrecognised key does not land here by default.",
            "additionalProperties": {
              "$ref": "#/components/schemas/QuantitativeValue"
            }
          },
          "relation": {
            "type": "string",
            "description": "The deprecated alias of [`Self::predicate`], carrying the identical\nvalue. Kept for one release because it is the only relation field the\ngenerated TypeScript client has ever had; new readers use `predicate`.",
            "nullable": true
          },
          "synthesized": {
            "type": "boolean",
            "description": "This edge was derived from provenance rather than authored as a\nrelationship (UI spec §2.1, adopted by DR-27).\n\n§5.7 draws it as the faint dotted line with **no taper** and §4.8 replaces\nits provenance triplet with the \"author an explicit `reported_in` edge to\nmake it first-class\" note, so it is a rendering channel a consumer cannot\nreconstruct: an implicit provenance link and an authored `reported_in`\nedge are the same `(from, to, predicate)` triple and differ only in who\nwrote them.\n\n⚠ **Nothing in this build sets it.** The deriver emits only edges a page\nstates, and DR-24 puts the `reported_in` emission in BioOKF-mode *ingest*\n— so today this is always `false` and therefore never serialized. It is\ndeclared now because Stage 6 freezes the generated TypeScript client and\na channel absent from that contract cannot be added by the producer\nalone."
          },
          "to": {
            "type": "string"
          }
        }
      },
      "GraphNode": {
        "type": "object",
        "required": [
          "id",
          "label",
          "kind",
          "path"
        ],
        "properties": {
          "credibility_tier": {
            "allOf": [
              {
                "$ref": "#/components/schemas/CredibilityTier"
              }
            ],
            "nullable": true
          },
          "degree": {
            "type": "integer",
            "format": "int32",
            "description": "Incident edge count — every edge with this node at either end, after\n[`crate::knowledge::graph`] has deduplicated them.\n\nUI spec §2.1, adopted by DR-27. It drives hub sizing (§5.6 floors its\n`deg(n)` at this value) and the descending-degree keyboard order (§5.12),\nand the renderer cannot derive it cheaply: it would have to walk the\nwhole edge list on every frame of a force layout, for a number that is\nconstant for the life of the graph.\n\n`Option` because the spec draws it optional and a consumer must be able\nto tell \"the producer did not supply this\" from \"this node has no edges\"\n— the deriver always fills it, and fills it with `Some(0)` for an\nisolated node.",
            "nullable": true,
            "minimum": 0
          },
          "external": {
            "type": "boolean",
            "description": "A node an edge points at that has no page in this bundle yet.\n\nOKF §11 makes a broken link something a consumer MUST tolerate, so this\nis not an error state — it is the curation queue, surfaced. See\n`graph::derive` for why a dangling *legacy* `[[…]]` link does not produce\none of these."
          },
          "id": {
            "type": "string"
          },
          "identifier": {
            "type": "string",
            "description": "The display identity — `identifier`, or `title` as SPEC §14's deprecated\nalias for it. Distinct from `label`, which stays whatever the page list\nhas always shown, so a renderer can change what it shows without the\nderiver changing what an edge resolves against.",
            "nullable": true
          },
          "kind": {
            "$ref": "#/components/schemas/PageKind"
          },
          "label": {
            "type": "string"
          },
          "node_type": {
            "type": "string",
            "description": "The concept document's own `type` (OKF §4.1), exactly as written.\n\nA raw `String` and not an enum, in both profiles. OKF leaves `type` open,\nand DR-7 forbids rejecting a page over an unrecognised value — an enum\nhere would be a rejection mechanism wearing a different hat, silently\nreplacing the producer's word with a fallback on the way past. `None` for\nevery page in a legacy base, which carries `kind` and no `type`.",
            "nullable": true
          },
          "path": {
            "type": "string",
            "description": "The page's logical path, or the empty string for an `external` node,\nwhich has no page to open."
          },
          "retracted": {
            "type": "boolean",
            "description": "True if this is a source node whose `raw/<id>/meta.yaml` marks it retracted."
          },
          "stale": {
            "type": "boolean",
            "description": "OKF §5.5: `stale_after` has passed. Computed here, at derivation time,\nrather than in the renderer — a renderer that compares dates would give a\ndifferent answer per client clock and per cache age."
          },
          "status": {
            "type": "string",
            "description": "OKF §5.4 `draft | stable | deprecated`, or whatever else the producer\nwrote. Emitted only when the page states one: §5.4 says an absent\n`status` *reads* as `stable`, and writing `stable` onto every legacy node\nwould turn a consumer's assumption into the producer's assertion.",
            "nullable": true
          },
          "subtype": {
            "type": "string",
            "description": "BioOKF §7.1 `subtype`: agent-coined, never validated against anything.",
            "nullable": true
          }
        }
      },
      "HistoryEntry": {
        "type": "object",
        "required": [
          "commit_sha",
          "kind",
          "summary",
          "timestamp"
        ],
        "properties": {
          "commit_sha": {
            "type": "string"
          },
          "delta": {
            "type": "string",
            "nullable": true
          },
          "kind": {
            "$ref": "#/components/schemas/ChangeKind"
          },
          "summary": {
            "type": "string"
          },
          "timestamp": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "HistoryQuery": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "Icon": {
        "type": "object",
        "required": [
          "src"
        ],
        "properties": {
          "mimeType": {
            "type": "string"
          },
          "sizes": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "src": {
            "type": "string"
          }
        }
      },
      "ImageContent": {
        "type": "object",
        "required": [
          "data",
          "mimeType"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": true
          },
          "annotations": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Annotations"
              },
              {
                "type": "object"
              }
            ]
          },
          "data": {
            "type": "string"
          },
          "mimeType": {
            "type": "string"
          }
        }
      },
      "ImportChoice": {
        "type": "string",
        "enum": [
          "bundle",
          "individual"
        ]
      },
      "ImportKind": {
        "type": "string",
        "description": "Which shape the importer decided on.",
        "enum": [
          "single",
          "bundle"
        ]
      },
      "ImportPreview": {
        "type": "object",
        "description": "The serialisable half of a plan — everything except the file bytes.",
        "required": [
          "kind",
          "id",
          "displayName",
          "groups",
          "components",
          "evidence",
          "source",
          "shadows",
          "fileCount"
        ],
        "properties": {
          "ambiguity": {
            "allOf": [
              {
                "$ref": "#/components/schemas/Ambiguity"
              }
            ],
            "nullable": true
          },
          "components": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlannedSkill"
            }
          },
          "displayName": {
            "type": "string"
          },
          "entryPoint": {
            "type": "string",
            "nullable": true
          },
          "evidence": {
            "$ref": "#/components/schemas/Evidence"
          },
          "fileCount": {
            "type": "integer",
            "description": "How many files would be written.",
            "minimum": 0
          },
          "groups": {
            "type": "object"
          },
          "id": {
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/ImportKind"
          },
          "shadows": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "source": {
            "$ref": "#/components/schemas/SourceProvenance"
          },
          "version": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "ImportRequest": {
        "type": "object",
        "description": "What to import, in the two forms a caller has.\n\nOne request type for a pasted repository URL, an agent's tool call, a local\n`.zip` and a marketplace asset, because giving each of those its own\nresolution is how the four came to disagree in the first place.",
        "properties": {
          "choice": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ImportChoice"
              }
            ],
            "nullable": true
          },
          "components": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Which components to keep when `choice` is `individual`."
          },
          "filePath": {
            "type": "string",
            "description": "A `.zip` on the machine the daemon runs on.",
            "nullable": true
          },
          "planId": {
            "type": "string",
            "description": "Answer to a previous preview's question, by its `planId`.",
            "nullable": true
          },
          "reference": {
            "type": "string",
            "description": "Branch, tag or commit. Overrides a ref in the URL.",
            "nullable": true
          },
          "url": {
            "type": "string",
            "description": "`https://github.com/owner/repo`, a `/tree/<ref>` URL, or a direct\narchive URL on an allowed host.",
            "nullable": true
          }
        }
      },
      "ImportResult": {
        "oneOf": [
          {
            "type": "object",
            "required": [
              "installed",
              "preview",
              "status"
            ],
            "properties": {
              "installed": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/InstalledPackage"
                },
                "description": "One entry per installed unit: a bundle is one, \"install these\nseparately\" is one each."
              },
              "preview": {
                "$ref": "#/components/schemas/ImportPreview"
              },
              "status": {
                "type": "string",
                "enum": [
                  "installed"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "planId",
              "preview",
              "status"
            ],
            "properties": {
              "planId": {
                "type": "string",
                "description": "Pass this back with a `choice` to answer."
              },
              "preview": {
                "$ref": "#/components/schemas/ImportPreview"
              },
              "status": {
                "type": "string",
                "enum": [
                  "needsChoice"
                ]
              }
            }
          }
        ],
        "description": "The answer to an import request: either it happened, or somebody has to\nchoose first.\n\n⚠ **`NeedsChoice` is a 200, not an error.** It is a legitimate outcome the\ncaller is expected to act on — the issue's \"real pending user-input state\" —\nand an agent that saw a 4xx would reasonably retry the same call rather than\nasking the person the question it was handed.",
        "discriminator": {
          "propertyName": "status"
        }
      },
      "ImportSessionRequest": {
        "type": "object",
        "required": [
          "json"
        ],
        "properties": {
          "json": {
            "type": "string"
          }
        }
      },
      "IngestBody": {
        "type": "object",
        "required": [
          "source",
          "model"
        ],
        "properties": {
          "focus": {
            "type": "string",
            "nullable": true
          },
          "model": {
            "$ref": "#/components/schemas/ModelRef"
          },
          "source": {}
        }
      },
      "IngestConversationBody": {
        "type": "object",
        "required": [
          "session_ids",
          "model"
        ],
        "properties": {
          "focus": {
            "type": "string",
            "nullable": true
          },
          "model": {
            "$ref": "#/components/schemas/ModelRef"
          },
          "session_ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Session ids to digest. At least one is required."
          }
        }
      },
      "InspectJobResponse": {
        "type": "object",
        "properties": {
          "processStartTime": {
            "type": "string",
            "nullable": true
          },
          "runningDurationSeconds": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "sessionId": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "InstalledPackage": {
        "type": "object",
        "description": "What an install did.",
        "required": [
          "id",
          "displayName",
          "kind",
          "skills",
          "directory",
          "replaced",
          "catalogGeneration"
        ],
        "properties": {
          "catalogGeneration": {
            "type": "integer",
            "format": "int64",
            "description": "The catalog generation after the refresh, so a caller can tell whether\nit is looking at an inventory that already includes this.",
            "minimum": 0
          },
          "directory": {
            "type": "string"
          },
          "displayName": {
            "type": "string"
          },
          "entryPoint": {
            "type": "string",
            "nullable": true
          },
          "id": {
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/ImportKind"
          },
          "replaced": {
            "type": "boolean",
            "description": "True when this replaced an existing install of the same id."
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Component skill names, in the order they were installed."
          }
        }
      },
      "InterruptAccepted": {
        "type": "object",
        "description": "Response body for an accepted soft interrupt (#69).",
        "required": [
          "turn_id"
        ],
        "properties": {
          "turn_id": {
            "type": "string",
            "description": "The agent-loop turn that took the message and will inject it. Identifies\nthe reply loop, and is deliberately *not* the `turn_id` `/agent/cancel`\nreports (that one names the server's turn lock); the two id spaces are\nshaped differently so they cannot be confused."
          }
        }
      },
      "InterruptRequest": {
        "type": "object",
        "description": "Request body for the soft-interrupt route.",
        "required": [
          "session_id",
          "text"
        ],
        "properties": {
          "session_id": {
            "type": "string"
          },
          "text": {
            "type": "string"
          },
          "turn_id": {
            "type": "string",
            "description": "Client idempotency key for a steer submitted while a delegated child is\nstill waiting for its initial runtime. Ordinary active-turn interrupts\ndo not require it.",
            "nullable": true
          }
        }
      },
      "JsonObject": {
        "type": "object",
        "additionalProperties": true
      },
      "KbFormat": {
        "type": "string",
        "description": "Which of the two OKF profiles a base's **producer** follows (DR-6).\n\nBoth profiles write the same on-disk shape — BioOKF only adds constraints,\nso a BioOKF bundle is always a valid OKF bundle. The value therefore selects\nhow strictly a *write* is checked and which vocabulary the sub-agent is\ntaught; it never selects how a page is *read*, which is the property that\nlets one reader, one graph deriver and one renderer serve both.\n\n⚠ **On its own this does not answer \"is this base OKF?\".** It carries\n`#[serde(default)]` like every other manifest field (DR-12), so every\n`manifest.yaml` written before Stage 3 — every base on disk — reads back as\n`Okf` while its pages are `title`/`kind` frontmatter and `[[wiki links]]`.\nThe **generation number** is what separates them: ask [`Manifest::profile`],\nwhich folds [`CURRENT_SCHEMA_VERSION`] in, and never this field alone.",
        "enum": [
          "okf",
          "biookf"
        ]
      },
      "KbListEntry": {
        "allOf": [
          {
            "$ref": "#/components/schemas/Manifest"
          },
          {
            "type": "object",
            "required": [
              "tier"
            ],
            "properties": {
              "tier": {
                "$ref": "#/components/schemas/KbTier"
              }
            }
          }
        ],
        "description": "One row of `GET /knowledge/bases`: the stored manifest plus the privacy tier\n(issue #56).\n\nThe tier is **flattened alongside** the manifest rather than added to it,\nbecause `manifest.yaml` is the on-disk record and the tier lives in\n`.kb-tiers`. A `tier` field on [`Manifest`] would be persisted by the next\n`manifest::save` and become a second, staler answer to a question the tier\nstore already answers — and it would also appear on `kb_list_bases`, a\nmodel-facing tool whose payload Task 10D's metadata register governs.\n\nThis route is user-facing: the renderer is the only caller, and Task 10C\nalready removes private bases from the model's own listing entirely."
      },
      "KbTier": {
        "type": "string",
        "description": "A knowledge base's privacy tier (issue #56, design §9.3 B4).\n\nThe stored form is the lowercase word in `.kb-tiers`, and it is the same\nvocabulary [`crate::knowledge::tier`] compares against — one spelling, so the\nenum and the store cannot drift.\n\n⚠ **This is a USER-FACING type, not a model-facing one.** Task 10D's metadata\nregister governs what a model may learn about a base; nothing here is added\nto a `#[tool]` response. It travels on the HTTP surface the renderer reads\n(`GET /knowledge/bases`, `GET|POST /knowledge/bases/{id}/tier`) and nowhere\nelse — in particular it is deliberately NOT a field on [`Manifest`], because\n`manifest.yaml` is the on-disk record and a second copy of the tier there\nwould be a second answer to the question `.kb-tiers` already answers.\n\nThe `bool` in `tier.rs` is unchanged and stays: `biorouter-mcp` cannot depend\non `biorouter`, where `ProviderTier` lives, and the ratchet's argument is the\nCALLER's capability rather than a base's tier.",
        "enum": [
          "public",
          "private"
        ]
      },
      "KbTierResponse": {
        "type": "object",
        "required": [
          "id",
          "tier",
          "page_count",
          "raw_source_count"
        ],
        "properties": {
          "changed_at": {
            "type": "string",
            "description": "RFC 3339, and set exactly when `reason` is.",
            "nullable": true
          },
          "id": {
            "type": "string"
          },
          "page_count": {
            "type": "integer",
            "description": "What a publicize would release, counted from the tree at read time — not\nfrom anything the renderer already had. The confirmation states the blast\nradius rather than asking \"are you sure\", so these are the numbers it\nsays out loud.",
            "minimum": 0
          },
          "raw_source_count": {
            "type": "integer",
            "minimum": 0
          },
          "reason": {
            "type": "string",
            "description": "`publicized_by_user` / `privatized_by_user`, or absent for a base whose\ntier only the ratchet has ever touched. A base the user released must\nnever be indistinguishable from one that was always public.",
            "nullable": true
          },
          "tier": {
            "$ref": "#/components/schemas/KbTier"
          }
        }
      },
      "KillJobResponse": {
        "type": "object",
        "required": [
          "message"
        ],
        "properties": {
          "message": {
            "type": "string"
          }
        }
      },
      "LintBody": {
        "type": "object",
        "required": [
          "model"
        ],
        "properties": {
          "autofix": {
            "type": "boolean",
            "nullable": true
          },
          "model": {
            "$ref": "#/components/schemas/ModelRef"
          }
        }
      },
      "LintReport": {
        "type": "object",
        "description": "The payload of the lint stream's terminal `event: done` frame, and — since\nStage 6 — a published schema.\n\nIt is `ToSchema` rather than merely `Serialize` because\n`POST /knowledge/bases/{id}/lint` answers over SSE, so the shape a client\nhas to parse cannot be inferred from the response body's own type. Declaring\nit puts the four lists **and** the typed [`Diagnostics`] into\n`components.schemas`, and therefore into the generated TypeScript, where the\nrenderer reads the frame. A hand-written interface in `ui/` would be the\nsame contract with nothing keeping it in step.",
        "required": [
          "orphans",
          "contradictions",
          "stale_sources",
          "missing_concept_pages"
        ],
        "properties": {
          "contradictions": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Pages with `contradiction: true` in frontmatter."
          },
          "diagnostics": {
            "$ref": "#/components/schemas/Diagnostics"
          },
          "missing_concept_pages": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Link targets written in source pages that name no page under\n`knowledge/` — again over all four grammars, so a typed base's `edges:`\ncitations are read. Existing, confined raw evidence files are not missing\nconcept pages, even though the graph represents them as external nodes."
          },
          "orphans": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Pages nothing else in the bundle links to, in any of the four grammars\n(`graph::bundle_links`) — not the legacy bracket form alone, which is\nwhat made this list every page of a typed base."
          },
          "stale_sources": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Sources whose `ingested_at` is >90 days ago AND have no inbound links."
          }
        }
      },
      "LintResult": {
        "type": "object",
        "description": "What the lint stream's terminal `event: done` frame actually carries.\n\nThe [`LintReport`] is nested rather than flattened because a lint answers two\nquestions — what is wrong, and what was changed about it — and an autofix\nthat rewrote pages has a `commit_sha` the report itself cannot express.\nPublished as a schema for the reason its `report` field is; see\n[`LintReport`].",
        "required": [
          "report",
          "fixes_applied"
        ],
        "properties": {
          "commit_sha": {
            "type": "string",
            "description": "The autofix commit, or `None` for a read-only lint — which is every lint\nthat ran without `autofix`, and therefore without a provider at all.",
            "nullable": true
          },
          "fixes_applied": {
            "type": "integer",
            "minimum": 0
          },
          "report": {
            "$ref": "#/components/schemas/LintReport"
          }
        }
      },
      "ListPagesQuery": {
        "type": "object",
        "properties": {
          "path_prefix": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "ListSchedulesResponse": {
        "type": "object",
        "required": [
          "jobs"
        ],
        "properties": {
          "jobs": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ScheduledJob"
            }
          }
        }
      },
      "ListWorkflowResponse": {
        "type": "object",
        "required": [
          "manifests"
        ],
        "properties": {
          "manifests": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WorkflowManifest"
            }
          }
        }
      },
      "LlamaCppDeleteRequest": {
        "type": "object",
        "required": [
          "model"
        ],
        "properties": {
          "model": {
            "type": "string",
            "description": "Catalog model name (e.g. `gemma4`) or raw Hugging Face spec."
          }
        }
      },
      "LlamaCppDeleteResponse": {
        "type": "object",
        "required": [
          "deleted_fallback_cache",
          "status"
        ],
        "properties": {
          "deleted_fallback_cache": {
            "type": "boolean",
            "description": "Whether a Biorouter/llama.cpp Hugging Face fallback cache directory was removed."
          },
          "status": {
            "$ref": "#/components/schemas/LlamaCppStatusResponse"
          }
        }
      },
      "LlamaCppEnsureRequest": {
        "type": "object",
        "required": [
          "model"
        ],
        "properties": {
          "model": {
            "type": "string",
            "description": "Catalog model name (e.g. `gemma4`) or raw Hugging Face spec."
          }
        }
      },
      "LlamaCppModel": {
        "type": "object",
        "description": "One curated local model, as shown in the GUI/TUI pickers.",
        "required": [
          "name",
          "display_name",
          "family",
          "official_url",
          "hf_spec",
          "download_size",
          "description",
          "min_gpu_memory_gib",
          "recommended_gpu_memory_gib",
          "context_limit",
          "active_params_b",
          "speed_hint",
          "is_default",
          "downloaded",
          "download_status",
          "download_source",
          "fallback_downloaded",
          "fallback_download_status",
          "suitable",
          "suitability_status",
          "suitability_message"
        ],
        "properties": {
          "active_params_b": {
            "type": "integer",
            "format": "int64",
            "description": "Approximate parameters active per generated token, in billions (MoE\nmodels activate a subset per token; this drives tokens/sec).",
            "minimum": 0
          },
          "context_limit": {
            "type": "integer",
            "minimum": 0
          },
          "description": {
            "type": "string"
          },
          "display_name": {
            "type": "string"
          },
          "download_size": {
            "type": "string"
          },
          "download_source": {
            "type": "string",
            "description": "`ollama`, `huggingface_cache`, or `none`."
          },
          "download_status": {
            "$ref": "#/components/schemas/ModelCacheStatus"
          },
          "downloaded": {
            "type": "boolean",
            "description": "Whether the exact GGUF/quantization is already in Biorouter's llama.cpp cache."
          },
          "fallback_download_status": {
            "$ref": "#/components/schemas/ModelCacheStatus"
          },
          "fallback_downloaded": {
            "type": "boolean",
            "description": "Whether the llama.cpp-compatible Hugging Face fallback is already cached."
          },
          "family": {
            "type": "string"
          },
          "hf_spec": {
            "type": "string"
          },
          "is_default": {
            "type": "boolean",
            "description": "True for the model Biorouter preselects."
          },
          "min_gpu_memory_gib": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "model_path": {
            "type": "string",
            "description": "The local GGUF blob path used when Ollama has already pulled this model.",
            "nullable": true
          },
          "name": {
            "type": "string"
          },
          "official_url": {
            "type": "string"
          },
          "ollama_name": {
            "type": "string",
            "nullable": true
          },
          "recommended_gpu_memory_gib": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "speed_hint": {
            "type": "string",
            "description": "Short human-readable expected-speed hint shown next to the download\nsize (e.g. \"Fast per token — 3B active (MoE), but heavy to load\")."
          },
          "suitability_message": {
            "type": "string"
          },
          "suitability_status": {
            "$ref": "#/components/schemas/LlamaCppSuitability"
          },
          "suitable": {
            "type": "boolean",
            "description": "True when detected GPU-addressable memory meets the recommended tier."
          }
        }
      },
      "LlamaCppStatusResponse": {
        "type": "object",
        "required": [
          "sidecar",
          "catalog",
          "system"
        ],
        "properties": {
          "catalog": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LlamaCppModel"
            }
          },
          "sidecar": {
            "$ref": "#/components/schemas/SidecarStatus"
          },
          "system": {
            "$ref": "#/components/schemas/LlamaCppSystemInfo"
          }
        }
      },
      "LlamaCppSuitability": {
        "type": "string",
        "enum": [
          "suitable",
          "above_recommendation",
          "unknown_resources"
        ]
      },
      "LlamaCppSystemInfo": {
        "type": "object",
        "required": [
          "os",
          "total_memory_gib",
          "accelerator_memory_kind",
          "default_context_size",
          "model_cache_dir",
          "model_cache_layout"
        ],
        "properties": {
          "accelerator_memory_gib": {
            "type": "integer",
            "format": "int64",
            "description": "Apple Silicon unified memory, or detected discrete VRAM elsewhere.",
            "nullable": true,
            "minimum": 0
          },
          "accelerator_memory_kind": {
            "type": "string",
            "description": "`apple_unified`, `vram`, or `unknown_vram`."
          },
          "default_context_size": {
            "type": "integer",
            "minimum": 0
          },
          "model_cache_dir": {
            "type": "string",
            "description": "Ollama-compatible model store root (`OLLAMA_MODELS` or `~/.ollama/models`)."
          },
          "model_cache_layout": {
            "type": "string"
          },
          "os": {
            "type": "string"
          },
          "total_memory_gib": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "LlamaCppWarmupRequest": {
        "type": "object",
        "required": [
          "model"
        ],
        "properties": {
          "model": {
            "type": "string",
            "description": "Catalog model name (e.g. `gemma4`) or raw Hugging Face spec."
          }
        }
      },
      "LlamaCppWarmupResponse": {
        "type": "object",
        "required": [
          "sidecar",
          "output"
        ],
        "properties": {
          "output": {
            "type": "string"
          },
          "sidecar": {
            "$ref": "#/components/schemas/SidecarStatus"
          }
        }
      },
      "LoadedProvider": {
        "type": "object",
        "required": [
          "config",
          "is_editable"
        ],
        "properties": {
          "config": {
            "$ref": "#/components/schemas/DeclarativeProviderConfig"
          },
          "is_editable": {
            "type": "boolean"
          }
        }
      },
      "LocationResponse": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "type": "string",
            "description": "Absolute on-disk path to the knowledge base directory (the folder that\nholds `knowledge/`, `raw/`, `index.md`, …). Clients use this to open the\nfolder in the OS file explorer so users can inspect raw markdown."
          }
        }
      },
      "Manifest": {
        "type": "object",
        "required": [
          "id",
          "name",
          "color",
          "created_at",
          "schema_version",
          "format"
        ],
        "properties": {
          "biookf_version": {
            "type": "string",
            "description": "The BioOKF revision, when [`Self::format`] is `biookf`.\n\nIt lives **here and not in `index.md`** (DR-23's corollary): OKF §8\npermits `okf_version` in a bundle-root index file and nothing else, so a\n`biookf_version` there is a conformance failure — which is exactly the\ndeviation BioOKF's own spec makes and BioRouter does not.",
            "nullable": true
          },
          "color": {
            "type": "string"
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "default_model": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ModelRef"
              }
            ],
            "nullable": true
          },
          "format": {
            "$ref": "#/components/schemas/KbFormat"
          },
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "okf_version": {
            "type": "string",
            "description": "The OKF revision this bundle declares, mirroring the bundle-root\n`index.md` frontmatter (OKF §8/§12). `None` on a base written before the\nOKF generation, which is the honest answer: it declares no revision.\n\nUnlike the six fields above these two take no `#[schema(required)]`.\nThat pairing exists there because the server always serializes those, so\nthe `serde(default)` describes only the read side; here the `None`\ndescribes genuinely absent data, and `required` would be a false\nstatement about the response that the generated TypeScript would then\nbelieve.",
            "nullable": true
          },
          "schema_version": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "MemoryCategoryInventory": {
        "type": "object",
        "description": "One category file, listed in full.",
        "required": [
          "name",
          "entries",
          "revision",
          "size_bytes"
        ],
        "properties": {
          "entries": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MemoryEntry"
            }
          },
          "modified": {
            "type": "integer",
            "format": "int64",
            "description": "The category file's modification time, Unix seconds.\n\n**Per category, not per entry.** Appending any memory restamps the whole\nfile, so this dates the most recent write to the category and says\nnothing about when the other entries arrived. `None` when the filesystem\ndoes not report one.",
            "nullable": true
          },
          "name": {
            "type": "string"
          },
          "revision": {
            "type": "string",
            "description": "Digest of the whole category file as it was listed.\n\nThe compare-and-set token for every delete: any write to the category —\nan agent appending, another window deleting — changes it, so a delete\nthat still carries the listed revision is a delete of the category the\nuser was actually looking at. Without it the user confirms a list that\nhas since moved on and destroys something they were never shown."
          },
          "size_bytes": {
            "type": "integer",
            "format": "int64",
            "description": "The category file's size on disk.",
            "minimum": 0
          }
        }
      },
      "MemoryDeleteCategoryRequest": {
        "type": "object",
        "required": [
          "scope",
          "category",
          "revision"
        ],
        "properties": {
          "category": {
            "type": "string"
          },
          "revision": {
            "type": "string",
            "description": "`MemoryCategoryInventory.revision` of the category as it was listed.\nDeleting a whole category is consent to lose the memories that were on\nthe screen, not whatever arrived while the confirmation was open."
          },
          "scope": {
            "$ref": "#/components/schemas/MemoryScope"
          },
          "working_dir": {
            "type": "string",
            "description": "Required when `scope` is `local`.",
            "nullable": true
          }
        }
      },
      "MemoryDeleteCategoryResponse": {
        "type": "object",
        "required": [
          "removed_entries"
        ],
        "properties": {
          "removed_entries": {
            "type": "integer",
            "description": "How many memories the category held.",
            "minimum": 0
          }
        }
      },
      "MemoryDeleteEntryRequest": {
        "type": "object",
        "required": [
          "scope",
          "category",
          "index",
          "digest",
          "revision"
        ],
        "properties": {
          "category": {
            "type": "string"
          },
          "digest": {
            "type": "string",
            "description": "`MemoryEntry.digest` of the row that was listed at that position — the\nrow's identity, over its tags as well as its body."
          },
          "index": {
            "type": "integer",
            "description": "The entry's position in the category, as listed.",
            "minimum": 0
          },
          "revision": {
            "type": "string",
            "description": "`MemoryCategoryInventory.revision` of the category as it was listed. The\ndelete is a compare-and-set against it, so a list that went stale while\nan agent appended to the store deletes nothing and the caller reloads."
          },
          "scope": {
            "$ref": "#/components/schemas/MemoryScope"
          },
          "working_dir": {
            "type": "string",
            "description": "Required when `scope` is `local`.",
            "nullable": true
          }
        }
      },
      "MemoryDeleteEntryResponse": {
        "type": "object",
        "required": [
          "remaining",
          "category_removed"
        ],
        "properties": {
          "category_removed": {
            "type": "boolean",
            "description": "Whether the category itself was removed because it emptied."
          },
          "remaining": {
            "type": "integer",
            "description": "Memories left in the category.",
            "minimum": 0
          }
        }
      },
      "MemoryEntry": {
        "type": "object",
        "description": "One stored memory, exactly as it sits in the category file.",
        "required": [
          "index",
          "tags",
          "content",
          "digest"
        ],
        "properties": {
          "content": {
            "type": "string",
            "description": "The entry body, interior newlines preserved."
          },
          "digest": {
            "type": "string",
            "description": "Digest of this entry exactly as it is serialized on disk — tag line and\nbody together.\n\nThis is the row's identity, and it is what a delete has to name. The body\nalone is not an identity: two entries can carry the same text under\ndifferent tags, and a body-only guard is satisfied by whichever of them\nhappens to sit at the index (#63 review, finding 6)."
          },
          "index": {
            "type": "integer",
            "description": "Position within the category file, counting from zero.\n\nStable only for as long as the file is untouched, which is why\n[`MemoryServer::delete_entry`] takes [`MemoryEntry::digest`] back as a\nguard rather than trusting the index on its own.",
            "minimum": 0
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Words from the entry's leading `# …` line; empty when it has none."
          }
        }
      },
      "MemoryInventoryResponse": {
        "type": "object",
        "description": "Both stores, as far as the caller can see them.",
        "required": [
          "global"
        ],
        "properties": {
          "global": {
            "$ref": "#/components/schemas/MemoryStoreInventory"
          },
          "local": {
            "allOf": [
              {
                "$ref": "#/components/schemas/MemoryStoreInventory"
              }
            ],
            "nullable": true
          }
        }
      },
      "MemoryScope": {
        "type": "string",
        "description": "Which of the two stores an entry lives in.\n\nThe distinction is the whole subject of issue #63: `Local` is this project's\n`.biorouter/memory`, reachable only by a session opened in that directory;\n`Global` is the machine-wide store every Biorouter session on the computer\nshares.",
        "enum": [
          "global",
          "local"
        ]
      },
      "MemoryStoreInventory": {
        "type": "object",
        "description": "One store: where it is, and everything in it.",
        "required": [
          "scope",
          "path",
          "exists",
          "categories"
        ],
        "properties": {
          "categories": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MemoryCategoryInventory"
            }
          },
          "exists": {
            "type": "boolean",
            "description": "Whether that directory exists yet. The store is created lazily on first\nwrite, so \"no directory\" is the ordinary empty state, not an error."
          },
          "path": {
            "type": "string",
            "description": "Absolute path of the store directory, shown to the user so \"global\" and\n\"local\" are not the only thing they have to go on."
          },
          "scope": {
            "$ref": "#/components/schemas/MemoryScope"
          }
        }
      },
      "MergeBody": {
        "type": "object",
        "required": [
          "source_kb_id"
        ],
        "properties": {
          "dry_run": {
            "type": "boolean",
            "description": "Report what would happen and write nothing. Defaults to **true**, so a\nclient that forgets the field gets the preview rather than the merge.\nThis is the least reversible operation in the subsystem and\n`POST /restore` restores a whole tree, not one page."
          },
          "source_kb_id": {
            "type": "string",
            "description": "The knowledge base to merge FROM. It is only read and is left unchanged."
          }
        }
      },
      "MergeReport": {
        "type": "object",
        "description": "What the merge did, or — for a dry run — what it *would* do.\n\nThe dry run and the merge produce this from the **same** [`plan`] call, so\nthe preview cannot describe a different operation from the one that runs.",
        "required": [
          "destination_kb_id",
          "source_kb_id",
          "dry_run",
          "raw_copied",
          "raw_deduped",
          "pages_carried",
          "identifiers_renamed",
          "paths_renamed",
          "references_rewritten",
          "destination_pages_before",
          "canonical_violations",
          "destination_tier",
          "owners_added"
        ],
        "properties": {
          "canonical_violations": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Empty when the destination stayed canonical. A non-empty list aborts the\ntransaction, so it is only ever populated on a failure path or a dry run."
          },
          "commit_sha": {
            "type": "string",
            "description": "The squash commit on the destination's `main`, or `None` for a dry run.",
            "nullable": true
          },
          "destination_kb_id": {
            "type": "string"
          },
          "destination_pages_before": {
            "type": "integer",
            "description": "The destination's page count before the merge — the size of the set\n[`verify_snapshot`] checked.",
            "minimum": 0
          },
          "destination_tier": {
            "type": "string",
            "description": "The destination's tier after the merge, as the word the store holds."
          },
          "dry_run": {
            "type": "boolean",
            "description": "True when nothing was written."
          },
          "identifiers_renamed": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Rename"
            },
            "description": "Identifier collisions. The **incoming** side was renamed; the\ndestination's identifier is untouched."
          },
          "owners_added": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Institutions added to the destination by the fold (DR-26)."
          },
          "pages_carried": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CarriedPage"
            }
          },
          "paths_renamed": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Rename"
            },
            "description": "Page-path collisions, as logical paths."
          },
          "raw_copied": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Rename"
            },
            "description": "Raw sources copied into the destination, `from` → `to` (equal when there\nwas no collision)."
          },
          "raw_deduped": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RawDedup"
            },
            "description": "Raw sources already present in the destination, matched on the sha256 in\n`raw/<id>/meta.yaml`. Not copied; every reference to them is repointed at\nthe destination's copy."
          },
          "references_rewritten": {
            "type": "integer",
            "description": "How many references were repointed — edge `object`, edge\n`primary_source`, `raw_source` paths and body links.",
            "minimum": 0
          },
          "references_seen": {
            "type": "integer",
            "description": "How many references the rewriter **looked at**, of the same four kinds.\n\nThe denominator, and it is here because its absence hid a corruption. A\npreview that says only \"1 identifier renamed, 3 references rewritten\"\nreads as complete, and read exactly that way while plain `[[Name]]`\nlinks went through a map that could not rename them — silently\nretargeted at the destination's own page of that name. `seen -\nrewritten` is what the merge saw and deliberately left alone, so a\nreader can ask why a number is large before approving the least\nreversible operation in the subsystem.",
            "minimum": 0
          },
          "source_kb_id": {
            "type": "string"
          }
        }
      },
      "Message": {
        "type": "object",
        "description": "A message to or from an LLM",
        "required": [
          "role",
          "created",
          "content",
          "metadata"
        ],
        "properties": {
          "content": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/MessageContent"
            }
          },
          "created": {
            "type": "integer",
            "format": "int64"
          },
          "id": {
            "type": "string",
            "nullable": true
          },
          "metadata": {
            "$ref": "#/components/schemas/MessageMetadata"
          },
          "role": {
            "$ref": "#/components/schemas/Role"
          }
        }
      },
      "MessageContent": {
        "oneOf": [
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/TextContent"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "text"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/ImageContent"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "image"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/ToolRequest"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "toolRequest"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/ToolResponse"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "toolResponse"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/ToolConfirmationRequest"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "toolConfirmationRequest"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/ActionRequired"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "actionRequired"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/FrontendToolRequest"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "frontendToolRequest"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/ThinkingContent"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "thinking"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/RedactedThinkingContent"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "redactedThinking"
                    ]
                  }
                }
              }
            ]
          },
          {
            "allOf": [
              {
                "$ref": "#/components/schemas/SystemNotificationContent"
              },
              {
                "type": "object",
                "required": [
                  "type"
                ],
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": [
                      "systemNotification"
                    ]
                  }
                }
              }
            ]
          }
        ],
        "description": "Content passed inside a message, which can be both simple content and tool content",
        "discriminator": {
          "propertyName": "type"
        }
      },
      "MessageEvent": {
        "oneOf": [
          {
            "type": "object",
            "description": "Authoritative lifecycle edge for a turn visible on the read-only session\nobserver. It lets a quiet turn render as active without inferring work\nfrom model output or polling `/agent/resume` across initialization.",
            "required": [
              "turn_id",
              "type"
            ],
            "properties": {
              "turn_id": {
                "type": "string"
              },
              "type": {
                "type": "string",
                "enum": [
                  "TurnStarted"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Authoritative observer snapshot of the session's current lifecycle.\n`None` is meaningful: it retires a turn whose terminal edge was missed\nwhile the observer was disconnected.",
            "required": [
              "active_turn_id",
              "type"
            ],
            "properties": {
              "active_turn_id": {
                "type": "string",
                "nullable": true
              },
              "type": {
                "type": "string",
                "enum": [
                  "TurnState"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "message",
              "token_state",
              "type"
            ],
            "properties": {
              "message": {
                "$ref": "#/components/schemas/Message"
              },
              "token_state": {
                "$ref": "#/components/schemas/TokenState"
              },
              "type": {
                "type": "string",
                "enum": [
                  "Message"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "error",
              "code",
              "scope",
              "retryable",
              "type"
            ],
            "properties": {
              "code": {
                "type": "string"
              },
              "error": {
                "type": "string"
              },
              "provider_kind": {
                "type": "string",
                "nullable": true
              },
              "retryable": {
                "type": "boolean"
              },
              "scope": {
                "$ref": "#/components/schemas/TurnErrorScope"
              },
              "turn_id": {
                "type": "string",
                "nullable": true
              },
              "type": {
                "type": "string",
                "enum": [
                  "Error"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "reason",
              "token_state",
              "type"
            ],
            "properties": {
              "reason": {
                "type": "string"
              },
              "token_state": {
                "$ref": "#/components/schemas/TokenState"
              },
              "turn_id": {
                "type": "string",
                "nullable": true
              },
              "type": {
                "type": "string",
                "enum": [
                  "Finish"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "model",
              "mode",
              "type"
            ],
            "properties": {
              "mode": {
                "type": "string"
              },
              "model": {
                "type": "string"
              },
              "type": {
                "type": "string",
                "enum": [
                  "ModelChange"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "request_id",
              "message",
              "type"
            ],
            "properties": {
              "message": {
                "type": "object"
              },
              "request_id": {
                "type": "string"
              },
              "type": {
                "type": "string",
                "enum": [
                  "Notification"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "A tool call the model has begun emitting, announced as soon as its name\nis known — before its arguments finish generating. Advisory only: the\nclient draws a skeleton tool card keyed by `id` and later merges the\nauthoritative `Message` tool request (same `id`) into it. This is NOT a\ntool request and must never be dispatched, gated, or persisted; it does\nnot enter `all_messages` or the coalescer.",
            "required": [
              "id",
              "name",
              "type"
            ],
            "properties": {
              "id": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "partial_args": {
                "type": "string",
                "nullable": true
              },
              "type": {
                "type": "string",
                "enum": [
                  "ToolCallPending"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "conversation",
              "token_state",
              "type"
            ],
            "properties": {
              "conversation": {
                "$ref": "#/components/schemas/Conversation"
              },
              "token_state": {
                "$ref": "#/components/schemas/TokenState"
              },
              "type": {
                "type": "string",
                "enum": [
                  "UpdateConversation"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "#59: the ids this turn's messages were actually persisted under.\n\nA client that watches a whole turn go by cannot otherwise learn them: a\nmessage is streamed *before* it is stored, one streamed assistant reply\ncan become several stored rows, and the model-only rows (BR-47 post-edit\ndiagnostics, loop-guard / stall / budget nudges, hook context) are never\nstreamed at all. Without this frame `expectedMessageIds` on\n`POST /sessions/{session_id}/edit_message` — the ids of every message the\nclient's view holds — is unsatisfiable, and the in-place edit it guards\nanswers 409 on a session nobody else has touched.\n\nIds only: this frame never re-sends message bodies. Each entry carries\n`userVisible`, which is what separates a row the client is deliberately\nnot shown from one it was simply never told about — a client draws\nnothing for a `userVisible: false` row but must still name it.\n\nThe converse does NOT hold: `userVisible: true` is not an instruction to\ndraw, it is the absence of the hidden flag, and the content usually\narrived already inside a `Message` frame (one streamed reply is stored as\nseveral rows). This frame is for accounting; the transcript comes from\n`Message` frames alone. See `PersistedMessage::user_visible`.",
            "required": [
              "messages",
              "type"
            ],
            "properties": {
              "messages": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/PersistedMessage"
                }
              },
              "type": {
                "type": "string",
                "enum": [
                  "MessagesPersisted"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "Issue #56 Gate B: what this turn is running on — the provider and model\nthe agent holds at the seam, plus the chat's classification after the\nturn ratchet has committed.\n\nAdvisory display state, exactly like `ToolCallPending`: never persisted,\nnever replayed, never shown to the model, and it changes nothing about\nwhat the privacy gates permit or refuse. `privacy_tier` here is a REPORT\nof a classification the daemon has already written; reading it is not a\nsecond write and no gate consults this frame.\n\n⚠ **Sent on every turn that reaches a provider**, not only on a repaired\nbind. It carries the two fields a client cannot compute and that change\nunderneath it: the ratcheted `privacy_tier` (measured by #196 as the one\nfield that lagged, which is why a chat a turn had just made private\nshowed neither the note nor the chip override until a reload) and the\nbinding `restore_provider_from_session` took from a row another window,\nthe CLI or a schedule may have rewritten.\n\n⚠ It does NOT carry the selection it may have displaced, so a client must\nnot treat receiving it as \"the user's choice was overridden\" — on the\noverwhelmingly common turn it names exactly what the client is already\nshowing. Compare it against what you display, and say nothing when they\nagree. Widening it from the repair arm is safe precisely because that\ncomparison, not the frame's arrival, is what earns a word on screen.",
            "required": [
              "provider",
              "model",
              "privacy_tier",
              "type"
            ],
            "properties": {
              "model": {
                "type": "string"
              },
              "privacy_reason": {
                "type": "string",
                "description": "That classification's provenance on the row (`turn:versa_azure`,\n`mcp:…`, `backfill:…`), or `None` for a row never raised.",
                "nullable": true
              },
              "privacy_tier": {
                "$ref": "#/components/schemas/SessionClassification"
              },
              "provider": {
                "type": "string"
              },
              "type": {
                "type": "string",
                "enum": [
                  "PrivacyProviderPinned"
                ]
              }
            }
          },
          {
            "type": "object",
            "required": [
              "type"
            ],
            "properties": {
              "type": {
                "type": "string",
                "enum": [
                  "Ping"
                ]
              }
            }
          }
        ],
        "discriminator": {
          "propertyName": "type"
        }
      },
      "MessageMetadata": {
        "type": "object",
        "description": "Metadata for message visibility",
        "required": [
          "userVisible",
          "agentVisible"
        ],
        "properties": {
          "agentVisible": {
            "type": "boolean",
            "description": "Whether the message should be included in the agent's context window"
          },
          "pinned": {
            "type": "boolean",
            "description": "#51: carry this message verbatim through every compaction instead of\ndissolving it into a summary. Never dropped by summarization — but still\nsubject to the overall context budget, which trims the oldest pins first\nand reports it. Defaults to false."
          },
          "provenance": {
            "allOf": [
              {
                "$ref": "#/components/schemas/MessageProvenance"
              }
            ],
            "nullable": true
          },
          "userVisible": {
            "type": "boolean",
            "description": "Whether the message should be visible to the user in the UI"
          }
        }
      },
      "MessageProvenance": {
        "type": "object",
        "description": "Where a message came from, when it did not originate with this session's own\nuser↔agent pair. Cross-session control without provenance is\nindistinguishable from prompt injection (BR-71 §2.4) — stamped in storage,\nnot just in the UI.\n\n**The guarantee, stated as what is actually enforced.** A stamp is not\nsuppressible by anything on the normalize → compact → write-back path, which\nis the path that decides what the model sees and what the store keeps. Three\nsites across two stages had to be taught it, because each rebuilds or\nreplaces metadata rather than updating it — the `..self` builders inherited\nthe field for free, which is exactly why these stood out:\n\n- [`crate::conversation::merge_consecutive_messages`] keeps only the first\nmessage's metadata, so a change of origin is a merge boundary\n(`is_provenance_boundary`), exactly as `pinned` is.\n- the legacy compaction path replaces the archived original's metadata with\n`MessageMetadata::invisible()`, and rebuilds the preserved copy from its\ntext alone; both carry the stamp across explicitly\n(`crate::context_mgmt`).\n\nIt is deliberately NOT a claim about anything outside that path. A caller\nholding a `Message` can always construct an unstamped one, and a stamp whose\n`kind` a reader does not recognise degrades to `None` rather than taking the\nrest of the metadata down with it (see `MessageMetadata::provenance`). The\ndefence a stamp *enables* — [`frame_workspace_injection`] — is baked into\nmessage content, not metadata, precisely so it cannot be undone by a metadata\nedit.\n\n`Hash` is derived deliberately: this value is part of\n[`crate::conversation::normalize`]'s per-message cache validator. See\n`message_fingerprint` there.",
        "required": [
          "kind"
        ],
        "properties": {
          "fromSessionId": {
            "type": "string",
            "nullable": true
          },
          "fromSessionName": {
            "type": "string",
            "nullable": true
          },
          "kind": {
            "$ref": "#/components/schemas/ProvenanceKind"
          }
        }
      },
      "ModelCacheStatus": {
        "type": "string",
        "enum": [
          "downloaded",
          "partial",
          "not_downloaded"
        ]
      },
      "ModelConfig": {
        "type": "object",
        "required": [
          "model_name",
          "toolshim"
        ],
        "properties": {
          "context_limit": {
            "type": "integer",
            "nullable": true,
            "minimum": 0
          },
          "fast_model": {
            "type": "string",
            "nullable": true
          },
          "max_tokens": {
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "model_name": {
            "type": "string"
          },
          "reasoning_effort": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ReasoningEffort"
              }
            ],
            "nullable": true
          },
          "request_params": {
            "type": "object",
            "description": "Provider-specific request parameters (e.g., anthropic_beta headers)",
            "additionalProperties": {},
            "nullable": true
          },
          "temperature": {
            "type": "number",
            "format": "float",
            "nullable": true
          },
          "toolshim": {
            "type": "boolean"
          },
          "toolshim_model": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "ModelInfo": {
        "type": "object",
        "description": "Information about a model's capabilities",
        "required": [
          "name",
          "context_limit"
        ],
        "properties": {
          "context_limit": {
            "type": "integer",
            "description": "The maximum context length this model supports",
            "minimum": 0
          },
          "currency": {
            "type": "string",
            "description": "Currency for the costs (default: \"$\")",
            "nullable": true
          },
          "input_token_cost": {
            "type": "number",
            "format": "double",
            "description": "Cost per token for input (optional)",
            "nullable": true
          },
          "name": {
            "type": "string",
            "description": "The name of the model"
          },
          "output_token_cost": {
            "type": "number",
            "format": "double",
            "description": "Cost per token for output (optional)",
            "nullable": true
          },
          "supported_input_mime_types": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "MIME types that can be sent as structured model input blocks.",
            "nullable": true
          },
          "supports_cache_control": {
            "type": "boolean",
            "description": "Whether this model supports cache control",
            "nullable": true
          },
          "supports_vision": {
            "type": "boolean",
            "description": "Whether this model accepts image inputs (multimodal vision)",
            "nullable": true
          }
        }
      },
      "ModelRef": {
        "type": "object",
        "required": [
          "provider",
          "model"
        ],
        "properties": {
          "model": {
            "type": "string"
          },
          "provider": {
            "type": "string"
          }
        }
      },
      "ModelUsageRow": {
        "type": "object",
        "description": "One `(model, provider)` group of the per-model usage breakdown.\n\n`model_id` / `provider` are `None` for turns recorded before model\nattribution landed, or when the provider reported no model — those rows\naggregate together as the \"unknown\" group.",
        "required": [
          "inputTokens",
          "outputTokens",
          "turns"
        ],
        "properties": {
          "cacheCreationTokens": {
            "type": "integer",
            "format": "int64",
            "description": "Input tokens written to the prompt cache.",
            "nullable": true
          },
          "cacheReadTokens": {
            "type": "integer",
            "format": "int64",
            "description": "Input tokens served from the prompt cache. `None` means at least one\ncontributing event predates cache accounting or did not report it.",
            "nullable": true
          },
          "inputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "modelId": {
            "type": "string",
            "nullable": true
          },
          "outputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "provider": {
            "type": "string",
            "nullable": true
          },
          "totalTokens": {
            "type": "integer",
            "format": "int64",
            "description": "Billed tokens across all four disjoint buckets. `None` means at least\none contributing event has no reconstructable billed total.",
            "nullable": true
          },
          "turns": {
            "type": "integer",
            "format": "int64",
            "description": "Number of billed turns attributed to this group."
          }
        }
      },
      "PackageSummary": {
        "type": "object",
        "description": "The part of an installed package's record the interface needs. The full\nrecord lives beside the skills as `biorouter-package.json`; see\n`crate::agents::skill_package`.",
        "required": [
          "id",
          "displayName"
        ],
        "properties": {
          "displayName": {
            "type": "string"
          },
          "entryPoint": {
            "type": "string",
            "description": "The router/entry-point skill a manifest declared, by frontmatter name.",
            "nullable": true
          },
          "groups": {
            "type": "object",
            "description": "Optional named groups, e.g. `core` / `on-demand`."
          },
          "id": {
            "type": "string"
          },
          "installedAt": {
            "type": "string",
            "nullable": true
          },
          "installer": {
            "type": "string",
            "nullable": true
          },
          "resolvedCommit": {
            "type": "string",
            "nullable": true
          },
          "sourceRef": {
            "type": "string",
            "nullable": true
          },
          "sourceUrl": {
            "type": "string",
            "nullable": true
          },
          "version": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "PageContent": {
        "type": "object",
        "required": [
          "path",
          "content",
          "frontmatter"
        ],
        "properties": {
          "content": {
            "type": "string"
          },
          "frontmatter": {
            "type": "object",
            "description": "Raw YAML frontmatter from the page file."
          },
          "path": {
            "type": "string"
          }
        }
      },
      "PageKind": {
        "type": "string",
        "enum": [
          "source",
          "entity",
          "concept",
          "hub",
          "note",
          "flag"
        ]
      },
      "PageRef": {
        "type": "object",
        "required": [
          "path",
          "title",
          "kind"
        ],
        "properties": {
          "kind": {
            "type": "string"
          },
          "path": {
            "type": "string"
          },
          "title": {
            "type": "string"
          }
        }
      },
      "ParseWorkflowRequest": {
        "type": "object",
        "required": [
          "content"
        ],
        "properties": {
          "content": {
            "type": "string"
          }
        }
      },
      "ParseWorkflowResponse": {
        "type": "object",
        "required": [
          "workflow"
        ],
        "properties": {
          "workflow": {
            "$ref": "#/components/schemas/Workflow"
          }
        }
      },
      "PendingContinuationOwnership": {
        "type": "string",
        "enum": [
          "owned",
          "foreign",
          "settling"
        ]
      },
      "PendingContinuationRef": {
        "type": "object",
        "required": [
          "ownership",
          "superseded_turn_id"
        ],
        "properties": {
          "continuation_lease": {
            "type": "string",
            "description": "Returned only to the same stable owner after the lease is fully live.",
            "nullable": true
          },
          "ownership": {
            "$ref": "#/components/schemas/PendingContinuationOwnership"
          },
          "superseded_turn_id": {
            "type": "string"
          }
        }
      },
      "PermissionLevel": {
        "type": "string",
        "description": "Enum representing the possible permission levels for a tool.",
        "enum": [
          "always_allow",
          "ask_before",
          "never_allow"
        ]
      },
      "PersistedMessage": {
        "type": "object",
        "description": "One row a turn persisted, as published by [`AgentEvent::MessagesPersisted`].",
        "required": [
          "id",
          "userVisible"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "The `msg_uid` the row was actually stored under — the id\n`POST /sessions/{id}/edit_message` compares `expectedMessageIds`\nagainst."
          },
          "userVisible": {
            "type": "boolean",
            "description": "Whether this row is HIDDEN from the transcript. It is not a rendering\ninstruction, and reading it as one double-draws.\n\n`false` is the model-only plumbing a turn stores but deliberately\nkeeps out of the transcript (the BR-47 post-edit diagnostics, the\nloop-guard / stall / budget nudges, hook context). Publishing it\n*with* the flag is what separates \"you are deliberately not being\nshown this row\" from \"you were never told it exists\" — the client can\nname the id without drawing anything for it. That direction is exact:\n`false` means the row must not appear in the transcript.\n\n`true` means only \"not hidden\" — NOT \"draw this\". The content may\nalready have been delivered inside a `Message` frame, and on a\ntool-bearing turn it has been: one streamed reply is stored as a\nrebuilt thinking row plus one `tool_use` row per request, each built\nfrom `Message::assistant()` / `Message::new` and so carrying the\ndefault `user_visible: true`, while the client was shown that same\ncontent once already as the reply itself. A client that drew every\n`true` row would render the same tool request twice.\n\nThis frame is for ACCOUNTING — naming rows so `expectedMessageIds`\ncan be complete. The transcript still comes from `Message` frames\nalone."
          }
        }
      },
      "PlannedSkill": {
        "type": "object",
        "description": "One component of a planned import.",
        "required": [
          "name",
          "description",
          "directory",
          "entryPoint"
        ],
        "properties": {
          "description": {
            "type": "string"
          },
          "directory": {
            "type": "string",
            "description": "Directory name under the package root. Taken from the source layout so\na manifest referring to `skills/media-use` still resolves after install."
          },
          "entryPoint": {
            "type": "boolean",
            "description": "The router the package declares as its way in."
          },
          "group": {
            "type": "string",
            "description": "The manifest's group for this component, e.g. `core` / `on-demand`.",
            "nullable": true
          },
          "name": {
            "type": "string",
            "description": "The frontmatter `name`, preserved exactly."
          }
        }
      },
      "PreviewBody": {
        "type": "object",
        "required": [
          "commit_sha",
          "path"
        ],
        "properties": {
          "commit_sha": {
            "type": "string"
          },
          "path": {
            "type": "string"
          }
        }
      },
      "PreviewResponse": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "PricingData": {
        "type": "object",
        "required": [
          "provider",
          "model",
          "input_token_cost",
          "output_token_cost",
          "currency"
        ],
        "properties": {
          "cache_read_cost": {
            "type": "number",
            "format": "double",
            "nullable": true
          },
          "cache_write_cost": {
            "type": "number",
            "format": "double",
            "nullable": true
          },
          "context_length": {
            "type": "integer",
            "format": "int32",
            "nullable": true,
            "minimum": 0
          },
          "currency": {
            "type": "string"
          },
          "input_token_cost": {
            "type": "number",
            "format": "double"
          },
          "model": {
            "type": "string"
          },
          "output_token_cost": {
            "type": "number",
            "format": "double"
          },
          "provider": {
            "type": "string"
          }
        }
      },
      "PricingQuery": {
        "type": "object",
        "required": [
          "provider",
          "model"
        ],
        "properties": {
          "model": {
            "type": "string"
          },
          "provider": {
            "type": "string"
          }
        }
      },
      "PricingResponse": {
        "type": "object",
        "required": [
          "pricing",
          "source"
        ],
        "properties": {
          "pricing": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PricingData"
            }
          },
          "source": {
            "type": "string"
          }
        }
      },
      "PrincipalType": {
        "type": "string",
        "enum": [
          "Extension",
          "Tool"
        ]
      },
      "PrivacyBarrierBody": {
        "type": "object",
        "description": "The body of the 409 `/agent/update_provider` returns when a privacy boundary\nrefused the bind (issue #56, Gate A).\n\nTyped rather than a bare string because the GUI does not merely report the\nrefusal — it renders §14.4's repair card from it, and the card names both\ncolliding tiers and offers the private models that would work. A plain-text\n409 would leave the renderer parsing prose.",
        "required": [
          "code",
          "session_classification",
          "provider_tier",
          "available_private_providers"
        ],
        "properties": {
          "available_private_providers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The names of the providers a private chat *can* be switched to, so the\ncard can offer the way forward rather than only the wall."
          },
          "code": {
            "type": "string",
            "description": "Discriminator the client switches on. Always `privacy_barrier`."
          },
          "provider_tier": {
            "$ref": "#/components/schemas/ProviderTier"
          },
          "session_classification": {
            "$ref": "#/components/schemas/SessionClassification"
          }
        }
      },
      "PrivacyDisclosureResponse": {
        "type": "object",
        "description": "What `GET /privacy/disclosure` serves (issue #56, DR-17 requirement 3).\n\n⚠ **The copy is on the wire on purpose.** The sentence exists in the GUI\ndialog, the settings panel, the provider grid, the model chip, the CLI,\n`docs/` and the landing site; four hand-written copies drift within one\nrelease and the drifted one is always the one a user reads. One definition\nlives in `biorouter::privacy::disclosure` and the renderer renders what it is\nhanded — a hardcoded English string in a component is the failure this shape\nexists to prevent, and it is invisible until the two disagree.",
        "required": [
          "title_template",
          "long",
          "short",
          "acknowledged"
        ],
        "properties": {
          "acknowledged": {
            "type": "boolean",
            "description": "Has the user acknowledged on this install? Once per install, not once per\nsession — a dialog on every chat is a dialog nobody reads."
          },
          "long": {
            "type": "string",
            "description": "The long form: the blocking dialog and the settings panel."
          },
          "short": {
            "type": "string",
            "description": "The one-line form: the model chip's tooltip and the provider grid's\nCommercial section."
          },
          "title_template": {
            "type": "string",
            "description": "The dialog heading, with `{provider}` still in it — the renderer\nsubstitutes the display name of the provider it is warning about, and so\nnever has to know the English around it."
          }
        }
      },
      "ProvenanceKind": {
        "type": "string",
        "enum": [
          "agent_injection",
          "user_direct",
          "spawn_context"
        ]
      },
      "ProviderAffiliation": {
        "type": "object",
        "description": "DR-26's third axis for one provider **instance**, in the shape a UI can\nrender (issue #56).\n\n⚠ **Resolved from the instance, never from a name.** [`Self::of`] is the only\nconstructor a caller should reach for, and it reads\n[`Provider::tier`] and [`Provider::affiliation`] off one `&dyn Provider` —\nthe same two instance methods every gate reads. A `versa_* => ucsf` table\nwould keep claiming the institution for a Versa module repointed at another\nhost, which `tier()` has already demoted to Public: a private-looking\ninstitution chip on a public flow. Both Versa providers derive `ucsf` from\nthe same gateway-host check that decides their tier\n(`providers::ucsf_gateway_affiliation`), so a repointed instance loses\nPrivate and `ucsf` **together**, and this type is what carries that pairing\nout to the UI.\n\n⚠ **`Option<ProviderAffiliation>::None` is \"public — no affiliation at all\",\nand it is a fourth state distinct from all three\n[`ProviderAffiliationKind`]s.** See [`Self::resolved`] for the table.",
        "required": [
          "kind"
        ],
        "properties": {
          "institutions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AffiliationInstitution"
            },
            "description": "The covering institutions, ascending by id. Non-empty exactly for\n[`ProviderAffiliationKind::Institutions`], and **never** empty there:\n`InstitutionSet` cannot be constructed empty, for the reason its own doc\nrecords (an empty model set is a subset of every allowlist and so reaches\nevery private extension)."
          },
          "kind": {
            "$ref": "#/components/schemas/ProviderAffiliationKind"
          }
        }
      },
      "ProviderAffiliationKind": {
        "type": "string",
        "description": "Which of DR-26's three model-side affiliations this is.\n\n⚠ **There is no `Public` variant, and that is the whole shape of the type.**\nA public model's affiliation is not a value here, it is the *absence* of a\n[`ProviderAffiliation`] — `Option::None` — exactly as\n[`ModelAffiliation`] has no public variant. Giving \"public\" a seat would\ninvite a renderer to draw a chip for it, and a chip that says \"no\ninstitution\" on a public model reads as a *constraint* on a model that has\nnone of the private tier's protections at all.\n\nA closed enum rather than a free string so a renderer can switch on it\ntotally: a fourth affiliation must be a compile error at the badge, not a\nsilently-unhandled branch that renders as the safest-looking of the three.",
        "enum": [
          "local",
          "institutions",
          "unstated"
        ]
      },
      "ProviderDetails": {
        "type": "object",
        "required": [
          "name",
          "metadata",
          "is_configured",
          "provider_type"
        ],
        "properties": {
          "affiliation": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ProviderAffiliation"
              }
            ],
            "nullable": true
          },
          "is_configured": {
            "type": "boolean"
          },
          "metadata": {
            "$ref": "#/components/schemas/ProviderMetadata"
          },
          "name": {
            "type": "string"
          },
          "provider_type": {
            "$ref": "#/components/schemas/ProviderType"
          },
          "resolved_tier": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ProviderTier"
              }
            ],
            "nullable": true
          }
        }
      },
      "ProviderEngine": {
        "type": "string",
        "enum": [
          "openai",
          "ollama",
          "anthropic"
        ]
      },
      "ProviderMetadata": {
        "type": "object",
        "description": "Metadata about a provider's configuration requirements and capabilities",
        "required": [
          "name",
          "display_name",
          "description",
          "default_model",
          "known_models",
          "model_doc_link",
          "config_keys"
        ],
        "properties": {
          "allows_unlisted_models": {
            "type": "boolean",
            "description": "Whether this provider allows entering model names not in the fetched list"
          },
          "config_keys": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConfigKey"
            },
            "description": "Required configuration keys"
          },
          "default_model": {
            "type": "string",
            "description": "The default/recommended model for this provider"
          },
          "description": {
            "type": "string",
            "description": "Description of the provider's capabilities"
          },
          "display_name": {
            "type": "string",
            "description": "Display name for the provider in UIs"
          },
          "institutions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AffiliationInstitution"
            },
            "description": "The institution(s) whose gateway this provider *ships* pointed at — the\n**type-level** institution claim, exactly as [`Self::tier`] is the\ntype-level tier claim, and declared by the same builder pattern\n([`Self::with_institution`]) in the provider's own module.\n\n## Why this exists beside `ProviderDetails::affiliation`\n\n`ProviderDetails::affiliation` is instance-resolved and strictly better,\nand it is what any surface should prefer. But `GET /config/providers`\nresolves it **only for a configured provider** — an unconfigured one has\nno keys, cannot be constructed, and must not have every provider module's\nconstructor run on a plain GET (see `resolve_provider_axes`). So on a\nmachine where nothing is set up yet — first-run onboarding, the one place\na user most needs to be told which gateway belongs to their institution —\nevery row's `affiliation` is `null`. This field is the answer for exactly\nthat gap: it lets a catalog head a group \"UCSF\" from the daemon's own\ndata rather than from a name-keyed table in the renderer.\n\n⚠ **Not a badge, and not an enforcement input.** Same warning as\n[`Self::tier`]'s, and for the same reason: this is what the provider\n*ships* pointed at, so a Versa instance repointed at another host still\ncarries `ucsf` here while [`Provider::affiliation`] correctly reports\n`None`. It may only ever be read as a *fallback for a row the daemon did\nnot resolve*, never in preference to an instance answer, and never by a\ngate. `privacy::affiliation` reads [`Provider::affiliation`] and nothing\nelse.\n\nEmpty for every provider that ships pointed at no institution, which is\nalmost all of them."
          },
          "known_models": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ModelInfo"
            },
            "description": "A list of currently known models with their capabilities"
          },
          "model_doc_link": {
            "type": "string",
            "description": "Link to the docs where models can be found"
          },
          "name": {
            "type": "string",
            "description": "The unique identifier for this provider"
          },
          "runs_locally": {
            "type": "boolean",
            "description": "Whether this provider's inference runs on the user's own machine — a\nbundled or self-hosted server — rather than on a remote service.\n\nDisplay only: it is what splits the private tier into the settings\ngrid's \"Local Models\" and \"Institutional Models\" sections. It is **not**\nthe privacy tier, and neither field is derivable from the other: a\nself-hosted server pointed off the machine is still `runs_locally` by\ntype and Public by instance."
          },
          "tier": {
            "$ref": "#/components/schemas/ProviderTier"
          }
        }
      },
      "ProviderTier": {
        "type": "string",
        "description": "CAPABILITY — the least-privileged model currently bound to a session.\n\nDeliberately **not** `Ord`: `max` over this type is always a bug. A mixed\nlead/worker composite is `least(lead, worker)`, so a private lead with a\npublic worker has **public** reach.",
        "enum": [
          "public",
          "private"
        ]
      },
      "ProviderType": {
        "type": "string",
        "enum": [
          "Preferred",
          "Builtin",
          "Declarative",
          "Custom"
        ]
      },
      "ProvidersResponse": {
        "type": "object",
        "required": [
          "providers"
        ],
        "properties": {
          "providers": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProviderDetails"
            }
          }
        }
      },
      "QuantitativeValue": {
        "oneOf": [
          {
            "type": "number",
            "format": "double",
            "description": "Ordered before [`Self::Text`] so a JSON number deserializes as one;\nserde's untagged reader takes the first arm that matches, and a JSON\nstring never matches `f64`."
          },
          {
            "type": "string"
          }
        ],
        "description": "One reading of one BioOKF §7.3 quantitative slot: a number when the producer\nwrote one, and the text they wrote otherwise.\n\nUI spec §2.1 types the map as `Record<string, string | number>`, and both\narms are load-bearing. `p_value: 3.0e-6` is a number and a renderer that\nreceived it as a string could not right-align or format it; `p_value: <0.001`\nand `effect_size: 2.5 nM` are *also* things a model writes, and coercing\nthose to `None` deletes a statistic with nothing left to report it.\n\nThe variant is chosen by the value alone, never by the key — see\n[`crate::knowledge::graph`]'s `QUANTITATIVE_KEYS` for why the key decides\nonly which map an attribute lands in."
      },
      "QueryBody": {
        "type": "object",
        "required": [
          "question",
          "model"
        ],
        "properties": {
          "file_as_page": {
            "type": "boolean",
            "nullable": true
          },
          "model": {
            "$ref": "#/components/schemas/ModelRef"
          },
          "question": {
            "type": "string"
          }
        }
      },
      "RawAudioContent": {
        "type": "object",
        "required": [
          "data",
          "mimeType"
        ],
        "properties": {
          "data": {
            "type": "string"
          },
          "mimeType": {
            "type": "string"
          }
        }
      },
      "RawDedup": {
        "type": "object",
        "required": [
          "source_id",
          "matched",
          "sha256"
        ],
        "properties": {
          "matched": {
            "type": "string",
            "description": "The destination raw-source id it was found to be byte-identical to."
          },
          "sha256": {
            "type": "string"
          },
          "source_id": {
            "type": "string",
            "description": "The raw-source id in the source base."
          }
        }
      },
      "RawEmbeddedResource": {
        "type": "object",
        "required": [
          "resource"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": true
          },
          "resource": {
            "$ref": "#/components/schemas/ResourceContents"
          }
        }
      },
      "RawImageContent": {
        "type": "object",
        "required": [
          "data",
          "mimeType"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": true
          },
          "data": {
            "type": "string"
          },
          "mimeType": {
            "type": "string"
          }
        }
      },
      "RawResource": {
        "type": "object",
        "required": [
          "uri",
          "name"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": true
          },
          "description": {
            "type": "string"
          },
          "icons": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Icon"
            }
          },
          "mimeType": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "size": {
            "type": "integer",
            "minimum": 0
          },
          "title": {
            "type": "string"
          },
          "uri": {
            "type": "string"
          }
        }
      },
      "RawSourceResponse": {
        "type": "object",
        "required": [
          "source_id",
          "source_md_path"
        ],
        "properties": {
          "source_id": {
            "type": "string"
          },
          "source_md_path": {
            "type": "string"
          }
        }
      },
      "RawTextContent": {
        "type": "object",
        "required": [
          "text"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": true
          },
          "text": {
            "type": "string"
          }
        }
      },
      "ReadPageQuery": {
        "type": "object",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "type": "string"
          }
        }
      },
      "ReadPageResponse": {
        "type": "object",
        "required": [
          "content"
        ],
        "properties": {
          "content": {
            "type": "string"
          }
        }
      },
      "ReadResourceRequest": {
        "type": "object",
        "required": [
          "session_id",
          "extension_name",
          "uri"
        ],
        "properties": {
          "extension_name": {
            "type": "string"
          },
          "session_id": {
            "type": "string"
          },
          "uri": {
            "type": "string"
          }
        }
      },
      "ReadResourceResponse": {
        "type": "object",
        "required": [
          "uri",
          "text"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": {},
            "nullable": true
          },
          "mimeType": {
            "type": "string",
            "nullable": true
          },
          "text": {
            "type": "string"
          },
          "uri": {
            "type": "string"
          }
        }
      },
      "ReasoningEffort": {
        "type": "string",
        "description": "How hard the agent should think this turn.",
        "enum": [
          "quick",
          "normal",
          "deep"
        ]
      },
      "RecoverContinuationAction": {
        "type": "string",
        "enum": [
          "take_over",
          "abandon"
        ]
      },
      "RecoverContinuationRequest": {
        "type": "object",
        "required": [
          "session_id",
          "superseded_turn_id",
          "continuation_owner_id",
          "action"
        ],
        "properties": {
          "action": {
            "$ref": "#/components/schemas/RecoverContinuationAction"
          },
          "continuation_owner_id": {
            "type": "string",
            "description": "Stable per-window owner returned only to that same window on resume."
          },
          "session_id": {
            "type": "string"
          },
          "superseded_turn_id": {
            "type": "string",
            "description": "Exact retired generation shown by `ResumeAgentResponse.pending_continuation`."
          }
        }
      },
      "RecoverContinuationResponse": {
        "type": "object",
        "required": [
          "resolution",
          "superseded_turn_id"
        ],
        "properties": {
          "continuation_lease": {
            "type": "string",
            "description": "Present only after this caller explicitly takes ownership. Group\nabandonment never returns another window's opaque lease.",
            "nullable": true
          },
          "resolution": {
            "type": "string"
          },
          "superseded_turn_id": {
            "type": "string"
          }
        }
      },
      "RedactedThinkingContent": {
        "type": "object",
        "required": [
          "data"
        ],
        "properties": {
          "data": {
            "type": "string"
          }
        }
      },
      "RemoveExtensionRequest": {
        "type": "object",
        "required": [
          "name",
          "session_id"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "RemovePackageRequest": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "The install directory name — a `CatalogBundle.name`, or the last\ncomponent of a `CatalogSkill.slug`."
          },
          "sourceRoot": {
            "type": "string",
            "description": "Which skills root it lives under. Omit for the Biorouter one.\n\n⚠ **Validated against [`skill_catalog::roots`], not merely resolved.**\nThis handler deletes a directory tree, so the root is chosen from the\nset the daemon itself enumerated rather than taken from the caller. A\npath the caller invents matches nothing and is refused — which is why\nthis can safely cover `~/.claude/skills` and a project directory, the\ntwo the Skills pane has always offered a Delete for.",
            "nullable": true
          }
        }
      },
      "Rename": {
        "type": "object",
        "required": [
          "from",
          "to"
        ],
        "properties": {
          "from": {
            "type": "string"
          },
          "to": {
            "type": "string"
          }
        }
      },
      "ResetCategory": {
        "type": "string",
        "enum": [
          "applications",
          "knowledge",
          "skills",
          "extensions",
          "schedules",
          "workflows",
          "history"
        ]
      },
      "ResetCounts": {
        "type": "object",
        "required": [
          "applications",
          "knowledgeBases",
          "skills",
          "extensions",
          "schedules",
          "workflows",
          "conversations"
        ],
        "properties": {
          "applications": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "conversations": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "extensions": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "knowledgeBases": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "schedules": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "skills": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "workflows": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "ResetErrorResponse": {
        "type": "object",
        "required": [
          "message"
        ],
        "properties": {
          "message": {
            "type": "string"
          }
        }
      },
      "ResetPreviewResponse": {
        "type": "object",
        "required": [
          "counts"
        ],
        "properties": {
          "counts": {
            "$ref": "#/components/schemas/ResetCounts"
          }
        }
      },
      "ResetRequest": {
        "type": "object",
        "required": [
          "categories"
        ],
        "properties": {
          "categories": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ResetCategory"
            }
          }
        }
      },
      "ResetResponse": {
        "type": "object",
        "required": [
          "reset",
          "removed"
        ],
        "properties": {
          "removed": {
            "$ref": "#/components/schemas/ResetCounts"
          },
          "reset": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ResetCategory"
            }
          }
        }
      },
      "ResourceContents": {
        "anyOf": [
          {
            "type": "object",
            "required": [
              "uri",
              "text"
            ],
            "properties": {
              "_meta": {
                "type": "object",
                "additionalProperties": true
              },
              "mimeType": {
                "type": "string"
              },
              "text": {
                "type": "string"
              },
              "uri": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "required": [
              "uri",
              "blob"
            ],
            "properties": {
              "_meta": {
                "type": "object",
                "additionalProperties": true
              },
              "blob": {
                "type": "string"
              },
              "mimeType": {
                "type": "string"
              },
              "uri": {
                "type": "string"
              }
            }
          }
        ]
      },
      "Response": {
        "type": "object",
        "properties": {
          "json_schema": {
            "nullable": true
          }
        }
      },
      "RestartAgentRequest": {
        "type": "object",
        "required": [
          "session_id"
        ],
        "properties": {
          "session_id": {
            "type": "string"
          }
        }
      },
      "RestartAgentResponse": {
        "type": "object",
        "required": [
          "extension_results"
        ],
        "properties": {
          "extension_results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExtensionLoadResult"
            }
          }
        }
      },
      "RestoreBody": {
        "type": "object",
        "required": [
          "commit_sha"
        ],
        "properties": {
          "commit_sha": {
            "type": "string"
          }
        }
      },
      "RestoreResponse": {
        "type": "object",
        "required": [
          "new_commit_sha"
        ],
        "properties": {
          "new_commit_sha": {
            "type": "string"
          }
        }
      },
      "ResumeAgentRequest": {
        "type": "object",
        "required": [
          "session_id",
          "load_model_and_extensions"
        ],
        "properties": {
          "continuation_owner_id": {
            "type": "string",
            "description": "Stable per-window label used only to rehydrate that same window's\npending Stop-and-Send lease. It is not an authentication credential.",
            "nullable": true
          },
          "load_model_and_extensions": {
            "type": "boolean"
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "ResumeAgentResponse": {
        "type": "object",
        "required": [
          "session",
          "initializing"
        ],
        "properties": {
          "active_turn": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ActiveTurnRef"
              }
            ],
            "nullable": true
          },
          "extension_results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExtensionLoadResult"
            },
            "nullable": true
          },
          "initialization_error": {
            "allOf": [
              {
                "$ref": "#/components/schemas/AgentInitializationError"
              }
            ],
            "nullable": true
          },
          "initializing": {
            "type": "boolean",
            "description": "True only while a delegated child is queued before its exact runtime\nprofile has been installed. Clients must wait instead of fetching tools\nor submitting a turn through a generic placeholder agent."
          },
          "pending_continuation": {
            "allOf": [
              {
                "$ref": "#/components/schemas/PendingContinuationRef"
              }
            ],
            "nullable": true
          },
          "session": {
            "$ref": "#/components/schemas/Session"
          }
        }
      },
      "RetryConfig": {
        "type": "object",
        "description": "Configuration for retry logic in workflow execution",
        "required": [
          "max_retries",
          "checks"
        ],
        "properties": {
          "checks": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SuccessCheck"
            },
            "description": "List of success checks to validate workflow completion"
          },
          "max_retries": {
            "type": "integer",
            "format": "int32",
            "description": "Maximum number of retry attempts before giving up",
            "minimum": 0
          },
          "on_failure": {
            "type": "string",
            "description": "Optional shell command to run on failure for cleanup",
            "nullable": true
          },
          "on_failure_timeout_seconds": {
            "type": "integer",
            "format": "int64",
            "description": "Timeout in seconds for on_failure commands (default: 600 seconds)",
            "nullable": true,
            "minimum": 0
          },
          "timeout_seconds": {
            "type": "integer",
            "format": "int64",
            "description": "Timeout in seconds for individual shell commands (default: 300 seconds)",
            "nullable": true,
            "minimum": 0
          }
        }
      },
      "Role": {
        "oneOf": [
          {
            "type": "string"
          },
          {
            "type": "string"
          }
        ]
      },
      "RunNowResponse": {
        "type": "object",
        "required": [
          "session_id"
        ],
        "properties": {
          "session_id": {
            "type": "string"
          }
        }
      },
      "RunningSessionsResponse": {
        "type": "object",
        "description": "BR-71: the sessions holding a turn right now.",
        "required": [
          "session_ids"
        ],
        "properties": {
          "session_ids": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "SaveWorkflowRequest": {
        "type": "object",
        "required": [
          "workflow"
        ],
        "properties": {
          "id": {
            "type": "string",
            "nullable": true
          },
          "workflow": {
            "$ref": "#/components/schemas/Workflow"
          }
        }
      },
      "SaveWorkflowResponse": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          }
        }
      },
      "ScanWorkflowRequest": {
        "type": "object",
        "required": [
          "workflow"
        ],
        "properties": {
          "workflow": {
            "$ref": "#/components/schemas/Workflow"
          }
        }
      },
      "ScanWorkflowResponse": {
        "type": "object",
        "required": [
          "has_security_warnings"
        ],
        "properties": {
          "has_security_warnings": {
            "type": "boolean"
          }
        }
      },
      "ScheduleWorkflowRequest": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "cron_schedule": {
            "type": "string",
            "nullable": true
          },
          "id": {
            "type": "string"
          }
        }
      },
      "ScheduledJob": {
        "type": "object",
        "required": [
          "id",
          "source",
          "cron"
        ],
        "properties": {
          "creator_session_id": {
            "type": "string",
            "description": "Issue #56 (§9.3 C2). The chat this schedule was created from, when there\nwas one — `/loop` and `/schedule` always have one; a workflow scheduled\nfrom the CLI or the schedules route does not.\n\nA scheduled run resolves its provider from THIS session's recorded\n`provider_name` before falling back to the global default (see\n[`resolve_scheduled_provider`]). Without it a job created from a chat on\na private model silently runs on the user's commercial default — R5 —\nand, once the design's §6.3 rule that a `Scheduled` session inherits its\ncreator's classification lands, Gate A would refuse the bind on every\ntick forever.",
            "nullable": true
          },
          "cron": {
            "type": "string"
          },
          "current_session_id": {
            "type": "string",
            "nullable": true
          },
          "currently_running": {
            "type": "boolean"
          },
          "id": {
            "type": "string"
          },
          "last_error": {
            "type": "string",
            "description": "The last run's failure, kept ON THE JOB so the schedules UI can show it.\n\nA cron tick that returns `Err` used to leave nothing behind but a log\nline, and a scheduled run mints a fresh session each time, so there was\nno surface anywhere that a repeating job had been failing since the day\nit was created. Cleared by the next successful run.",
            "nullable": true
          },
          "last_run": {
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "max_runs": {
            "type": "integer",
            "format": "int32",
            "description": "Optional cap on total firings. When `Some(n)`, the job auto-pauses once\n`run_count` reaches `n` — the bound that keeps `/loop` from running\nforever. `None` = unbounded (durable `/schedule` jobs).",
            "nullable": true,
            "minimum": 0
          },
          "owns_source": {
            "type": "boolean",
            "description": "Did the scheduler write [`Self::source`] itself, and may it therefore\ndelete that file when the job goes?\n\n⚠ Written by [`Scheduler::add_scheduled_job`] from its `make_copy`\nargument and by nothing else — a value supplied at a construction site is\noverwritten, so callers pass `None` and let the scheduler answer.\n\n⚠ `Option`, not `bool`, and the third state is the whole point. A job\npersisted before this field existed deserialises as `None`, and the two\ncreation paths that produced those rows are indistinguishable from the\nrow alone: `POST /schedule/create` and `biorouter schedule add` copied the\nworkflow and own the copy, while a schedule added from a workflow row\npoints straight at the user's own file. `false` for both would strand\nevery legacy copy; `true` for both would delete a user's workflow. `None`\nsays \"unrecorded\", and [`scheduler_owns_source`] answers it from where the\nfile actually lives.",
            "nullable": true
          },
          "paused": {
            "type": "boolean"
          },
          "process_start_time": {
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "run_count": {
            "type": "integer",
            "format": "int32",
            "description": "Number of times this job has fired. Used to enforce `max_runs`.",
            "minimum": 0
          },
          "source": {
            "type": "string"
          }
        }
      },
      "SecretDestination": {
        "oneOf": [
          {
            "type": "object",
            "required": [
              "kind"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "keyring"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "An extension's own environment, keyed by extension name.",
            "required": [
              "extensionName",
              "kind"
            ],
            "properties": {
              "extensionName": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "extensionEnv"
                ]
              }
            }
          }
        ],
        "description": "Where the trusted surface must put the values it collects.",
        "discriminator": {
          "propertyName": "kind"
        }
      },
      "SecretKeyRequest": {
        "type": "object",
        "description": "One credential a [`ActionRequiredData::SecretRequest`] card asks for.",
        "required": [
          "key",
          "label"
        ],
        "properties": {
          "description": {
            "type": "string",
            "description": "Optional help text: where to get the value, what format it takes.",
            "nullable": true
          },
          "key": {
            "type": "string",
            "description": "The name it is stored under — an env var name, a keyring entry."
          },
          "label": {
            "type": "string",
            "description": "What the field is called on the card."
          },
          "required": {
            "type": "boolean",
            "description": "Whether the request can be satisfied without this one."
          }
        }
      },
      "Session": {
        "type": "object",
        "required": [
          "id",
          "working_dir",
          "name",
          "created_at",
          "updated_at",
          "extension_data",
          "message_count"
        ],
        "properties": {
          "accumulated_input_tokens": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "accumulated_output_tokens": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "accumulated_total_tokens": {
            "type": "integer",
            "format": "int64",
            "description": "Lifetime totals. New usage writes use the four-bucket billed total;\ndatabases created before billed-bucket accounting may contain legacy\ncontext totals here, so reporting and budgets use `token_events` instead.\nThese counters grow without bound, so SQLite and Rust both use 64-bit.",
            "nullable": true
          },
          "branch_point_msg_uid": {
            "type": "string",
            "description": "The durable `msg_uid` of the exact parent message this session was\nbranched at — the divergence point (BR-45). Paired with `diverged_from`\n(parent session), it is the edge label of the branch forest. `None` for\nnormally-created sessions. Anchoring on this stable id instead of a\nwhole-second timestamp is what fixes the same-second over-truncation.",
            "nullable": true
          },
          "conversation": {
            "allOf": [
              {
                "$ref": "#/components/schemas/Conversation"
              }
            ],
            "nullable": true
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "diverged_from": {
            "type": "string",
            "description": "Id of the session this one was diverged (branched) from, if any. Set by\n`diverge_session`; `None` for normally-created sessions. Lets the UI show\na session's lineage (\"branched from …\").",
            "nullable": true
          },
          "extension_data": {
            "$ref": "#/components/schemas/ExtensionData"
          },
          "id": {
            "type": "string"
          },
          "input_tokens": {
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "message_count": {
            "type": "integer",
            "minimum": 0
          },
          "model_config": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ModelConfig"
              }
            ],
            "nullable": true
          },
          "name": {
            "type": "string"
          },
          "output_tokens": {
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "parent_session_id": {
            "type": "string",
            "description": "Id of the parent session that spawned this one as a subagent (BR-71).\nSibling of `diverged_from` (branch lineage): `diverged_from` records a\nuser fork; this records a delegation. `None` for non-subagent sessions.\nIt is what the interface groups tabs by, and what\n`refuse_unless_direct_subagent_child` reads to keep a delegation-scoped\ngrant pointed at its own children. It is NOT read by the §7 capability\nmatrix any more: that matrix had an `L` axis until lineage stopped being\na boundary for writes (issue #56).",
            "nullable": true
          },
          "privacy_reason": {
            "type": "string",
            "description": "Audit and UX only — never read by a gate. One of `turn:<provider>`,\n`mcp:<extension>`, `inherited:<parent_id>`, `diverged:<parent_id>`,\n`backfill:<provider>`, `declassified_by_user`. §12.4 grades the\ndeclassification confirmation on whether it has ever been `mcp:*`.\n\nIt therefore holds the **dominant** provenance, not the first or the\nlatest one: the storage layer lets an `mcp:` raise displace a non-`mcp:`\nreason and lets nothing displace an `mcp:` reason. Freezing it on the\nfirst raise would answer \"has it ever been `mcp:*`\" with a flat no, since\nGate B's `turn:*` always lands before Gate C's `mcp:*`.",
            "nullable": true
          },
          "privacy_tier": {
            "$ref": "#/components/schemas/SessionClassification"
          },
          "provider_name": {
            "type": "string",
            "nullable": true
          },
          "schedule_id": {
            "type": "string",
            "nullable": true
          },
          "session_type": {
            "$ref": "#/components/schemas/SessionType"
          },
          "total_tokens": {
            "type": "integer",
            "format": "int32",
            "description": "The *current* turn's usage — the live context-window occupancy. Bounded by\nthe model's context limit, so `i32` is safe.",
            "nullable": true
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          },
          "user_set_name": {
            "type": "boolean"
          },
          "user_workflow_values": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "nullable": true
          },
          "workflow": {
            "allOf": [
              {
                "$ref": "#/components/schemas/Workflow"
              }
            ],
            "nullable": true
          },
          "working_dir": {
            "type": "string"
          }
        }
      },
      "SessionClassification": {
        "type": "string",
        "description": "CLASSIFICATION — the most sensitive thing a session has ever touched.\n\n`Ord` is derived and `Public < Private`, so `max` is the accumulation and is\nspellable. Monotone in time; the storage layer refuses to lower it (see\n`SessionUpdateBuilder`'s `CASE WHEN` emission).",
        "enum": [
          "public",
          "private"
        ]
      },
      "SessionDisplayInfo": {
        "type": "object",
        "required": [
          "id",
          "name",
          "createdAt",
          "workingDir",
          "messageCount"
        ],
        "properties": {
          "accumulatedInputTokens": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "accumulatedOutputTokens": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "accumulatedTotalTokens": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "createdAt": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "inputTokens": {
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "messageCount": {
            "type": "integer",
            "minimum": 0
          },
          "name": {
            "type": "string"
          },
          "outputTokens": {
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "scheduleId": {
            "type": "string",
            "nullable": true
          },
          "totalTokens": {
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "workingDir": {
            "type": "string"
          }
        }
      },
      "SessionExtensionsResponse": {
        "type": "object",
        "required": [
          "extensions"
        ],
        "properties": {
          "extensions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExtensionConfig"
            }
          }
        }
      },
      "SessionInsights": {
        "type": "object",
        "required": [
          "totalSessions",
          "sessionsLast7Days",
          "sessionsLast30Days"
        ],
        "properties": {
          "sessionsLast30Days": {
            "type": "integer",
            "minimum": 0
          },
          "sessionsLast7Days": {
            "type": "integer",
            "minimum": 0
          },
          "tokensLast30Days": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "tokensLast7Days": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "totalSessions": {
            "type": "integer",
            "minimum": 0
          },
          "totalTokens": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        }
      },
      "SessionListResponse": {
        "type": "object",
        "required": [
          "sessions"
        ],
        "properties": {
          "sessions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Session"
            },
            "description": "List of available session information objects"
          }
        }
      },
      "SessionMetaChanged": {
        "type": "object",
        "description": "What changed about one chat's row.\n\n⚠ **Advisory, exactly like the frame in `/reply`.** It is not a session\npayload and no gate consults it: `privacy_tier` here is a REPORT of a\nclassification the daemon has already committed, and a consumer that wants\nto act on it re-reads the row.",
        "required": [
          "revision",
          "session_id"
        ],
        "properties": {
          "model_name": {
            "type": "string",
            "description": "`model_config_json`'s `model_name`, as the row now holds it.",
            "nullable": true
          },
          "privacy_reason": {
            "type": "string",
            "description": "The row's classification provenance, as stored.",
            "nullable": true
          },
          "privacy_tier": {
            "type": "string",
            "description": "The row's classification, as stored.",
            "nullable": true
          },
          "provider_name": {
            "type": "string",
            "description": "`provider_name` as the row now holds it.",
            "nullable": true
          },
          "revision": {
            "type": "integer",
            "format": "int64",
            "description": "The revision this change was stamped with.",
            "minimum": 0
          },
          "session_id": {
            "type": "string",
            "description": "The chat whose row moved."
          }
        }
      },
      "SessionMetaDelta": {
        "type": "object",
        "description": "Everything that happened after a client's `since`.",
        "required": [
          "revision",
          "changes"
        ],
        "properties": {
          "changes": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SessionMetaChanged"
            },
            "description": "The changes after `since`, oldest first."
          },
          "revision": {
            "type": "integer",
            "format": "int64",
            "description": "The revision the caller should send next time.",
            "minimum": 0
          },
          "truncated": {
            "type": "boolean",
            "description": "The caller fell further behind than the ring holds, so `changes` is a\nPARTIAL history. Applying it and believing yourself current is the stale\n-row bug this feed exists to end, one layer down. Refetch instead."
          }
        }
      },
      "SessionModelUsageResponse": {
        "type": "object",
        "description": "Per-model token breakdown for one session.",
        "required": [
          "models"
        ],
        "properties": {
          "models": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ModelUsageRow"
            },
            "description": "One row per `(model, provider)` group; a `null` `modelId` is the\n\"unknown\" bucket (turns recorded before model attribution, or providers\nthat reported no model)."
          }
        }
      },
      "SessionSkillsRequest": {
        "type": "object",
        "description": "Add and remove are applied to **this** conversation only.",
        "required": [
          "sessionId"
        ],
        "properties": {
          "add": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Skill (or bundle) names to enable for this conversation, even where the\nmachine-wide preference has them off."
          },
          "remove": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Skill (or bundle) names to disable for this conversation, even where the\nmachine-wide preference has them on."
          },
          "sessionId": {
            "type": "string"
          }
        }
      },
      "SessionSkillsResponse": {
        "type": "object",
        "description": "The catalog after the write, plus the override that produced it.\n\nReturning the whole catalog rather than an acknowledgement is deliberate: it\nis what lets the interface render confirmed state instead of the optimistic\nstate it just guessed, and it collapses the toggle-then-refetch race that\ntwo concurrent toggles would otherwise lose.",
        "required": [
          "catalog",
          "sessionAdd",
          "sessionRemove"
        ],
        "properties": {
          "catalog": {
            "$ref": "#/components/schemas/CatalogView"
          },
          "sessionAdd": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The persisted `workspace_skills/v1` value, echoed so a caller can tell a\nper-chat deviation from a machine-wide default without re-deriving it."
          },
          "sessionRemove": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "SessionState": {
        "type": "string",
        "description": "How one session deviates from the machine-wide answer for a given skill.",
        "enum": [
          "default",
          "added",
          "removed"
        ]
      },
      "SessionSummary": {
        "type": "object",
        "required": [
          "id",
          "working_dir",
          "name",
          "created_at",
          "updated_at",
          "message_count"
        ],
        "properties": {
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "diverged_from": {
            "type": "string",
            "description": "BR-45: the session this one was branched from, or `None`.\n\nCarried on the SUMMARY, not just on the full [`Session`], because the\nsidebar draws a glyph per chat kind and had no other way to know. Its\nonly remaining signal was the default branch NAME (`\"… (branch 2)\"`),\nwhich anyone can rename away: of the branches on this machine, one in\nfive had been renamed and so drew as an ordinary chat.",
            "nullable": true
          },
          "id": {
            "type": "string"
          },
          "message_count": {
            "type": "integer",
            "format": "int64"
          },
          "name": {
            "type": "string"
          },
          "parent_session_id": {
            "type": "string",
            "description": "BR-71: `sub_agent` rows are grouped under this parent in History.",
            "nullable": true
          },
          "privacy_tier": {
            "$ref": "#/components/schemas/SessionClassification"
          },
          "session_type": {
            "type": "string",
            "description": "BR-71: the session's type as stored (`user`/`scheduled`/`sub_agent`).",
            "nullable": true
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          },
          "user_set_name": {
            "type": "boolean"
          },
          "working_dir": {
            "type": "string"
          }
        }
      },
      "SessionType": {
        "type": "string",
        "enum": [
          "user",
          "scheduled",
          "sub_agent",
          "hidden",
          "terminal"
        ]
      },
      "SessionsQuery": {
        "type": "object",
        "required": [
          "limit"
        ],
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 0
          }
        }
      },
      "SetActiveBody": {
        "type": "object",
        "properties": {
          "clear_primary": {
            "type": "boolean",
            "description": "Forget the primary *at this scope*: the session then has no primary\neven while the machine-wide default names a base. Mutually exclusive\nwith `primary_kb` and `inherit_primary`."
          },
          "hidden_kbs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Replace this scope's hidden list — i.e. redefine the session's set.\nOmit to leave the set alone. `[]` is an explicit \"hide nothing here\",\nnot a request to inherit the machine-wide list.",
            "nullable": true
          },
          "inherit_primary": {
            "type": "boolean",
            "description": "Drop this scope's own primary preference. A session then follows the\nmachine-wide choice; at machine scope this restores Biorouter's shipped\nSoul default. This is the way back from `clear_primary`, and the only\nway out of the explicit \"no primary\" that deleting a session's pinned\nbase leaves behind. Mutually exclusive with `primary_kb` and\n`clear_primary`."
          },
          "kb_id": {
            "type": "string",
            "description": "Deprecated alias for `primary_kb`, kept for one release so a stale\nrenderer bundle talking to a fresh daemon keeps working. Follows the\nsame rule: omitted leaves the pointer alone, `null` forgets it — which\nis exactly how such a bundle clears.",
            "nullable": true
          },
          "primary_kb": {
            "type": "string",
            "description": "Make this base the session's primary — the KB-less write target. It\nmust be a member of the **resulting** set, so `hidden_kbs` in the same\nbody is applied first. Omit to leave the pointer alone; send `null` to\nforget it (the same as `clear_primary`).",
            "nullable": true
          },
          "session_id": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "SetDefaultModelBody": {
        "type": "object",
        "properties": {
          "model": {
            "allOf": [
              {
                "$ref": "#/components/schemas/ModelRef"
              }
            ],
            "nullable": true
          }
        }
      },
      "SetKbTierBody": {
        "type": "object",
        "required": [
          "tier"
        ],
        "properties": {
          "tier": {
            "$ref": "#/components/schemas/KbTier"
          }
        }
      },
      "SetProviderRequest": {
        "type": "object",
        "required": [
          "provider",
          "model"
        ],
        "properties": {
          "model": {
            "type": "string"
          },
          "provider": {
            "type": "string"
          }
        }
      },
      "SetSlashCommandRequest": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "slash_command": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "Settings": {
        "type": "object",
        "properties": {
          "biorouter_model": {
            "type": "string",
            "nullable": true
          },
          "biorouter_provider": {
            "type": "string",
            "nullable": true
          },
          "temperature": {
            "type": "number",
            "format": "float",
            "nullable": true
          }
        }
      },
      "SetupResponse": {
        "type": "object",
        "required": [
          "success",
          "message"
        ],
        "properties": {
          "message": {
            "type": "string"
          },
          "success": {
            "type": "boolean"
          }
        }
      },
      "Severity": {
        "type": "string",
        "description": "Three severities, no fatal one — see the module header.\n\nA distinct type from [`okf::Severity`] rather than a re-export, because this\none crosses the wire: it is `Deserialize` and `rename_all = \"lowercase\"`, and\npinning that spelling here means a rename upstream cannot silently change a\nJSON payload the UI reads.",
        "enum": [
          "error",
          "warning",
          "info"
        ]
      },
      "SidebarSessionListResponse": {
        "type": "object",
        "required": [
          "sessions",
          "has_more"
        ],
        "properties": {
          "has_more": {
            "type": "boolean"
          },
          "next_offset": {
            "type": "integer",
            "format": "int32",
            "nullable": true,
            "minimum": 0
          },
          "sessions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SessionSummary"
            }
          }
        }
      },
      "SidecarState": {
        "type": "string",
        "description": "Lifecycle state of the managed llama-server process.",
        "enum": [
          "no_binary",
          "stopped",
          "starting",
          "ready",
          "error"
        ]
      },
      "SidecarStatus": {
        "type": "object",
        "description": "Snapshot of the sidecar consumed by the GUI onboarding card, settings and\n`biorouter doctor`.",
        "required": [
          "state",
          "build"
        ],
        "properties": {
          "binary_path": {
            "type": "string",
            "nullable": true
          },
          "build": {
            "type": "string",
            "description": "Pinned llama.cpp build."
          },
          "context_size": {
            "type": "integer",
            "description": "Context window the running model actually exposes (read live from the\nserver), or `None` until it is ready. Tracks the model, not a preset.",
            "nullable": true,
            "minimum": 0
          },
          "detail": {
            "type": "string",
            "description": "Most recent log line (download progress, load stage) or error tail.",
            "nullable": true
          },
          "hf_spec": {
            "type": "string",
            "description": "Hugging Face `repo:quant` spec backing `model`.",
            "nullable": true
          },
          "model": {
            "type": "string",
            "description": "Friendly model name (catalog name or raw HF spec) currently requested.",
            "nullable": true
          },
          "model_path": {
            "type": "string",
            "description": "Local model blob path used for the running process, if an Ollama model\nstore hit was available.",
            "nullable": true
          },
          "model_source": {
            "type": "string",
            "description": "`ollama` when launched from an Ollama model blob, otherwise\n`huggingface`.",
            "nullable": true
          },
          "ollama_name": {
            "type": "string",
            "description": "Ollama model name checked first, when this is a catalog model.",
            "nullable": true
          },
          "port": {
            "type": "integer",
            "format": "int32",
            "nullable": true,
            "minimum": 0
          },
          "state": {
            "$ref": "#/components/schemas/SidecarState"
          },
          "warmed": {
            "type": "boolean",
            "description": "True only after Biorouter has received a non-empty completion from this\nmodel in this process."
          }
        }
      },
      "SkillRoot": {
        "type": "object",
        "description": "One directory skills are discovered under, with its provenance.",
        "required": [
          "path",
          "source"
        ],
        "properties": {
          "path": {
            "type": "string"
          },
          "source": {
            "$ref": "#/components/schemas/SkillSource"
          }
        }
      },
      "SkillSource": {
        "type": "object",
        "description": "Where a skill came from, as the interface shows it.",
        "required": [
          "kind",
          "label"
        ],
        "properties": {
          "extension": {
            "type": "string",
            "description": "The extension's directory name, when `kind` is `extension`.",
            "nullable": true
          },
          "kind": {
            "$ref": "#/components/schemas/SkillSourceKind"
          },
          "label": {
            "type": "string",
            "description": "A short human label for the \"where from\" chip — the extension's name\nwhen it has one, else the root's own."
          }
        }
      },
      "SkillSourceKind": {
        "type": "string",
        "description": "Which of the five kinds of root a skill came from.\n\n⚠ A **unit-variant** enum carrying no data, with the extension name held\nbeside it in [`SkillSource`] rather than inside an `Extension { .. }`\nvariant. The variant form is the more natural Rust, and it generates an\ninternally-tagged object that `serde(flatten)` and `utoipa` disagree about —\nthe spec emits an `allOf` the TypeScript client cannot narrow. A flat struct\ncrosses the wire unambiguously, which matters more here than the tidier\ntype, because this shape IS the contract the picker renders from.",
        "enum": [
          "claudeHome",
          "agentsHome",
          "biorouter",
          "extension",
          "project"
        ]
      },
      "SkillState": {
        "type": "object",
        "description": "The composed enablement of one catalog entry, with every input kept\nseparate so the interface can explain *why* a switch is where it is.",
        "required": [
          "machineEnabled",
          "session",
          "sessionViaBundle",
          "hiddenContext",
          "effective"
        ],
        "properties": {
          "effective": {
            "type": "boolean",
            "description": "What the model actually sees: the composition of the three above."
          },
          "hiddenContext": {
            "type": "boolean",
            "description": "A shipped **Context** the user switched off in Settings → Contexts. Such\na skill is hidden from the catalog but stays loadable by exact name; see\n`skills_extension::hidden_contexts_in`."
          },
          "machineEnabled": {
            "type": "boolean",
            "description": "`skills-config.json` (`disabled[]`) says this is on. A bundle child is\noff when either its own name or its bundle's name is listed."
          },
          "session": {
            "$ref": "#/components/schemas/SessionState"
          },
          "sessionViaBundle": {
            "type": "boolean",
            "description": "The deviation was written against the BUNDLE's name, not this skill's.\nLets the interface explain a member's switch instead of leaving it\nlooking arbitrary."
          }
        }
      },
      "SlashCommand": {
        "type": "object",
        "required": [
          "command",
          "help",
          "command_type"
        ],
        "properties": {
          "command": {
            "type": "string"
          },
          "command_type": {
            "$ref": "#/components/schemas/CommandType"
          },
          "help": {
            "type": "string"
          }
        }
      },
      "SlashCommandsResponse": {
        "type": "object",
        "required": [
          "commands"
        ],
        "properties": {
          "commands": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SlashCommand"
            }
          }
        }
      },
      "SourceMeta": {
        "type": "object",
        "required": [
          "id",
          "title",
          "ingested_at",
          "sha256",
          "mime",
          "credibility"
        ],
        "properties": {
          "credibility": {
            "$ref": "#/components/schemas/Credibility"
          },
          "id": {
            "type": "string"
          },
          "ingested_at": {
            "type": "string",
            "format": "date-time"
          },
          "mime": {
            "type": "string"
          },
          "original_filename": {
            "type": "string",
            "nullable": true
          },
          "sha256": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "url": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "SourceProvenance": {
        "type": "object",
        "description": "What a resolved import came from, kept so an update knows where to look and\na user can see what they installed.",
        "properties": {
          "installer": {
            "type": "string",
            "description": "`repository` | `archive` | `marketplace` | `cli` | `agent`.",
            "nullable": true
          },
          "reference": {
            "type": "string",
            "description": "The branch, tag or commit that was asked for.",
            "nullable": true
          },
          "resolvedCommit": {
            "type": "string",
            "description": "The immutable commit the archive actually came from, when the host tells\nus (GitHub returns it in `ETag`).",
            "nullable": true
          },
          "url": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "StartAgentRequest": {
        "type": "object",
        "required": [
          "working_dir"
        ],
        "properties": {
          "extension_overrides": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExtensionConfig"
            },
            "nullable": true
          },
          "workflow": {
            "allOf": [
              {
                "$ref": "#/components/schemas/Workflow"
              }
            ],
            "nullable": true
          },
          "workflow_deeplink": {
            "type": "string",
            "nullable": true
          },
          "workflow_id": {
            "type": "string",
            "nullable": true
          },
          "working_dir": {
            "type": "string"
          }
        }
      },
      "StopAgentRequest": {
        "type": "object",
        "required": [
          "session_id"
        ],
        "properties": {
          "session_id": {
            "type": "string"
          }
        }
      },
      "SubWorkflow": {
        "type": "object",
        "required": [
          "name",
          "path"
        ],
        "properties": {
          "description": {
            "type": "string",
            "nullable": true
          },
          "name": {
            "type": "string"
          },
          "path": {
            "type": "string"
          },
          "sequential_when_repeated": {
            "type": "boolean"
          },
          "values": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "nullable": true
          }
        }
      },
      "SubmitSecretsRequest": {
        "type": "object",
        "description": "Answering a credential card (#117).\n\n⚠ **This request body carries secrets and nothing else in the codebase does.**\nThe values reach `submit_credentials`, which writes them to the OS credential\nstore and drops them. Do not add a field to the response that could carry one\nback, do not add `#[derive(Debug)]` (see the hand-written impl below), and do\nnot log the body.",
        "required": [
          "id"
        ],
        "properties": {
          "cancelled": {
            "type": "boolean",
            "description": "The user dismissed the dialog. `values` is ignored when this is set."
          },
          "id": {
            "type": "string",
            "description": "The card id, as published on the `secretRequest` message."
          },
          "values": {
            "type": "object",
            "description": "What the user typed, keyed by the card's key names.",
            "additionalProperties": {
              "type": "string"
            }
          }
        }
      },
      "SuccessCheck": {
        "oneOf": [
          {
            "type": "object",
            "description": "Execute a shell command and check its exit status (0 = pass).",
            "required": [
              "command",
              "type"
            ],
            "properties": {
              "command": {
                "type": "string",
                "description": "The shell command to execute"
              },
              "type": {
                "type": "string",
                "enum": [
                  "Shell"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "BR-48: pass when a file exists at `path`. A relative `path` resolves\nagainst the session working directory (interactive gate) or the process\nworking directory (workflow retry).",
            "required": [
              "path",
              "type"
            ],
            "properties": {
              "path": {
                "type": "string",
                "description": "The file (or directory) that must exist."
              },
              "type": {
                "type": "string",
                "enum": [
                  "FileExists"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "BR-48: run `command` and pass when its combined stdout+stderr contains\n`substring`. Unlike [`Shell`](SuccessCheck::Shell), the exit status is\nignored — this checks the *content* of the output (e.g. `0 failed`,\n`PASS`, a coverage percentage).",
            "required": [
              "command",
              "substring",
              "type"
            ],
            "properties": {
              "command": {
                "type": "string",
                "description": "The shell command whose output is inspected."
              },
              "substring": {
                "type": "string",
                "description": "The substring the output must contain."
              },
              "type": {
                "type": "string",
                "enum": [
                  "OutputContains"
                ]
              }
            }
          },
          {
            "type": "object",
            "description": "BR-48: pass when the JSON file at `path` parses and validates against the\ninline JSON Schema `schema` (same engine as the workflow final-output\ncontract, [`crate::agents::final_output_tool`]).",
            "required": [
              "path",
              "schema",
              "type"
            ],
            "properties": {
              "path": {
                "type": "string",
                "description": "The JSON file to validate."
              },
              "schema": {
                "description": "The JSON Schema the file's contents must satisfy."
              },
              "type": {
                "type": "string",
                "enum": [
                  "JsonSchema"
                ]
              }
            }
          }
        ],
        "description": "A single success check to validate workflow completion, or (BR-48) to gate\nan interactive chat turn from finishing with unmet conditions.\n\nOriginally a single `Shell` variant used only by the workflow retry path.\nBR-48 added the non-shell variants and a done-ness gate so an interactive\nsession can also declare \"not done until these hold\" — see\n[`crate::agents::done_gate`].",
        "discriminator": {
          "propertyName": "type"
        }
      },
      "SystemInfo": {
        "type": "object",
        "required": [
          "app_version",
          "os",
          "os_version",
          "architecture",
          "enabled_extensions"
        ],
        "properties": {
          "app_version": {
            "type": "string"
          },
          "architecture": {
            "type": "string"
          },
          "enabled_extensions": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "model": {
            "type": "string",
            "nullable": true
          },
          "os": {
            "type": "string"
          },
          "os_version": {
            "type": "string"
          },
          "provider": {
            "type": "string",
            "nullable": true
          }
        }
      },
      "SystemNotificationContent": {
        "type": "object",
        "required": [
          "notificationType",
          "msg"
        ],
        "properties": {
          "msg": {
            "type": "string"
          },
          "notificationType": {
            "$ref": "#/components/schemas/SystemNotificationType"
          }
        }
      },
      "SystemNotificationType": {
        "type": "string",
        "enum": [
          "thinkingMessage",
          "inlineMessage"
        ]
      },
      "TextContent": {
        "type": "object",
        "required": [
          "text"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": true
          },
          "annotations": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/Annotations"
              },
              {
                "type": "object"
              }
            ]
          },
          "text": {
            "type": "string"
          }
        }
      },
      "ThinkingContent": {
        "type": "object",
        "required": [
          "thinking",
          "signature"
        ],
        "properties": {
          "signature": {
            "type": "string"
          },
          "thinking": {
            "type": "string"
          }
        }
      },
      "TokenState": {
        "type": "object",
        "required": [
          "inputTokens",
          "outputTokens",
          "totalTokens",
          "accumulatedInputTokens",
          "accumulatedOutputTokens",
          "accumulatedTotalTokens"
        ],
        "properties": {
          "accumulatedInputTokens": {
            "type": "integer",
            "format": "int64",
            "description": "Lifetime totals. `i64` because they grow without bound over a long\nsession; `i32` wrapped negative at ~2.1e9."
          },
          "accumulatedOutputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "accumulatedTotalTokens": {
            "type": "integer",
            "format": "int64"
          },
          "inputTokens": {
            "type": "integer",
            "format": "int32"
          },
          "outputTokens": {
            "type": "integer",
            "format": "int32"
          },
          "totalTokens": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "Tool": {
        "type": "object",
        "required": [
          "name",
          "inputSchema"
        ],
        "properties": {
          "_meta": {
            "type": "object",
            "additionalProperties": true
          },
          "annotations": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ToolAnnotations"
              },
              {
                "type": "object"
              }
            ]
          },
          "description": {
            "type": "string"
          },
          "icons": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Icon"
            }
          },
          "inputSchema": {
            "type": "object",
            "additionalProperties": true
          },
          "name": {
            "type": "string"
          },
          "outputSchema": {
            "type": "object",
            "additionalProperties": true
          },
          "title": {
            "type": "string"
          }
        }
      },
      "ToolAnnotations": {
        "type": "object",
        "properties": {
          "destructiveHint": {
            "type": "boolean"
          },
          "idempotentHint": {
            "type": "boolean"
          },
          "openWorldHint": {
            "type": "boolean"
          },
          "readOnlyHint": {
            "type": "boolean"
          },
          "title": {
            "type": "string"
          }
        }
      },
      "ToolConfirmationRequest": {
        "type": "object",
        "required": [
          "id",
          "toolName",
          "arguments"
        ],
        "properties": {
          "arguments": {
            "$ref": "#/components/schemas/JsonObject"
          },
          "id": {
            "type": "string"
          },
          "prompt": {
            "type": "string",
            "nullable": true
          },
          "toolName": {
            "type": "string"
          }
        }
      },
      "ToolInfo": {
        "type": "object",
        "description": "Information about the tool used for building prompts",
        "required": [
          "name",
          "description",
          "parameters"
        ],
        "properties": {
          "description": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "parameters": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "permission": {
            "allOf": [
              {
                "$ref": "#/components/schemas/PermissionLevel"
              }
            ],
            "nullable": true
          }
        }
      },
      "ToolPermission": {
        "type": "object",
        "required": [
          "tool_name",
          "permission"
        ],
        "properties": {
          "permission": {
            "$ref": "#/components/schemas/PermissionLevel"
          },
          "tool_name": {
            "type": "string"
          }
        }
      },
      "ToolPreview": {
        "oneOf": [
          {
            "type": "object",
            "description": "A command that will be handed to a shell verbatim.",
            "required": [
              "command",
              "truncated",
              "kind"
            ],
            "properties": {
              "command": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "shell"
                ]
              },
              "truncated": {
                "type": "boolean",
                "description": "The command was clipped to fit the frame."
              }
            }
          },
          {
            "type": "object",
            "description": "An edit to a file that already exists, as a line diff.",
            "required": [
              "path",
              "lines",
              "added",
              "removed",
              "truncated",
              "kind"
            ],
            "properties": {
              "added": {
                "type": "integer",
                "minimum": 0
              },
              "kind": {
                "type": "string",
                "enum": [
                  "fileEdit"
                ]
              },
              "lines": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/ToolPreviewLine"
                }
              },
              "path": {
                "type": "string"
              },
              "removed": {
                "type": "integer",
                "minimum": 0
              },
              "truncated": {
                "type": "boolean",
                "description": "Lines beyond the cap were dropped; `added`/`removed` still count the whole edit."
              }
            }
          },
          {
            "type": "object",
            "description": "Creation of a new file, as its full (clipped) contents.",
            "required": [
              "path",
              "content",
              "lineCount",
              "truncated",
              "kind"
            ],
            "properties": {
              "content": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "fileWrite"
                ]
              },
              "lineCount": {
                "type": "integer",
                "minimum": 0
              },
              "path": {
                "type": "string"
              },
              "truncated": {
                "type": "boolean"
              }
            }
          },
          {
            "type": "object",
            "description": "Anything else: the arguments, pretty-printed, so no tool confirms blind.",
            "required": [
              "json",
              "truncated",
              "kind"
            ],
            "properties": {
              "json": {
                "type": "string"
              },
              "kind": {
                "type": "string",
                "enum": [
                  "arguments"
                ]
              },
              "truncated": {
                "type": "boolean"
              }
            }
          }
        ],
        "description": "What a pending tool call will do, in a shape the confirmation card can render.",
        "discriminator": {
          "propertyName": "kind"
        }
      },
      "ToolPreviewLine": {
        "type": "object",
        "description": "One line of a rendered diff, with its provenance.",
        "required": [
          "kind",
          "text"
        ],
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/ToolPreviewLineKind"
          },
          "text": {
            "type": "string"
          }
        }
      },
      "ToolPreviewLineKind": {
        "type": "string",
        "description": "One line of a rendered diff.",
        "enum": [
          "context",
          "added",
          "removed"
        ]
      },
      "ToolRequest": {
        "type": "object",
        "required": [
          "id",
          "toolCall"
        ],
        "properties": {
          "_meta": {
            "type": "object"
          },
          "id": {
            "type": "string"
          },
          "metadata": {
            "type": "object"
          },
          "toolCall": {
            "type": "object"
          }
        }
      },
      "ToolResponse": {
        "type": "object",
        "required": [
          "id",
          "toolResult"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "metadata": {
            "type": "object"
          },
          "toolResult": {
            "type": "object"
          }
        }
      },
      "ToolRisk": {
        "type": "string",
        "description": "Per-action risk grade for a tool call.\n\nThe `Ord` derive is load-bearing: `Low < Medium < High < Unknown`, so an\nun-gradeable tool sorts above every graded one and a naive `>= threshold`\ncomparison already fails closed. [`SmartApproveConfig::requires_confirmation`]\nstill treats `Unknown` explicitly so it can be opted out of.\n\nBR-63 also ships the grade to the confirmation card, so it is a wire type:\nit serialises as `\"low\" | \"medium\" | \"high\" | \"unknown\"`.",
        "enum": [
          "low",
          "medium",
          "high",
          "unknown"
        ]
      },
      "TunnelInfo": {
        "type": "object",
        "required": [
          "state",
          "url",
          "hostname",
          "secret"
        ],
        "properties": {
          "hostname": {
            "type": "string"
          },
          "secret": {
            "type": "string"
          },
          "state": {
            "$ref": "#/components/schemas/TunnelState"
          },
          "url": {
            "type": "string"
          }
        }
      },
      "TunnelState": {
        "type": "string",
        "enum": [
          "idle",
          "starting",
          "running",
          "error",
          "disabled"
        ]
      },
      "TurnErrorScope": {
        "type": "string",
        "enum": [
          "provider",
          "session",
          "inference",
          "internal"
        ]
      },
      "UpdateCustomProviderRequest": {
        "type": "object",
        "required": [
          "engine",
          "display_name",
          "api_url",
          "api_key",
          "models"
        ],
        "properties": {
          "api_key": {
            "type": "string"
          },
          "api_url": {
            "type": "string"
          },
          "display_name": {
            "type": "string"
          },
          "engine": {
            "type": "string"
          },
          "headers": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "nullable": true
          },
          "models": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "supports_streaming": {
            "type": "boolean",
            "nullable": true
          }
        }
      },
      "UpdateFromSessionRequest": {
        "type": "object",
        "required": [
          "session_id"
        ],
        "properties": {
          "session_id": {
            "type": "string"
          }
        }
      },
      "UpdateProviderRequest": {
        "type": "object",
        "required": [
          "provider",
          "session_id"
        ],
        "properties": {
          "context_limit": {
            "type": "integer",
            "nullable": true,
            "minimum": 0
          },
          "model": {
            "type": "string",
            "nullable": true
          },
          "provider": {
            "type": "string"
          },
          "request_params": {
            "type": "object",
            "additionalProperties": {},
            "nullable": true
          },
          "session_id": {
            "type": "string"
          }
        }
      },
      "UpdateScheduleRequest": {
        "type": "object",
        "required": [
          "cron"
        ],
        "properties": {
          "cron": {
            "type": "string"
          }
        }
      },
      "UpdateSessionNameRequest": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Updated name for the session (max 200 characters)"
          }
        }
      },
      "UpdateSessionUserWorkflowValuesRequest": {
        "type": "object",
        "required": [
          "userWorkflowValues"
        ],
        "properties": {
          "userWorkflowValues": {
            "type": "object",
            "description": "Workflow parameter values entered by the user",
            "additionalProperties": {
              "type": "string"
            }
          }
        }
      },
      "UpdateSessionUserWorkflowValuesResponse": {
        "type": "object",
        "required": [
          "workflow"
        ],
        "properties": {
          "workflow": {
            "$ref": "#/components/schemas/Workflow"
          }
        }
      },
      "UpdateWorkingDirRequest": {
        "type": "object",
        "required": [
          "session_id",
          "working_dir"
        ],
        "properties": {
          "session_id": {
            "type": "string"
          },
          "working_dir": {
            "type": "string"
          }
        }
      },
      "UpsertConfigQuery": {
        "type": "object",
        "required": [
          "key",
          "value",
          "is_secret"
        ],
        "properties": {
          "confirm": {
            "type": "string",
            "description": "Issue #56 Task 30. The typed confirmation Settings → Privacy sends with a\nwrite to `BIOROUTER_PRIVACY_TIERS`, and nothing else sends at all.\n\n⚠ **What this is and what it is not.** It is a **UX guard against an\naccidental or model-composed config write**, not an authorization\nboundary: the phrase is a fixed string in the shipped source, so a caller\nholding the daemon secret replays it. That is acceptable because\n`check_token` has no principal — the daemon cannot tell Settings →\nPrivacy from any other loopback caller — and because the *authorization*\non this route is `X-User-Action`, not the phrase. What the phrase buys is\nthat the flip cannot be a side effect of an ordinary `/config/upsert`,\nwhich is the reachable path: a model *can* compose one of those through a\ntool.\n\n⚠ **This comment used to justify the phrase by adding *\"and a caller that\nalready holds the secret can raise its own session to private capability\nanyway\"*, citing AR-15. That is no longer true and is withdrawn** —\nAR-15 was retired on 2026-08-02 by DR-16 (commit `0757823f`), which made\nan upward provider bind require `X-User-Action`. Do not restore the\nargument: the guard does not need it, and a stale \"we are already open\nhere anyway\" is how a weakened control gets waved through.",
            "nullable": true
          },
          "is_secret": {
            "type": "boolean"
          },
          "key": {
            "type": "string"
          },
          "value": {}
        }
      },
      "UpsertPermissionsQuery": {
        "type": "object",
        "required": [
          "tool_permissions"
        ],
        "properties": {
          "tool_permissions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ToolPermission"
            }
          }
        }
      },
      "UsageGroup": {
        "type": "string",
        "description": "How `get_usage_report` buckets the per-turn ledger.",
        "enum": [
          "day",
          "model",
          "day_model"
        ]
      },
      "UsageReportResponse": {
        "type": "object",
        "required": [
          "from",
          "to",
          "group",
          "rows"
        ],
        "properties": {
          "from": {
            "type": "integer",
            "format": "int64",
            "description": "Echoed resolved window bounds (unix seconds), so the client can render\nthe exact range it got even when it sent no `from`/`to`."
          },
          "group": {
            "$ref": "#/components/schemas/UsageGroup"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/UsageReportRow"
            }
          },
          "to": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "UsageReportRow": {
        "type": "object",
        "description": "One bucket of the usage report.\n\n`date` is present unless grouping by [`UsageGroup::Model`]; `modelId` is\npresent unless grouping by [`UsageGroup::Day`]. `cost` is the dollar cost of\nthe priced turns in the bucket, or `None` when *every* contributing turn was\nunpriced (an unknown model) — a `null` cost never means \"$0\". `hasUnpriced`\nflags a bucket that mixes priced, unpriced, or incomplete turns, so a day\ncost can be read as \"at least this much\" rather than exact.",
        "required": [
          "inputTokens",
          "outputTokens",
          "turns",
          "hasUnpriced",
          "costExcludesCache"
        ],
        "properties": {
          "cacheCreationTokens": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "cacheReadTokens": {
            "type": "integer",
            "format": "int64",
            "description": "Prompt-cache read/creation tokens in the bucket. `None` preserves\nhistorical incompleteness; it must not be presented as a measured zero.",
            "nullable": true
          },
          "cost": {
            "type": "number",
            "format": "double",
            "nullable": true
          },
          "costExcludesCache": {
            "type": "boolean",
            "description": "`true` when cache cost is omitted because a contributing model has no\ncache rate or an event did not report a required cache bucket."
          },
          "date": {
            "type": "string",
            "nullable": true
          },
          "hasUnpriced": {
            "type": "boolean"
          },
          "inputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "modelId": {
            "type": "string",
            "nullable": true
          },
          "outputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "provider": {
            "type": "string",
            "nullable": true
          },
          "totalTokens": {
            "type": "integer",
            "format": "int64",
            "description": "Billed tokens, or `None` when the bucket includes incomplete history.",
            "nullable": true
          },
          "turns": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "UsageSummary": {
        "type": "object",
        "description": "Month-to-date and all-time usage totals, for the summary gauge.",
        "required": [
          "month",
          "monthToDate",
          "allTime"
        ],
        "properties": {
          "allTime": {
            "$ref": "#/components/schemas/UsageTotals"
          },
          "month": {
            "type": "string",
            "description": "Current local month, `YYYY-MM`."
          },
          "monthToDate": {
            "$ref": "#/components/schemas/UsageTotals"
          }
        }
      },
      "UsageSummaryResponse": {
        "allOf": [
          {
            "$ref": "#/components/schemas/UsageSummary"
          },
          {
            "type": "object",
            "properties": {
              "dollarPercent": {
                "type": "number",
                "format": "double",
                "description": "MTD cost as a percent of the dollar limit; `null` when no limit is set or\nthe MTD cost is unknown or only a lower bound.",
                "nullable": true
              },
              "monthlyDollarLimit": {
                "type": "number",
                "format": "double",
                "description": "Configured monthly dollar budget, or `null` when unset.",
                "nullable": true
              },
              "monthlyTokenLimit": {
                "type": "integer",
                "format": "int64",
                "description": "Configured monthly token allowance, or `null` when unset.",
                "nullable": true
              },
              "tokenPercent": {
                "type": "number",
                "format": "double",
                "description": "MTD billed tokens as a percent of the token limit; `null` when no limit\nis set or the historical billed total is incomplete.",
                "nullable": true
              }
            }
          }
        ]
      },
      "UsageTotals": {
        "type": "object",
        "description": "Token + cost totals for a time span, priced through [`model_cost_with_cache`].",
        "required": [
          "inputTokens",
          "outputTokens",
          "turns",
          "hasUnpriced",
          "costExcludesCache"
        ],
        "properties": {
          "cacheCreationTokens": {
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "cacheReadTokens": {
            "type": "integer",
            "format": "int64",
            "description": "Prompt-cache read/creation tokens in the span. `None` means the span\nincludes at least one event without cache-bucket accounting.",
            "nullable": true
          },
          "cost": {
            "type": "number",
            "format": "double",
            "description": "Dollar cost of the priced turns, or `None` when nothing in the span is\npriced. Priced-but-partial spans return the priced sum with\n`has_unpriced = true`.",
            "nullable": true
          },
          "costExcludesCache": {
            "type": "boolean",
            "description": "`true` when cache cost is omitted because pricing or cache accounting is\nincomplete — the figure is then a lower bound."
          },
          "hasUnpriced": {
            "type": "boolean"
          },
          "inputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "outputTokens": {
            "type": "integer",
            "format": "int64"
          },
          "totalTokens": {
            "type": "integer",
            "format": "int64",
            "description": "Billed tokens, or `None` when the span includes incomplete history.",
            "nullable": true
          },
          "turns": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "Workflow": {
        "type": "object",
        "required": [
          "title",
          "description"
        ],
        "properties": {
          "activities": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          },
          "author": {
            "allOf": [
              {
                "$ref": "#/components/schemas/Author"
              }
            ],
            "nullable": true
          },
          "description": {
            "type": "string"
          },
          "extensions": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExtensionConfig"
            },
            "nullable": true
          },
          "instructions": {
            "type": "string",
            "nullable": true
          },
          "knowledge_bases": {
            "allOf": [
              {
                "$ref": "#/components/schemas/WorkflowKnowledgeBases"
              }
            ],
            "nullable": true
          },
          "parameters": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WorkflowParameter"
            },
            "nullable": true
          },
          "prompt": {
            "type": "string",
            "nullable": true
          },
          "response": {
            "allOf": [
              {
                "$ref": "#/components/schemas/Response"
              }
            ],
            "nullable": true
          },
          "retry": {
            "allOf": [
              {
                "$ref": "#/components/schemas/RetryConfig"
              }
            ],
            "nullable": true
          },
          "settings": {
            "allOf": [
              {
                "$ref": "#/components/schemas/Settings"
              }
            ],
            "nullable": true
          },
          "skills": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          },
          "sub_workflows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SubWorkflow"
            },
            "nullable": true
          },
          "title": {
            "type": "string"
          },
          "version": {
            "type": "string"
          }
        }
      },
      "WorkflowKnowledgeBases": {
        "type": "object",
        "properties": {
          "default": {
            "type": "string",
            "nullable": true
          },
          "visible": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "WorkflowManifest": {
        "type": "object",
        "description": "One workflow as every surface sees it: the parsed document, where it came\nfrom, and the stable id the interfaces address it by.",
        "required": [
          "id",
          "workflow",
          "file_path",
          "last_modified"
        ],
        "properties": {
          "file_path": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "last_modified": {
            "type": "string"
          },
          "schedule_cron": {
            "type": "string",
            "nullable": true
          },
          "slash_command": {
            "type": "string",
            "nullable": true
          },
          "workflow": {
            "$ref": "#/components/schemas/Workflow"
          }
        }
      },
      "WorkflowParameter": {
        "type": "object",
        "required": [
          "key",
          "input_type",
          "requirement",
          "description"
        ],
        "properties": {
          "default": {
            "type": "string",
            "nullable": true
          },
          "description": {
            "type": "string"
          },
          "input_type": {
            "$ref": "#/components/schemas/WorkflowParameterInputType"
          },
          "key": {
            "type": "string"
          },
          "options": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          },
          "requirement": {
            "$ref": "#/components/schemas/WorkflowParameterRequirement"
          }
        }
      },
      "WorkflowParameterInputType": {
        "type": "string",
        "enum": [
          "string",
          "number",
          "boolean",
          "date",
          "file",
          "select"
        ]
      },
      "WorkflowParameterRequirement": {
        "type": "string",
        "enum": [
          "required",
          "optional",
          "user_prompt"
        ]
      },
      "WorkflowToYamlRequest": {
        "type": "object",
        "required": [
          "workflow"
        ],
        "properties": {
          "workflow": {
            "$ref": "#/components/schemas/Workflow"
          }
        }
      },
      "WorkflowToYamlResponse": {
        "type": "object",
        "required": [
          "yaml"
        ],
        "properties": {
          "yaml": {
            "type": "string"
          }
        }
      },
      "WritePageBody": {
        "type": "object",
        "required": [
          "content",
          "commit_message"
        ],
        "properties": {
          "commit_message": {
            "type": "string"
          },
          "content": {
            "type": "string"
          }
        }
      }
    },
    "securitySchemes": {
      "api_key": {
        "type": "apiKey",
        "in": "header",
        "name": "X-Secret-Key",
        "description": "The daemon's secret key. Biorouter's server is loopback-only and authenticates every route with this header except two: /status, and /tool_bridge/{nonce}, which is not part of this API at all. The bridge is an MCP endpoint a coding-agent child process calls, its path carries a single-turn capability nonce instead of a header (Codex sends no Authorization header), and it is deliberately absent from this spec because no Biorouter client should ever call it."
      }
    }
  }
}
