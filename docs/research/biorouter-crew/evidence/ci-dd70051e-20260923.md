# Hosted CI extraction: Rust and frontend

Read-only extraction from GitHub Actions, repository `BaranziniLab/biorouter`.
No tests or builds were rerun locally.

## Rust run

- Run: [35818114269](https://github.com/BaranziniLab/biorouter/actions/runs/35818114269)
- Commit: `dd70051e90d4935fcc908cc28dbdb144f9e79ce5`
- Overall: `completed / success`
- Required Rust jobs: Windows, Ubuntu, macOS test jobs all passed; `serve`,
  `guards`, and both cross-check jobs passed. `cross-build-nightly` was skipped.

The following are per-target results from the three `cargo test --workspace
--lib --bins` jobs. Counts are kept per target and per OS; they are not summed
because the targets overlap across the workspace and platforms.

| Target | Windows | Ubuntu | macOS |
|---|---:|---:|---:|
| `biorouter` lib | 4140 passed, 1 ignored | 4246 passed, 2 ignored | 4256 passed, 2 ignored |
| `biorouter-acp` lib | 17 passed | 17 passed | 17 passed |
| `biorouter-authprompt` lib | 0 | 0 | 1 passed |
| `biorouter-cli` lib | 508 passed | 532 passed | 532 passed |
| `biorouter-mcp` lib | 1644 passed, 3 ignored | 1692 passed, 6 ignored | 1692 passed, 6 ignored |
| `biorouter-sandbox` lib | 17 passed | 21 passed | 21 passed |
| `biorouter-server` lib | 733 passed | 758 passed | 758 passed |
| `biorouterd` bin | 713 passed | 729 passed | 729 passed |

The Windows SSH-policy regressions that had failed in the earlier run now pass:
`native_preflight_accepts_safe_two_hop_config`,
`native_preflight_rejects_weak_implicit_jump_before_connecting`, and
`native_preflight_rejects_jump_cycle`. The new
`shell_config_path_normalizes_verbatim_drive_fixture` test also passes.

The three former Ubuntu integration-fixture failures are green in the finalized
integration run: portable FIFO handling is covered by
`daemon_runtime_contract` (7 passed), the child-command preparation is covered
by `no_console_window_census` (20 passed), and the existing sandbox declaration
is covered by `every_test_binary_is_sandboxed` (5 passed; the assertion
`every_integration_test_binary_declares_the_sandbox` passed). The separate
`crew_transfer_authority` binary also passed its 10 tests. These counts are
from separate integration binaries and are not added to the core target table.

## Frontend run

- Run: [35818114160](https://github.com/BaranziniLab/biorouter/actions/runs/35818114160)
- Commit: `dd70051e90d4935fcc908cc28dbdb144f9e79ce5`
- `Unit tests (vitest)`: 563 test files passed; 6370 tests passed; 19 skipped.
- Other frontend jobs passed: Preview panel, Generated API contract, Static
  checks, Registry generator, Console windows (Windows), and BAAM shelf.

## PR #366 required-check rollup

`gh pr checks 366` reports pass for the Rust test jobs on Windows, Ubuntu, and
macOS; serve; both cross-checks; guards; all frontend jobs above; native builds
for darwin-arm64, darwin-x64, linux-arm64, linux-x64, and win32-x64;
native-release-gate; reference-apps; and no-ai-coauthor. The only skipped check
is the explicitly optional `cross-build-nightly` job.
