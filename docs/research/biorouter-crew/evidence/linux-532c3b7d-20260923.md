# Linux ARM64 validation report: source `532c3b7d`

- Source archive: `/private/tmp/biorouter-crew-source-532c3b7d/source.tar`
- Archive SHA-256: `b120e9b5e3f9b17970d0cb398d557c25e69d3d179959ab93a20dc9624fb592a9`
- Extracted source: `/private/tmp/biorouter-crew-linux-532c3b7d/source`
- Root `Cargo.lock` SHA-256: `60da88634d074791ee19ad2bd1c9000054166d8e391c4c027151982d4976d03e`
- Build image: local pinned `rust@sha256:bf5a9aa29062a6cb03c49bd59a46eb55e3cc770caf598a221a7866e500be3082`
- Warm target cache: `/private/tmp/biorouter-crew-linux-5455ebf9/target`
- Warm Cargo home: `/private/tmp/biorouter-crew-linux-5455ebf9/cargo-home`

The extracted snapshot contains the continuation recovery tests in
`crates/biorouter-cli/src/commands/shared_conversation.rs`, daemon-client
protocol/observer tests in `crates/biorouter-cli/src/daemon_client.rs`, and
observer stream tests in `crates/biorouter-server/src/routes/crew_observation.rs`.

Focused filters executed before the final ordinary non-test build:

```text
cargo test --locked -p biorouter-cli --lib commands::shared_conversation::tests -- --nocapture --test-threads=1
cargo test --locked -p biorouter-cli --lib daemon_client::tests -- --nocapture --test-threads=1
cargo test --locked -p biorouter-server --lib routes::crew_observation::tests -- --nocapture --test-threads=1
```

Each command used the exact extracted source in a read-only Docker mount,
`CARGO_TARGET_DIR=/cargo-target` backed by the warmed
`/private/tmp/biorouter-crew-linux-5455ebf9/target`,
`CARGO_HOME=/cargo-home` backed by the warmed Cargo home,
`CARGO_BUILD_JOBS=2`, and `CARGO_INCREMENTAL=0`.

The final ordinary non-test build command was:

```text
cargo build --locked -p biorouter-cli --bin biorouter -p biorouter-server --bin biorouterd --target aarch64-unknown-linux-gnu
```

It used the same pinned image, read-only source mount, warmed target/Cargo home,
two build jobs, and incremental compilation disabled. Existing 5455 artifacts
and all other containers remained untouched.

## Completed validation

Focused filters passed before the build:

- `commands::shared_conversation::tests`: 11 passed, 0 failed, 518 filtered.
- `daemon_client::tests`: 16 passed, 0 failed, 513 filtered.
- `routes::crew_observation::tests`: 12 passed, 0 failed, 746 filtered.

The ordinary non-test ARM64 `dev` build completed in 6m43s. Mode-0555 copies
are in `/private/tmp/biorouter-crew-linux-532c3b7d/artifacts/` and match the
just-built outputs:

| Artifact | SHA-256 | Format | GLIBC ceiling |
|---|---|---|---|
| `biorouter` | `35c52efd390166f59a48325d93211b6580feaf14efb9d129e162c0a900af2d73` | ELF 64-bit LSB PIE, ARM aarch64, mode 0555 | `GLIBC_2.39` |
| `biorouterd` | `81b7a2e3e4f62161490f04535bf11308c3c03aa96b305f27b2cf35d8a9ce8318` | ELF 64-bit LSB PIE, ARM aarch64, mode 0555 | `GLIBC_2.39` |

In a read-only pinned-image container with isolated tmpfs home, the CLI printed
`1.91.1`, the daemon printed `biorouter-server 1.91.1`, and `crew --help`
exited 0 with 2127 bytes.

The exact copied pair passed a fresh UID 1101 tmpfs-profile lifecycle. Start and
status passed; a valid-length incorrect approval proof returned daemon HTTP 403;
stop passed; restart preserved the profile ID and changed the instance ID; the
final stop passed; and post-stop status returned the expected missing-daemon
refusal. Secrets were synthetic and were not included in evidence.

Scope: local Docker on ARM64 with GLIBC 2.39. Institutional deployment, other architectures and the AWS product workflow require separate qualification.
