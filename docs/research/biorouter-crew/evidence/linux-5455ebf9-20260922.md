# Linux ARM64 validation of published 5455ebf9

Date: 2026-09-22

Source and lock provenance:

- Worktree HEAD: `5455ebf90f8cd9d6e837d296820f6303a6a7cb66`.
- Source archive: `/private/tmp/biorouter-crew-source-5455ebf9/source.tar`, SHA-256 `ce3a9aae72c1cce33924759b2e51bdff2abf4a7f28a08d8c71e8dda658600501`.
- `Cargo.lock` SHA-256: `60da88634d074791ee19ad2bd1c9000054166d8e391c4c027151982d4976d03e`.
- Build image: local `rust@sha256:bf5a9aa29062a6cb03c49bd59a46eb55e3cc770caf598a221a7866e500be3082`, ARM64, toolchain `1.98.1-aarch64-unknown-linux-gnu`.

## Production debug build

Read-only source was mounted into a disposable local Docker container. The command was:

```text
cargo build --locked -p biorouter-cli --bin biorouter -p biorouter-server --bin biorouterd --target aarch64-unknown-linux-gnu
```

Environment used `CARGO_BUILD_JOBS=2`, `CARGO_INCREMENTAL=0`, a dedicated Cargo target and home, and the pinned image above. The non-test `dev` build passed in 9m25s.

The immediately copied artifacts are in `/private/tmp/biorouter-crew-linux-5455ebf9/artifacts/`:

| Artifact | SHA-256 | Format | GLIBC ceiling |
|---|---|---|---|
| `biorouter` | `63d21cdd67b8297e8455f51aa3412d27cf982a7a5903a08897dd4eb61d0f5050` | ELF 64-bit LSB PIE, ARM aarch64, mode 755 | `GLIBC_2.39` |
| `biorouterd` | `f3107571d2379c9e5fafcb8b2533bdb4d62b92144b10269d834a511e646e4c96` | ELF 64-bit LSB PIE, ARM aarch64, mode 755 | `GLIBC_2.39` |

The copies were hashed against the just-built target outputs and matched byte-for-byte. In a read-only pinned-image container with an isolated tmpfs home, the CLI printed `1.91.1`, the daemon printed `biorouter-server 1.91.1`, and `biorouter crew --help` exited 0 with 2127 bytes of help output.

## Focused regression

The Linux server unit filter `ordinary_authentication_never_grants_computer_use` selected 1 test and passed (`1 passed, 0 failed, 751 filtered`). An initial exact-name invocation selected zero tests because the full module-qualified name was required; it was not counted as a pass. The follow-on redundant test rebuild was stopped before result and does not affect the production artifacts.

## Ordinary UID lifecycle

The exact copied pair was mounted read-only into a fresh ARM64 Docker container as UID 1101 with a new tmpfs profile and synthetic in-memory secrets. No retained profile, broker fixture, AWS resource, credential, or other container was accessed.

- Start and status passed; `user_action_installed` was true.
- A valid-length but incorrect approval proof on `credentials init` returned daemon HTTP 403 and exited 1.
- Stop passed.
- Restart passed with the same profile ID and a different instance ID.
- Final stop passed; post-stop status returned the expected missing-daemon refusal.

This is headless Linux CLI/daemon evidence only. No GUI, Windows, AWS, or external source transfer was used.

After validation, the preserved artifact copies were made read-only (mode 0555).
Both SHA-256 values remained unchanged. This evidence belongs to the published
revision above, before the subsequent main-branch integration and terminal
continuation-recovery change.
