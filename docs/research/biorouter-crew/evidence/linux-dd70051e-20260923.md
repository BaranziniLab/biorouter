# Linux ARM64 non-test artifact refresh: `dd70051e`

Validated source commit `dd70051e90d4935fcc908cc28dbdb144f9e79ce5` from a fresh
local Git archive. No AWS, UI, external source transfer, or repository edits were
used.

## Provenance and build

- Source archive: `/private/tmp/biorouter-crew-source-dd70051e/source.tar`
- Archive SHA-256: `de17292f9245fab82734a0b4a7292d57394e60495c583d268c067b4c17d0fbf1`
- Extracted source: `/private/tmp/biorouter-crew-linux-dd70051e/source`
- Root `Cargo.lock` SHA-256: `60da88634d074791ee19ad2bd1c9000054166d8e391c4c027151982d4976d03e`
- Docker image: local `rust@sha256:bf5a9aa29062a6cb03c49bd59a46eb55e3cc770caf598a221a7866e500be3082`
- Toolchain: `1.98.1-aarch64-unknown-linux-gnu`
- Warm target: `/private/tmp/biorouter-crew-linux-5455ebf9/target`
- Warm Cargo home: `/private/tmp/biorouter-crew-linux-5455ebf9/cargo-home`

The source was mounted read-only. The bounded build used
`CARGO_BUILD_JOBS=2`, `CARGO_INCREMENTAL=0`, `--locked`, and completed the
ordinary non-test `dev` profile in 4m57s:

```text
cargo build --locked -p biorouter-cli --bin biorouter -p biorouter-server --bin biorouterd --target aarch64-unknown-linux-gnu
```

## Immutable artifacts

Mode-0555 copies are in `/private/tmp/biorouter-crew-linux-dd70051e/artifacts/`.
Each copy was hashed against the just-built target output before validation.

| Artifact | SHA-256 | Format | Highest GLIBC |
|---|---|---|---|
| `biorouter` | `381eb225ab82015e85d1552f9629f887f206553e3fe017dd8e8c8cc85bcc1f67` | ELF 64-bit LSB PIE, ARM aarch64, mode 0555 | `GLIBC_2.39` |
| `biorouterd` | `81b7a2e3e4f62161490f04535bf11308c3c03aa96b305f27b2cf35d8a9ce8318` | ELF 64-bit LSB PIE, ARM aarch64, mode 0555 | `GLIBC_2.39` |

In a read-only pinned-image container with isolated tmpfs home, the CLI printed
`1.91.1`, the daemon printed `biorouter-server 1.91.1`, and `biorouter crew
--help` exited 0 with 2127 bytes.

## Ordinary-user lifecycle

The exact copied pair was mounted read-only into a fresh ARM64 Docker container
as UID 1101 with a new tmpfs profile and synthetic in-memory secrets.

- Start and status passed with `user_action_installed: true`.
- A valid-length incorrect approval proof on `credentials init` returned daemon HTTP 403 and exited 1.
- Stop passed.
- Restart preserved the profile ID and changed the instance ID.
- Final stop passed; post-stop status returned the expected missing-daemon refusal.

No prior artifact, profile, broker fixture, or other container was accessed.
This is headless Linux CLI/daemon evidence only and does not establish GUI,
Windows, AWS, or institutional deployment compatibility.
