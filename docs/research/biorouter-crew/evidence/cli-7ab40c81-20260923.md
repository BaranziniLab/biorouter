# Native CLI verification on 7ab40c81

The ordinary non-test debug build passed at source
`7ab40c817436a01df355336a264c17e5b8d5a5e7` using the established Hermit
environment, two Cargo jobs and incremental compilation disabled:

```text
cargo build -p biorouter-cli --bin biorouter -p biorouter-server --bin biorouterd
```

Mode-0555 copies in `/private/tmp/biorouter-crew-artifacts-7ab40c81/` are
ARM64 Mach-O binaries. The CLI reports `1.91.1`; the daemon reports
`biorouter-server 1.91.1`.

| Binary | SHA-256 |
|---|---|
| biorouter | `ffa2f599621159be35a9efbf9c6f74c0ce5f3497d72da41d0d6bcece29c09624` |
| biorouterd | `4a1082ec9e1e0cc0b464c8cd608156a21cd24544c27cd87a2d42e7f84b16185c` |

The daemon is byte-identical to the recorded 532c3b7d daemon. The CLI has the
shared invisible-formatting classification and correct JSON surrogate encoding.

Luna stopped and restarted only Bob's owned synthetic profile through supported
CLI lifecycle commands. Interactive SSH authentication succeeded with
`authenticated=true` and `exit_code=0`; the vault was unlocked through the
supported flow. This is not a new institutional or per-hop MFA qualification.

Message `6e9dce5f-ac85-4258-9b0d-a824b7def609` round-tripped through send and
history with exact codepoints, including BOM, bidi controls, zero-width space
and ordinary non-BMP emoji. A second message,
`de58d73f-e357-4e80-9fcb-376a33b202fb`, included the invisible tag U+E0041.
Its raw JSON contained the valid surrogate pair `\udb40\udc41`, with no raw
tag character. Decoding restored the exact original body. Text output rendered
the tag as `\u{e0041}`, preserved the ordinary emoji and contained no raw tag,
bidi or zero-width controls.

This verifies the changed native CLI paths on one owned connection. Earlier
three-user, model, attachment and observer evidence retains its recorded scope.
GUI, AWS and native Windows runtime acceptance remain separate gates.

## Initial history and source-revocation terminal behavior

Using the same CLI/daemon pair and the existing synthetic fixture, Bob was
re-invited into source A while retaining destination B. His authorized B history
positively contained derived message `b31210b8-aecc-4cbb-82d1-1d98f02f4474`
with A+B provenance. `crew watch` without `--after` uses the explicit
`Initial::All` contract and starts with earliest authorized history; no fabricated
cursor or provider reclassification was required.

After a two-second reader pause, Alice revoked only Bob's A membership and
successfully posted two new B-only controls. The reader then drained for eight
seconds. It received the derived message once and previously authorized history,
zero new post-revocation controls, then the exact daemon terminal frame:

```json
{"type":"error","code":"policy_changed","error":"Room observation ended. Clear cached room content and refresh authorized access; a stale cursor requires an explicit fresh history selection.","clear":true}
```

The CLI emitted its error object with the same code and request ID
`68309a49-be07-41cb-bad4-247fccfd8420`, then exited with status 1. The capture
contains 11 stream lines. Bob's A membership was restored through an ordinary
invitation and acceptance afterward.

This establishes bounded initial-history delivery and explicit terminal
invalidation after source revocation. The derived/history frames had already
reached the CLI pipe while authorized; this does not prove behavior with a
derived frame still in the daemon queue at revocation, or retroactive erasure
of delivered bytes. The complete queued-derived race matrix remains open.

## Stable mutation identity

Alice sent a small synthetic message with request ID
`luna-7ab-idem-20260923-03`. The first send and identical retry returned the
same message receipt, `5dae549b-2ce4-49a4-9f7a-a513da1f1ac8`. Bob's independent
history contained exactly one matching message. Reusing the request ID with a
different body exited 1 with `conflict: idempotency key reused with different
request` and added no second message. A preliminary Alice discovery failure
preceded the successful run; her owned daemon was restarted through supported
lifecycle and vault/authentication flows.

A separate attempted reference mutation on Bob's profile stopped at daemon
discovery and did not publish anything. Independent source/fixture inspection
established a wrong-case `BKx8iO` root instead of `bKx8iO` in the harness.
Both spellings identify the same directory on the case-insensitive filesystem,
so its `-d` fallback never ran. Configuration identity canonicalizes its path,
but runtime discovery correctly rejects the differing literal socket path.
No production gate or descriptor was changed. The attempted `--reference`
argument also used a message ID, whereas that flag accepts remote file-reference
IDs. That failed attempt is not reference-semantic evidence.

After a later explicit authentication/reconnection, retained typed outputs show
Bob connected in Private mode, a Bob workspace snapshot, and remote-file
reference `24156095-3050-4279-ad2b-298a626e165f` with `verified=false`.
The successful send receipt is message
`242b0c50-8235-4636-840f-2b7d4f9c0840`, body `remote-reference-send-7ab`, with
exactly that reference. Alice's separately captured history contains exactly
one matching message, body and reference. An independent Astra inspection
corroborated those selected raw fields; `verified=false` does not prove remote
file contents or download integrity.

The inline invocation scripts were not retained, and the prose report repeated
the wrong-case profile spelling. These receipts establish the stated subset,
not independent provenance for every exact CLI invocation or complete artifact/
interface parity. Bob's later history attempt again failed discovery and is
excluded. The successful receipt file is `file-send-7ab.json`; the similarly
named `reference-send-7ab.json` records an earlier refusal and is not positive
evidence.
