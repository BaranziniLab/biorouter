# Crew UI acceptance evidence

Publication scope: all screenshots named in this report are retained only in the local visual-evidence backup and are excluded from the published PR. The backup ref must never be published. Historical observation scopes below are unchanged; screenshot filenames identify local evidence, not published attachments.

This report records native Electron UI observations from the three-profile Crew fixture, grouped by the artifact used. Historical failures and later subset passes are retained separately; unfinished gates remain open.

The latest client refresh uses source `aac5f4ac` and daemon SHA-256 `1ce50cb31f63dca70c7bb25c571facd1672fe9271801ddbf5e82f5785483e407`, with broker `7311f126…`. All three clients reconnect with verified identities. The latest clean processing run issued the correct helper argv but failed while opening its Python script inside the approved work directory; a narrow seccomp fix is under validation. Personal MCP and selected-channel retrieval remain incomplete. Earlier human/file/PAM and model observations below retain their own artifact provenance. The separate API CSV positive and matched backend traces are in [the provider report](local-provider-boundary-report.md).

## Passing human workflow evidence

- Alice, Bob, and Carol were simultaneously retained as separate Electron clients using isolated profiles and the then-current daemon `8f0113378714acb7308ce36fd506e145ac26009ed454aae31575e4d8530abed9`.
- All three clients showed `Connected · identity verified` and displayed the same `Synthetic Lab` team and `#general` channel.
- Human messages were visible in `#general`: Alice `Restricted Synthetic Alice message for Crew QA`, Bob `Synthetic Bob human message: hello Alice and Carol`, and Carol `Synthetic Carol human message: hello Alice and Bob`.
- Carol uploaded and shared `crew-cua-fixture.txt`; the UI displayed SHA-256 `709dc5dcf994ac4633ed43861fd8ab87df3ab5abbe67f668b17f9ca524056c42` and `66 bytes`. The downloaded-byte hash matched. This is byte-integrity evidence; this report does not establish a completed user-named native Save.
- Carol created restricted `#small-luna`, invited Bob, and Bob accepted. Alice's channel list did not show it. Ownership transfer to Bob was accepted; Bob then saw one member and Carol's channel list cleared it.

## Agent and normal-chat observations

Carol's Settings UI configured Ollama at `http://127.0.0.1:11434`, selected private `qwen3:1.7b`, and showed the model in the Ask my agent dialog. The connection Edit UI saved remote root `/home/carol/work/crew-fixture` and showed execution allowed within that folder.

An SSH fixture setup created `/home/carol/work/crew-fixture/crew-task.csv` with three rows (`A,10`, `B,20`, `C,30`), 31 bytes, SHA-256 `b8d8853f57f79b6bc9e4c3736b9f7c840e0975ea0606d31cc934349d5606335a`. A fresh native Ask my agent task used relative path `crew-task.csv` and visibly emitted `Using crew__request` and `Requested remote.read`. The native session's View tool details showed `connection_id 55441429-e81c-4991-b1d4-283961df5e18`, method `remote.read`, and path `/tmp/biorouter-crew-dev-carol-clean/biorouter/data/crew/tasks/crew-task.csv`; the exact tool error was `Connection is outside the approved run scope`. The model then emitted a 10-row/42 summary after the refusal; that text is untrusted and is not an actual tool result. This is an agent guidance/invocation failure: the tool refused before file access, so no routing or CSV-read pass is claimed.

Typing `/crew Please check the latest updates...` into the native `#general` message composer posted the literal text as a human message and emitted no MCP timeline; that composer is not the personal-chat path. In the personal chat composer, the same natural-language request invoked an actual workspace-list tool and the UI showed `Tool status: success Ran Listing Workspace List · Limit: 50 · 1 result ready`, but this is not Crew MCP acceptance evidence. The correct `/crew` grant flow was then exercised: entering `/crew` alone opened Crew, `Connect your current agent conversation to #general`, and `Review access and posting permission`; the dialog showed `/home/carol/work/crew-fixture` and single-process permission, and `Allow this conversation to read and post here` was clicked. A subsequent natural-language CSV task produced an actual tool status error with `Connection ID: 55441429-e81c-4991-b1d4-283961df5e18`, method `remote.read`, and exact refusal `Connection is outside the approved run scope`. This is a real post-grant refusal before file access, pending the fresh daemon/context fix; no CSV result was claimed.

## Environment evidence

Earlier direct Electron launches aborted under the restricted command sandbox's AppKit mach lookup. Scoped GUI-permission launches succeeded with disposable Electron bundle copies, so the earlier failure is labeled test-environment launch restriction rather than a product failure.

For the next fresh conversation after the daemon/context fix, the local Ollama inventory includes `qwen3:8b` (ID `500a1f067a9f`, 5.2 GB) and `qwen3:1.7b` (ID `8f68893c685c`, 1.4 GB). The stronger model was pulled for synthetic-only acceptance; no external credentials or PHI are used.

## Separate PAM fixture

A separate disposable Docker fixture (`127.0.0.1:56929`, user `mfa`) was configured with `linux_pam_fixture.sh`, strict pinned host keys, and an isolated Carol profile SSH config alias `pam-luna`. Shell validation passed encrypted-key plus PAM authentication (`id -u` returned `1000`), wrong-secret rejection, and prompt cancellation without remote command output.

The earlier native Crew Authenticate UI saved a separate `carol-pam-plain` connection and visibly reached `(mfa@127.0.0.1) Password:`. Closing the authentication pane cancelled cleanly. After password submission, the UI exposed the exact stderr `unix_listener: path "/tmp/biorouter-crew-dev-carol-clean/temp/brcrew-3545e6956575/5fc0aeb1-4b33-4683-a34f-b8c57f190f95.NpIScSXNzamu9FyQ" too long for Unix domain socket`, followed by `SSH authentication ended (exit 255)`. The disposable container's SSH log recorded accepted public-key plus accepted keyboard-interactive/PAM authentication before disconnect. The same profile-configured OpenSSH command succeeds in the shell and returns UID 1000. This historical run proved native prompt/cancel and server-side PAM acceptance but failed the app handoff; the fixed-path authentication replay is recorded next.

Fresh foreground validation against daemon `1ffa69d980d4664b452b2c492100af96fd4055ba3d0e875f076b331fc83b8d86` used the correctly addressed Carol bundle and CDP 9413. The rendered Crew UI selected saved `carol-pam-plain`, reached the native `(mfa@127.0.0.1) Password:` prompt, accepted the synthetic PAM password, and showed `Authentication complete · connect`; this is an authentication-only positive because the saved descriptor still targets the main fixture and was not used to claim a full broker connection. Sanitized screenshots are retained under `docs/research/biorouter-crew/evidence/` (`carol-fresh-ui.png`, `carol-crew-home.png`, `carol-fresh-pam-prompt.png`, `carol-fresh-pam-after-password.png`).

The same foreground UI then clicked `Close authentication connection`; the terminal pane closed and Crew returned to the disconnected state with its Authenticate action. This cancellation result is retained as `carol-pam-cancelled.png`.

After the disposable rootless broker was refreshed to Linux artifact `7311f126d0e77f6b7e6f4b75e0ac32b099ff38b4e7757ca04e92737e9ffbc759` on the preserved workspace and socket, all three retained renderers reconnected through their actual Crew pages. Alice (`local-alice`), Bob (`local-bob-clean`), and Carol (`local-carol-clean`) each showed `Connected · identity verified`, `Synthetic Lab`, `#general`, and three members. Each sent a unique synthetic human marker at 07:33:15–16, visible in the shared channel. Carol then selected `/private/tmp/biorouter-crew-ui-attachments/synthetic-crew.png` in the native file picker. The UI reported `Upload verified`, displayed `synthetic-crew.png · 68 bytes`, `Preview image`, and `SHA-256 verified`; the source SHA-256 was `431ced6916a2a21a156e38701afe55bbd7f88969fbbfc56d7fe099d47f265460`. The native download control was invoked, but no deterministic browser download event/path was exposed by the Electron renderer, so no downloaded-file hash is claimed. Connected and PNG upload/preview screenshots are retained alongside the PAM images under `docs/research/biorouter-crew/evidence/`.

A larger synthetic RGB PNG (`320×180`, 1,619 bytes, SHA-256 `e744b1f1a930305ed1624cfe7d6337c5ec3ff6ed758f06a4a04867b5e28c9026`) was then uploaded and previewed in the actual renderer. The DOM image reported natural dimensions `320×180`; native download handling wrote a temporary file under `/Users/wgu/Downloads/` whose SHA-256 matched the source. An opaque 257-byte binary (`dd31e55fd954d9edc9e29f8e078387f0dd38e8ea832376311820de6322cdb0d1`) was also uploaded and its Download control invoked; the staged native temporary file hash matched exactly. The binary card correctly exposed Download without a preview control.

These later PNG/binary checks establish upload, preview where supported, shared file-card visibility and downloaded-byte integrity. They do not establish a completed user-named native Save: only hidden `.com.biorouter.luna.carol.*` staging files were found. The exact supported native selectors did not expose a positively identified Save dialog; a separate bounded Luna inspection likewise produced no usable binding. No unrelated window was operated. Copies of staged bytes are test evidence, not completed product saves. All three renderers independently showed all three fresh human markers and both new file cards.

## Client refresh at `367fe588`

This foreground relaunch used the copied bundles `BiorouterAliceElectron.app`, `BiorouterBobElectron.app`, and `BiorouterCarolElectron.app`, with CDP ports 9411, 9412, and 9413 and isolated profile roots. Retained foreground exec sessions were Alice 39891, Bob 57336, and Carol 19782. Each renderer loaded `http://localhost:5174/#/?` with title `Biorouter`; each daemon log resolved `/Users/wgu/.codex/worktrees/biorouter-crew/BioRouter/target/debug/biorouterd`. The verified daemon SHA-256 for this refresh is `3133646fa1fc6b6369677f0c6b769b7ead5ce1bd145bf803e38bea29d3f71cc3` (source product commit `367fe588`). After reconnect, all three rendered `Connected · identity verified`, `Synthetic Lab`, `#general`, and `People · 3`. Fresh rendered screenshots are `/private/tmp/final-9411-reconnected.png`, `/private/tmp/final-9412-reconnected.png`, and `/private/tmp/final-9413-reconnected.png`.

## Model workflows at `367fe588`

Carol's rendered Ask my agent run used Ollama `qwen3:8b` and `/no_think`, requesting a CSV read followed by processing and attachment. Run `9f1df8bb-2094-4a40-8e47-cdc3da43cd66` maps to session `20260922_2`, in channel `55441429-e81c-4991-b1d4-283961df5e18`. The room displayed four requested reads and an unsupported 10-row/42 summary. A separate read-only backend diagnostic matched all four typed tool requests with error responses: the model supplied that **channel ID as connection_id**, placed `path` at the top level instead of inside `params`, and used the absolute local task directory. Every response was `isError=true`, `Connection is outside the approved run scope`. No file access succeeded in this run; the model summary is rejected. Room progress labels alone do not establish tool arguments or results.

The separate later session `20260922_4` used correctly nested relative `remote.read(crew-task.csv)`. Its matched response returned the actual 31-byte CSV, SHA-256 `b8d8853f57f79b6bc9e4c3736b9f7c840e0975ea0606d31cc934349d5606335a`, containing three rows. The provider then returned no answer text or tool calls, and the UI reported the explicit Ollama error. This establishes an app-triggered tool-read subset, not completed processing, attachment, or personal-chat posting. No `remote.execute`, `remote.job_status`, or `remote.attach` success is claimed. Backend diagnostics are identified separately from rendered UI observations.

Source investigation found local working-directory/workspace-map and project-hint injection still active for Crew turns. The follow-up patch suppresses these ambient local inputs for authoritative Crew-scoped sessions and publishes matched typed tool outcomes in the room without copying raw results. Its regressions and fresh app replay are pending at this point in the record; earlier fabricated history has not been rewritten or accepted as evidence.

## Clean-channel controls at `373acdf5`

The daemon for this refresh was atomically installed and verified as `6c19b87f661f78a65011b33c00951206aecc14a19cbe75dab6d88a53ae1ca5ef`. Retained foreground sessions after restart were Alice 44170, Bob 57524, and Carol 48026 on CDP 9411/9412/9413. Each rendered `Connected · identity verified`, `Synthetic Lab`, and three members.

A clean restricted channel `#fresh-control` was created through Carol's rendered UI, then Alice and Bob were invited and accepted through their own rendered clients. Carol's exact single-control task used qwen3:8b and `/no_think` with JSON `{"method":"remote.read","params":{"path":"crew-task.csv"}}`, omitting `connection_id`. The rendered timeline showed `Tool response received: remote.read`; the returned file was 31 bytes with SHA-256 `b8d8853f57f79b6bc9e4c3736b9f7c840e0975ea0606d31cc934349d5606335a` and rows `A,10`, `B,20`, `C,30`. This is a valid tool-backed pass.

The first separate processing task performed actual `remote.execute`, `remote.job_status`, and `remote.attach`, but the completed Python process returned invalid `Row count: -1` and `Sum: 0`; the attached 19-byte summary was rejected. Its displayed job ID was `fa807bb05226f28b420aab09d9c161bb4e23c0f6bd98df406bfa88595e48611b`, and the displayed attachment identifier was a UUID, not a digest. A correction task explicitly requested `csv.DictReader`, but the model looped through another execute failure and receipt plus a status call without producing a terminal attach/result. No processing pass is claimed.

Alice and Bob each configured Ollama through their own rendered Settings UI and completed bounded owned tasks. Their first completed sessions contained assistant text and automatic room projection, but no typed Crew calls. A separate later Alice run issued `messages.history` for its current channel. Her selected-source run did not retrieve the human marker from `Synthetic Lab / #general`; no cross-channel positive is claimed. The provider report separates these run/session IDs.

A visual review of the final screenshots found no clipping, overlap, unreadable controls, or visible credentials. Evidence includes `final2-9411.png`, `final2-9412.png`, `fresh-control-members.png`, `fresh-control-result.png`, `carol-processing-correction.png`, and `alice-cross-channel.png`.

The separate correction task was cancelled via Carol's rendered `Cancel` control after backend diagnosis found syntax/idempotency failures. A post-cancel model message displayed a markdown “Corrected Solution” with expected `Row count: 3 / Sum: 60`, but no matching tool events; this generated text is rejected as fabricated and is not evidence of cancellation success or processing.

Security supplied prevalidated `summarize_csv.py` (SHA-256 `901605725a653a05544cd83bb7af79f711321475fb6d812a91cb991cc7f544d6`) with argv `python3 summarize_csv.py crew-task.csv summary.csv`. Carol session `20260922_8` instead issued a generated `python3 -c` script. Its matched status was exit 1 with a Python syntax error, followed by an empty Ollama completion. No attachment occurred; this was not execution of the supplied helper.

Carol seeded the source channel with the human-only fact `Cross-channel synthetic fact: marker KAPPA-9413 belongs to Carol and is restricted QA data.` An initial no-session `/crew` inspection did not complete navigation. The subsequent exact-command renderer patch and Bob replay below supersede that observation.

A fresh rendered Carol `/pair` composer accepted exact standalone `/crew`; Enter navigated to `#/crew` without a model turn. That inspection did not complete the no-session grant guidance or a personal model turn. An earlier inability to locate Send was an inspection limitation: Bob’s later replay found the rendered `aria-label="Send message"` control. The footer’s `Unavailable` label describes cost metadata, not provider availability.

Bob personal-flow evidence: after choosing the private Ollama model through rendered Settings, the personal composer exposed a DOM-backed `aria-label="Send message"` and sent a harmless synthetic greeting; the assistant replied. Standalone `/crew` from that existing personal chat navigated to `#/crew?sessionId=20260922_2`. Bob selected `#fresh-control`, opened the rendered review dialog, and clicked `Allow this conversation to read and post here`, returning to the same personal chat. A natural-language granted Crew read/post request then failed with the rendered provider message `Ollama completed without answer text or tool calls`; no Crew MCP result or post is claimed. Screenshots are retained under evidence.

Chronology/provenance correction: daemon `1ce50cb31f63dca70c7bb25c571facd1672fe9271801ddbf5e82f5785483e407` is the latest replay artifact; earlier `367fe588`/`6c19b87f...` observations are dated history. The prior processing record that appeared as a helper receipt was backend-correlated to session `20260922_8` / call `peok02fy`: the actual argv was generated `python3 -c`, not the supplied summarize_csv.py helper, and failed with SyntaxError followed by empty provider completion. The later clean helper attempt is session `20260922_9`, whose rendered metadata shows destination `c874aef4-10b5-41d3-99f7-6d6c90c4e644`, context discovery guidance, repeated execute/list/status calls, then UI cancellation. No helper processing pass is claimed.

The `Unavailable` footer seen in chat is cost metadata, not proof that Ollama was unavailable. Read-only Ollama diagnostics during the final run returned version `0.34.2` and loaded qwen3:8b digest `500a1f067a9f782620b40bee6f7b0c89e17ae61f686b92c24933e4ca4b2b8b41`, 8.2B Q4_K_M, context length 40960.

Rendered nested tool-details inspection corrected the clean helper classification: session `20260922_9` first used exact argv `["python3","summarize_csv.py","crew-task.csv","summary.csv"]` with idempotency key `abc123` and timeout 30. Job `3dcb6078a21fc8f1d3cd017f6912c732b442dc3f4250ebe5561647ff7e041966` returned status `failed`, exit code 2, stderr `python3: can't open file '/home/carol/work/crew-fixture/summarize_csv.py': [Errno 1] Operation not permitted`, and empty stdout. No attachment was attempted. Later model retries were stopped; this is a helper path/permission failure, not an argv mismatch.

### 2026-09-22 native-save bounded check (Luna)

A single bounded native-save check was attempted against the retained Carol client. The rendered Carol page was the actual Crew task renderer on CDP 9413 (`#/pair?resumeSessionId=20260922_9`), and the exact copied app path was `/private/tmp/BiorouterCarolElectron.app` (observed app label `Biorouter Crew Carol`). Native CUA inventory showed the Carol app running, but `getApp("com.biorouter.luna.carol")` returned `Invalid app`, and selecting the exact copied `.app` path returned `Running application not found: com.github.Electron`; selecting the observed label likewise returned `Invalid app`. No unrelated or generic Electron window was selected.

Because the exact app identity could not be acquired by the supported native selector, no Save dialog was positively identified and no native Save action was performed. No named-file completion or hash is claimed. Existing hidden Electron staging-byte observations remain staging only and are not upgraded by this check.

### 2026-09-22 signed fixture identity retry (Luna)

Read-only metadata showed the original copied bundle declared `com.biorouter.luna.carol` in `Info.plist` but reported `Identifier=Electron` under `codesign`. One disposable copy was created at `/private/tmp/BiorouterCarolElectronSigned.app`, assigned `CFBundleIdentifier=com.biorouter.luna.carol.signed` and display name `Biorouter Crew Carol Signed QA`, then ad-hoc signed with `codesign --force --sign - --identifier com.biorouter.luna.carol.signed`. Only Carol’s retained foreground session was stopped and relaunched as exec session `62077` (PID 16262), preserving the Carol profile and CDP 9413; Alice and Bob were untouched.

Native inventory then showed `Biorouter Crew Carol Signed QA` with id `com.biorouter.luna.carol.signed` running. The exact CUA app-selection call entered `waitingOnApproval` and returned no approval/result before the parent turn was interrupted. No Save dialog was opened and no named-file hash is claimed. The native binding attempt remains pending approval rather than a product Save result.

### 2026-09-22 Carol post-broker workflow blocker

After the broker upgrade, Carol’s rendered Crew UI reconnected successfully (`Connected · identity verified`; `local-carol-clean`; Synthetic Lab with #general, #fresh-control, and #processing-clean-2 visible; three people visible). Settings > Models > Switch models then showed current `qwen3:1.7b` and `LOCAL MODEL INVENTORY: 0 installed · 7 available`; qwen3:8b was not available in the visible picker. The provider/model combobox exposed no qwen3:8b option and the UI returned to Home during the bounded selection attempt. No model request, new channel, or processing task was submitted; this is a concrete provider-selection blocker for the required qwen3:8b processing workflow.

### 2026-09-22 Carol qwen3:8b helper processing success

Carol reconnected through the rendered Crew UI and the owned-agent panel was configured with `ollama · private`, model `qwen3:8b`, and remote folder `/home/carol/work/crew-fixture`. The task ran in the existing restricted `#processing-clean-2` channel (the visible UI exposed no create-channel control, so this is not labeled a fresh channel). Task conversation: `#/pair?resumeSessionId=20260922_10`.

The rendered typed `remote.execute` request used connection `8c991d2f-afef-4a3c-9d52-36647b4f76c7`, argv `python3 summarize_csv.py crew-task.csv summary.csv`, idempotency key `unique_key_20260922_1038`, timeout 30, and job ID `66537cc8d2128331cf8de475a43c6ca0b2605f66d4ee13a5439a257cff4e8f67`. `remote.job_status` returned exit code 0, completed status, and `row_count=3 total=60`. The rendered `remote.read` response for `summary.csv` reported 21 bytes, SHA-256 `29f857b262032224f20e849e76bed6907e8e02846835a632c8380b2bafb91ab3`, and bytes `row_count,total\n3,60\n`. The rendered `remote.attach` attached `summary.csv` (21 bytes) with ID `877d8a8e-e47c-41cd-9f9a-a03c99197f7c`. No generated alternate script or expected numeric answer was supplied.

### 2026-09-22 native CLI MFA and ProxyJump acceptance (Luna)

A separate disposable CLI profile and synthetic Linux PAM fixture were used after the retained Crew Electron clients were closed. The rebuilt artifacts were hash-checked locally: CLI `784c945022ca661b2cb97c8677686c044061226e00a9fa26bc752b0aa5b2271c`; daemon `5aa6d1c3e539e3c3c635ef0660af599aba51a008f49fa44c12a7c29937d2ded3`. The fixture used strict pinned host keys, an encrypted synthetic Ed25519 key, a synthetic PAM password, and a disposable two-hop localhost ProxyJump.

The native `biorouter crew auth` flow reached both key-passphrase prompts and the PAM prompt through ProxyJump and returned `authenticated: true` with exit code 0. A follow-up daemon status remained connected after CLI exit, proving the daemon-owned master remained usable. A wrong PAM secret was rejected by the fixture, and Ctrl-C at the PAM prompt returned an authentication-ended error without authenticating. An initial zero-size PTY was rejected with `Invalid authentication terminal size`; an explicit 24x80 PTY then passed. File-only sweeps over the disposable profile and fixture logs found no synthetic key passphrase or PAM secret. No retained workspace, production account, or model was involved.

### 2026-09-22 desktop artifact preparation (Luna; no GUI launch)

A bounded preparation pass for product `906bf68b` and workflow `0fb6cd13` completed the Electron main, preload, and renderer builds, and `tsc --noEmit` passed. The existing pinned backend pair was verified locally. This section records build preparation only: no app launch, profile change, native CUA retry, or GUI acceptance occurred, and the CLI-only condition remained active.
