# Crew invariant coverage map

Reconciled September 22, 2026 against the recorded artifact reports and integration handoff: cursor/draft source is committed in `37ac3803`; native per-hop preflight is committed in `bc3e26fb`. The historical `aac5f4ac` GUI daemon `1ce50cb3…`, previous `3145dfc5` Linux broker `a38b34be…` and newly installed broker `4f11d858…` have distinct evidence; retained GUI clients and old owned daemons are now closed. Hosted PR head `3145dfc5` now has 22 successes/one skip across 23 checks, no failures; all newer local commits and observer work are excluded.

Carol's rendered GUI helper processing/attachment now has a bounded positive, with passing independent backend/direct-SSH corroboration of typed calls, private `qwen3:8b`, exact output bytes/hash, UID 1103 and daemon `1ce50cb3…`. Personal-chat MCP, exact cross-channel retrieval and named native Save remain open. **Daemon-owned native CLI/GUI parity is mandatory new completion scope, with no CLI-only or mixed-interface acceptance yet.** See [plan §15](implementation-plan.md#15-daemon-owned-workflows-and-cligui-parity) and [the feature ledger](implementation-status.md#feature-and-interface-parity-ledger). I01–I24 retain their original evidence/gaps; P01–P10 below add service/interface regressions. No product tests, builds or probes were run for this documentation reconciliation.

## Evidence provenance and limits

- **BC**: [`broker_contract.rs`](../../../crates/biorouter-crew/tests/broker_contract.rs). These call `Broker::handle` directly with a supplied UID. They test broker authorization logic, **not** kernel credential extraction, SSH login, desktop wiring or a real provider. The file is Unix gated; `wrong_writer_node_refuses_before_torn_tail_repair` is Linux-only. The [validation report](validation-report.md) records 22 passing tests on its earlier reported Rust revision. Count source and execution separately.
- **V**: [validation report](validation-report.md), an append-only record with later validation refreshes. It records focused scope/control-path/provider tests, repository gates and artifact hashes; later successful gates supersede its intermediate Clippy failure, not every subsequent source change. It also records pinned x86_64 ELF/startup/service evidence, a refused emulated-kernel stop, failed local-model self-tests (`tool_loop`, then `provider_failure`), and a higher-timeout zero-exit response with no workflow assertions. None is a passing Crew conversational workflow.
- **CI**: [PR #366 checks](https://github.com/BaranziniLab/biorouter/pull/366/checks) now record 22 successes and one skip for the 23 checks on `3145dfc5`, with no failure. Earlier `aac5f4ac`/`367fe588` green observations are historical. This hosted result does not validate later local commits or the new observer implementation.
- **T373**: Focused evidence at `373acdf5`: five ToolActivity tests passed (matching typed requests/responses, outcome wording, deduplication, unmatched/unknown/non-Crew rejection and private payload exclusion; see [the unit refresh](validation-report.md#toolactivity-projection-unit-refresh-2026-09-22)). Three context tests also passed: Crew MOIM excludes the local path/workspace-map canary; ordinary MOIM retains them; real prompt preparation excludes Crew AGENTS hints and preserves ordinary hints. No retained platform-client fixture was exercised: the early return before platform clients is source-review evidence only. The rebuilt daemon is `6c19b87f…`; P records its four API boundary passes, and V records all ten final local repository checks passing on `373acdf5`. These eight focused tests and the repository gate establish no GUI acceptance by themselves.
- **TAAC**: Evidence for changes committed in `aac5f4ac`: nine `/crew` Vitest tests and UI lint passed. The default-parallel server route suite passed 11 tests with 712 filtered out: the five ToolActivity tests plus six cancellation helper/state/event tests using temporary ledgers and session-bus events. They cover cancellation/completion ordering, later progress refusal, revocation retry, uncertain/interrupted recovery, persistence failure and final-event wording, not live HTTP/network cancellation or remote process termination. Two core discovery commands each passed one test with 4,193 filtered out; the admission test also passed its one-test isolated fake-SSH child. They check bound-connection/source metadata, missing/expired/epoch-changed scope refusal and destination-only initial history without extra automatic retrieval. The child is execution of that admission fixture, not a third independent discovery test. [Independent source reviews](review-privacy.md) are committed. All ten full repository checks and the daemon/CLI builds pass, including version `1.91.1` and help checks; hashes are recorded in [implementation status](implementation-status.md). Both artifacts are installed. U records three verified client reconnects on `1ce50cb3…`; P records all four repeated API boundary passes. These subsets do not establish its full graphical/model workflows.
- **C**: The [live HTTP cancellation regression](validation-report.md#live-http-crew-cancellation-regression-2026-09-22) passes on daemon `1ce50cb3…` and broker `7311f126…` with a temporary profile and held mock-provider stream. A mismatched connection is refused; the owner receives confirmed cancellation; exactly one `Finish(reason=cancelled)` frame arrives; later released provider completion leaves the ledger cancelled; a completion-first run reports already-finished/completed and `cancelled=false`. It uses neither the three UI profiles nor Ollama. Unconfirmed remote-revocation failure/retry remains unit-only, and no remote process termination is established.
- **F**: The FIOCLEX broker correction is pushed in `3145dfc5`. [Independent source review](review-privacy.md#scoped-python-script-opening-follow-up--september-22-2026), all ten local repository checks, and the [actual Linux ARM64 regression](validation-report.md#linux-arm64-fioclex-regression-artifact-2026-09-22) pass for `remote.rs` SHA-256 `937cce9f67dd71d11775f2ba73d5b029aa01c331be0247012719cca5288f7f2c`. ELF `a38b34be38201fe6a1aacd21451d4c8b9311cc3478fd9d919f36810a037d83f5` executes the exact Python helper and returns `row_count=3 total=60`; its 21-byte output hashes to `29f857b262032224f20e849e76bed6907e8e02846835a632c8380b2bafb91ab3`. Network, subprocess, unknown ioctl, FIONCLEX, outside-file, `/etc/passwd` and `../.ssh` negatives remain denied. The three-user fixture was rootlessly upgraded to this artifact with preserved state. Separate pinned x86_64 ELF `eefaece19ce99b3ba54f7358c64a6d0ed4efb6788b37a8b080fefe3b7a9cb3e4` imports at most GLIBC 2.30 and passes version/help/start/status/bridge hello as UID 65532 in emulated Bullseye. Neither runtime check is a completed GUI processing/attachment workflow. The earlier Mach-O exit-126 canary remains excluded as a setup failure.
- **A**: [adversarial validation](adversarial-validation.md). Four passing typed broker tests cover blob-transfer revocation, non-owner archive/transfer, concurrent-writer refusal, and request bounds. Separate Linux runs inject journal write EIO, journal fsync EIO and journal write ENOSPC, preserving an earlier acknowledged message through restart and refusing a subsequent blob mutation before file creation. A live-socket harness passes invalid UTF-8, oversized-frame, invalid-version and truncated-close cases without journal changes. These are bounded assertions, not fuzzing, power loss or the full storage-fault matrix.
- **P**: [local provider boundary report](local-provider-boundary-report.md). The four API sink scenarios passed on daemon `1ffa69d9…`, repeated successfully on `373acdf5` daemon `6c19b87f…`, and now pass again on `aac5f4ac` daemon `1ce50cb3…`, all with unchanged broker `7311f126…`. Each allows one public control request, then records no additional request/private marker for workspace-private, personal-private, verified-private-alias and retained-history refusal. Two controlled fake-SSH tests exercise the transport mutex/in-flight response. Separately, the corrected real private Ollama API replay on `1ffa69d9…` retains typed remote.read, matching CSV and calculated rows=3 total=60; repaired offline parsing validates that trace without another inference. The initial parser-invalid attempt stays excluded. None of these API results establishes graphical G05 or final-artifact conversational MCP completion.
- **L**: [local Linux CLI/helper report](local-linux-cli-canary-report.md), with binary and working-diff hashes. Covers actual Linux bridge/helper, stable restart, wrong-node refusal, per-UID connection limits, and 10/30-real-UID 60-second warmups. These are not the 50-user/30-minute soak.
- **Q**: [50-real-UID workload report](local-real-uid-50-soak-report.md), now recording the second run with broker `7311f126…` in Debian GNU/Linux 13 (trixie), cached `rust:latest` runtime. The top-level run spans 13:42:00–14:12:31 UTC and records 1,500 distinct acknowledged/read-back IDs with matching body hashes, zero errors/disconnects, and exact same-state paginated replay after restart. It reports p99 903.92 ms, broker peak RSS 1,262,800 KiB, 106 open FDs and a 2,233,601-byte journal. This is standalone broker traffic with synthetic UIDs, not GUI-under-load acceptance; per-participant minimum duration is unmeasured. The runtime reports GLIBC 2.41; the unchanged ARM64 binary itself imports GLIBC 2.39 and failed Bullseye startup. Runtime identity and the binary's original build provenance are distinct, as is V's pinned x86_64 artifact.
- **U**: [Fixture integrity](qa-fixture-integrity.md) and [native UI acceptance](crew-ui-acceptance-report.md), with typed/backend corroboration in P, retain the human/file/private-channel/ownership and PAM observations under their own artifact hashes. The `373acdf5`/`6c19b87f…` GUI read `fefe37f6…` / session `20260922_5` / tool `call_q3k44r7t` completed with the exact 31-byte CSV, SHA-256 `b8d8853f57f79b6bc9e4c3736b9f7c840e0975ea0606d31cc934349d5606335a`. Alice/Bob first completed text-only tasks; later Alice history covered only its current channel, and an explicitly granted extra channel was not retrieved. Bob completed personal greeting → session-bound `/crew` → Review → Allow on `6c19b87f…`, but the subsequent `qwen3:1.7b` MCP turn failed empty. All three clients reconnect on `aac5f4ac` daemon `1ce50cb3…`; latest Alice `qwen3:8b` retrieval again returned stale CSV text without tools. Run `4d7554d4-9bb7-460b-8766-f9904c96cb04` / session `20260922_9` issued correct helper argv but the old broker returned Python open EPERM/exit 2; its cancelled ledger, null error and persisted/UI Stopped state give a bounded cancellation positive, not remote process termination. F validates the helper correction separately. Carol’s rendered session `20260922_10` now shows exact-helper execution, exit 0, the same 21-byte output/hash and attachment `877d8a8e-e47c-41cd-9f9a-a03c99197f7c` in existing restricted `#processing-clean-2`; [independent backend/direct-SSH corroboration passes](local-provider-boundary-report.md#carol-processing-clean-2-successful-helper-corroboration-read-only), including the retained completed run and exact disk bytes owned by UID 1103 on daemon `1ce50cb3…`. The earlier Settings inventory mismatch is not proof that Crew model selection is broken. Cross-channel retrieval and personal MCP remain open. Native download hashes establish staging only; exact signed-app CUA selection awaits access approval and no user-named Save has completed.
- **W**: Cursor/draft correction is committed in `37ac3803`: the integration handoff records four broker cursor and six UI tests plus typecheck passing. The [refreshed Linux report](validation-report.md#refreshed-linux-broker-artifact-and-contracts-2026-09-22) records ELF `4f11d858…`, built from the then-uncommitted cursor source, with 30 passed/one ignored and helper/confinement positives/negatives. It was not installed during that historical refresh; it has since been installed as recorded in the current ledger. Native OpenSSH per-hop preflight is committed in `bc3e26fb` and independently source-reviewed, with seven focused tests and three affected admission/worker tests passing per the handoff; formatting and whitespace checks pass. The legacy optional `ForkAfterAuthentication` test omits the config directive but modern `ssh -G` still emits that field. It does not test an absent dump field or an old native client. Two separate pure missing-field unit tests now pass (2/2); an old-client runtime remains untested. The cursor review covers UUID anchors, cached replies and internal numeric watermarks; visible workspace `policy_epoch` is unchanged. Native configuration preflight is not a frozen configuration snapshot. The combined local build/gate subsequently passed on `8d6c2ae4`; later corrections and full affected transport/UI acceptance remain separate.
- **S**: [multi-hop report](local-multihop-ssh-test-report.md) and [synthetic PAM report](linux-ssh-pam-test-report.md). Native OpenSSH probes now include approved exec with target TCP forwarding disabled, forced-SFTP exec refusal, changed gateway/final-target key refusal, and two manually initiated PAM reconnects with one prompt/session each. No Crew daemon/artifact was used in these added probes. They do not establish desktop reconnect/MFA, institutional semantics or expired credentials.
- **H**: [institutional compatibility report](institutional-ssh-compatibility.md), read-only capability probes. Narrows: NFS HOME, unavailable Landlock/pidfd syscall probes. Leo: ZFS, Landlock ABI 1 and pidfd. Neither establishes production Crew support.
- Source references below identify implemented checks, not execution evidence. Existing smoke scripts are procedures, not proof they ran. The Python AWS research fixture is not the Rust product binary.

## I01–I24

| ID | Existing focused assertions / recorded evidence | Remaining scope and gaps |
|---|---|---|
| I01 Stable workspace and aliases | L records same socket and workspace after restart. P verifies a private alias to the same broker/workspace prevents a new public run through the original active connection. Core `verify_workspace_identity` checks workspace UUID, owner UID, pinned key, fresh nonce signature and node digest; `connect` merges verified same-node aliases. S records native multi-hop routing. | No cited end-to-end wrong-descriptor/alias-policy regression through every desktop route or across different workspaces on the same cluster. Multi-hop SSH alone does not show two routes discover the same Crew workspace. |
| I02 Kernel UID and enrolled identity | BC `signed_auth_binds_uid_and_device_key`, `enrollment_invitation_is_bound_to_the_intended_device_key`, `additional_device_invitation_requires_the_active_principal_binding`; L/U exercise distinct real Unix users. A wire checks use the actual Unix socket/peer-credential path; Q records 50 distinct participant UIDs. Broker `peer_uid` supplies production identity; manager enrollment requires intended device key. | BC's UID is a supplied argument. Recycled UID with unchanged username cannot be detected by the kernel; explicit offboarding/add-device intent is required. Same-nickname collision, account rename/reuse and fresh-principal membership inheritance need dedicated negatives. |
| I03 Explicit team/channel membership | BC enrollment/team/invitation setup is exercised by membership, transfer and provenance tests. U reports private-channel invitation checks. Snapshot enumerates enrolled principals rather than OS accounts. | Setup success does not comprehensively test multi-team discovery, invitation expiry/replay, team-wide membership transitions or enumeration side channels. |
| I04 Ownership and transfer | BC `ownership_transfer_removes_old_owner_and_invalidates_stale_invites`; A `non_owner_cannot_archive_or_transfer_channel` passed; U reports ownership checks. Broker transfer/archive handlers check current authority and successor acceptance. | The full archive/remove matrix, concurrent transfer/accept, immutable creator audit and stale approvals remain broader than the focused non-owner test. |
| I05 Human and owned-agent room activity | U records three-person chat, completed distinct Alice/Bob text-only tasks, a later current-channel history call and the `373acdf5` tool-backed CSV read. P retains the earlier API tool/result positive. T373/TAAC cover typed activity projection; TAAC adds six cancellation helper/state/event tests including terminal ordering and retry. U/P now corroborate run `4d7554d4…` as cancelled in the ledger with a persisted Stopped notification and matching UI state. C adds live route ownership-mismatch refusal, confirmed owner cancellation, terminal event and both completion/cancellation orderings. | No complete three-user invoke/steer/approve/cancel matrix or final-parity-artifact workflow is established; Carol has an independently corroborated processing/attachment positive under U. Alice/Bob's first completed tasks contain no typed tools. The earlier Cancel click remains inconclusive; the later UI result and C are bounded positives, not proof of remote process termination, three-principal control coverage or all cancellation/recovery races. Unconfirmed remote-revocation retry is unit-only. |
| I06 Durable, resumable attachments | BC attachment-begin, validated attachment-only message and cross-channel provenance assertions; A transfer-revocation test passed. U reports upload/preview and downloaded-byte SHA256 verification; native PNG/binary checks verified temporary staging files only. L denies scoped remote symlink/traversal reads. | Completed user-named native Save awaits approval for exact signed-app CUA access; it has not passed. Typed revocation is not browser restart/resume or concurrent transfer. Partial finalization, attachment hardlink/special-file attacks, malicious previews and archive-content cases remain open. Remote-file canaries do not establish blob-store attack coverage. |
| I07 Source ACL/provenance | BC cross-channel attachment/reference provenance, membership revocation and restricted-message public-run invalidation; A tests blob revocation. P blocks restricted retained-context public dispatch. TAAC tests bound source-ID discovery and destination-only injected history; U/P prove one actual broker grant included the selected extra channel. | The model did not retrieve the seeded KAPPA fact; Alice's latest `1ce50cb3…`/`qwen3:8b` attempt again returned stale CSV text without a typed retrieval. Admitted scope is not successful cross-channel retrieval. Later-member access, search/fetch races, derived outputs and multi-source policy combinations need end-to-end proof. Discovery IDs are metadata, not new fetch authority. |
| I08 Intersection admission | BC Private/public-safe, UID/device, membership, revocation and epoch cases exercise parts of the intersection. P adds API/provider-sink workspace, personal, alias and retained-history denials. TAAC metadata checks refuse missing, locally expired and epoch-changed scope and omit another saved connection. Core resolves the actual provider and broker checks source scope. | No exhaustive any-one-denial matrix; local discovery does not prove live broker membership/TTL freshness. Institutional data-compartment/provider-realm labels are not implemented, rather than merely untested. Current public/private plus source-ACL enforcement must not be described as institution-wide PHI authorization. Every ingress/egress and unknown endpoint case remains broader than the tested subset. |
| I09 Private blocks all public paths | BC Private-run denial; V private-transcript/public-provider compaction and scoped MCP-sampling refusals. P's four API scenarios prove allowed control traffic followed by zero additional restricted requests. T373 excludes local path/map and AGENTS canaries from Crew ambient context while preserving ordinary-session context. Managed-hook refusal is recorded in implementation-status. | T373 is not a public-sink or retained-platform-client test; bypassing those clients is source-reviewed only. Sink evidence covers the tested API route/artifacts, not every embedding/OCR/summary/plugin, UI/slash/MCP, scheduled or reconnect path. Graphical G05 remains unestablished. |
| I10 Public never relabels private history | BC public-safe admission, restricted-message invalidation and cross-channel provenance. P switches workspace/connection Private→Public after a private post to the same public-safe channel and receives retained-context denial without new sink traffic. V records failed-policy-save authority preservation. | Retained blob/cache and historical-conversation variants remain separate from the tested message scenario. The policy-save test is not a disk-full or post-rename-fsync fault test. |
| I11 Epoch / queued work | BC epoch/projection and sequential delayed-response checks; V scope helper test. P's fake-SSH tests cover a queued request refused before write and a held response refused after policy change. TAAC covers cancellation reservation before success/progress updates and the reverse completion-first order in helper/state tests. U/P add one actual run's cancelled ledger and persisted/UI Stopped outcome; C tests confirmed live HTTP cancellation before held-provider release and the completion-first ordering. | C does not force unconfirmed remote-revocation retry, which remains unit-only, or establish remote process termination. Fake SSH and temporary-ledger tests do not cover all live SSH/provider races. Alias transitions during jobs, cancellation of already-submitted effects and cross-profile runtime races remain open. |
| I12 Invoking UID and isolation | L actual rootless helper produces a CSV and denies socket creation, subprocess creation, symlinks, traversal and fake grants. `host_remote_scope_cannot_overlap_broker_authority_state` checks broker-store overlap. F adds an actual Linux exact-helper positive plus other-ioctl/network/process/path negatives after the narrow FIOCLEX allowance. H records incompatible kernels. | Scheduler/job-allocation and arbitrary shell are not implemented. F is bounded helper evidence, not the full graphical workflow. Other authority stores, hostile same-UID races, cross-user process isolation and missing-confinement refusals on the target matrix remain distinct. Fixture administrator provisioning is not product rootlessness. |
| I13 Ordered, bounded, idempotent journal | BC exact replay/conflict, delta restart replay and logical-state quota assertions. A rejects concurrent writers and allows reopen after release; oversized wire leaves journal unchanged. Q reads back all 1,500 distinct acknowledged IDs/body hashes and paginates them again after restart. | NFS fencing and the full replay-boundary matrix remain outside this evidence. Q covers one message workload/artifact, not every mutation, GUI load or resource budget. Retention/compaction and an expiring dedupe retry horizon are not implemented; fixed quotas stop writes. Connection quotas alone do not prove journal locking. |
| I14 Durable acknowledgment and recovery | BC torn-tail, checksum corruption and delta replay; L wrong-node+torn-tail preservation. A journal write/fsync EIO and write ENOSPC injections preserve an earlier acknowledged message through restart and reject a subsequent blob mutation before file creation. Q adds exact 1,500-message same-state restart replay. | Controlled restart and one injected errno do not establish power loss/server loss, every append/fsync/ack boundary or interior revocation corruption. Attachment/directory-fsync and uncertain post-rename outcomes remain untested. |
| I15 Storage qualification | BC logical-state quota rejection; A injected journal EIO/ENOSPC recovery checks. H measures NFS/ZFS and kernel primitives. Broker explicitly refuses NFS/SMB rather than silently relocating canonical storage. | Logical 16 MiB quota and injected ENOSPC are not literal disk-full evidence. Directory-fsync failure, physical exhaustion, remount/server disconnect and qualified NFS durability remain open. NFS stays unsupported. |
| I16 Framing/cursors/queues | A typed bounds and actual-socket invalid UTF-8, oversized frame, invalid version and truncated-close cases pass, preserving journal and healthy hello. L checks per-UID limits. W source review covers UUID cursors, current source-ACL anchor checks, cached wire projection, internal monotonic watermarks and stale-page recovery. | W records bounded focused cursor/draft and Linux contract passes; complete real-client paging, revocation/reconnect and final-artifact execution remain open. No broad fuzzing or slow-consumer resync proof. Ordinary workspace pagination is not Crew history evidence. Expiring dedupe retry horizon remains unimplemented. |
| I17 Chunk publication ordering | BC attachment begin, attachment-only post and provenance; A transfer-revocation test and poisoned-journal refusal before a subsequent blob file is created. Source checks offsets/digests, fsyncs file and journals completion; UI caps browser payload size. | The faulting A operation is message.post, not blob publication. The blob design does not implement the plan's separate final-file rename/orphan grace sweep; uncertain blob-fsync files may need recovery. Each attachment boundary and constant-memory behavior remain unproven; UI full-file memory is bounded by 64 MiB, not constant memory. |
| I18 SSH trust/MFA state machine | S covers two jump daemons, target/gateway changed-key refusal, approved exec with forwarding disabled, forced-SFTP exec refusal, prompt cancellation, encrypted-key/PAM and wrong-secret behavior. Two manual PAM reconnects each prompt once and reach UID 1000. U adds earlier desktop authentication/close; V covers control paths. W adds source-reviewed native per-hop preflight. | W preflight is committed with seven focused and three affected passes; its optional-field case is not old-client runtime evidence; no executed secure per-hop Crew acceptance yet. S policy/reconnect probes are CLI-only and use no Crew artifact. Full desktop broker handoff, expiry/certificates, TOTP/Duo/security-key, per-hop MFA and Windows native SSH remain open. Auth reuse currently ends at explicit disconnect/SSH termination; a maximum authentication-age lifetime is not implemented or claimed. |
| I19 Secret/process containment | S checks synthetic secrets absent from its sshd log and cancellation. T373 proves synthetic private tool success/error payloads do not enter projected activity text. Source uses human-only auth IPC, strips worker environment, limits grants and verifies pidfd stop. | No comprehensive prompts/events/telemetry/screenshots/renderer-log secret sweep, process-tree cancellation audit across OSes or exact-target PID-reuse negative. Activity unit tests are not a whole-room/model-output redaction guarantee; fixture-log absence has its reported scope. |
| I20 UI state and identity | U human/file/channel observations and live standalone `/crew` navigation without a model turn. V records four CrewView regressions; TAAC adds nine command/navigation/draft/extras tests. W source review confirms stale older-page refusal preserves the current authorized channel and draft while clearing stale messages. Server nickname/avatar/read positions and local saved connection preferences are implemented. | W has six focused UI passes and typecheck; these are not complete live paging acceptance. No complete unread/read-position, stale authorization redraw, long-history paging, accessibility or multi-instance reconnect/MFA matrix. Navigation is not full personal MCP or installed-app acceptance. Native named Save awaits exact signed-app CUA access approval; staged bytes remain separate. |
| I21 Shared saved connection for MCP/UI | V scoped-request tests, T373 MOIM/prompt isolation and TAAC bound-connection/source discovery tests. U records the earlier post-grant wrong-scope refusal, the `373acdf5` owned-task CSV read, exact-command navigation, and Bob's completed personal grant flow on `6c19b87f…` preserving session `20260922_2`. P retains the earlier API tool round trip. | Bob's subsequent personal MCP request failed with an empty completion from actual model `qwen3:1.7b`; full conversational MCP remains open. A no-session navigation creates no grant or conversation. No retained platform-client fixture was tested; its skip is source-reviewed only. Grant/navigation and owned-task read evidence do not establish the complete personal-chat workflow. |
| I22 Typed explicit conversational grants | U records Bob's harmless personal greeting → session-bound `/crew` → selected `#fresh-control` → Review → Allow flow on `6c19b87f…`, alongside standalone navigation without inference. TAAC's nine tests keep exact navigation distinct from ordinary `/crew` prose and preserve staged extras without dispatch. P proves API controls/privacy negatives; server human/idle-turn/provider checks remain. | Bob's post-consent natural MCP request failed empty; successful conversational execution, ambiguous destinations, read-only versus posting permission and graphical/MCP sink negatives remain open. The new-chat UI still requires a non-sensitive first turn or an existing conversation before granting; navigation alone grants nothing. |
| I23 Durable remote invocation IDs | L actual helper execution; U/P retain processing idempotency-conflict refusals. Source reserves/fsyncs job identity before spawn and refuses blind reuse. TAAC covers local cancellation persistence/recovery; C adds live confirmed-cancel event/order checks. F now executes the exact Python helper successfully in a disposable Linux fixture while confinement negatives remain denied. | Disconnect-during-ack, parent crash, marker-fsync, completed-record faults and cross-run cancel are not fully exercised. Historical GUI run `4d7554d4…` issued correct argv but hit the old-broker EPERM; the corrected Carol GUI processing/attachment has a rendered positive, with passing independent backend/direct-SSH corroboration of typed calls, private `qwen3:8b`, exact output bytes/hash, UID 1103 and daemon `1ce50cb3…`. Cancelled ledger/UI Stopped does not prove remote-process termination. |
| I24 Rootless platform support | L ordinary-UID Linux ARM64 CLI/helper; F live preserved-state rootless upgrade to ARM64 `a38b34be…` and pinned x86_64 `eefaece1…` bounded Bullseye version/help/start/status/bridge-hello smoke as UID 65532, importing at most GLIBC 2.30. V retains earlier malformed-artifact guard negatives and safe-stop refusal when pidfd_open was unavailable. H records institutional capability limits. | Not full deb/rpm installation, native x86_64 stop/helper, all-distribution, Windows desktop or installed macOS acceptance. NFS canonical storage and automatic failover are unsupported, not merely untested; Narrows remains blocked and Leo ABI 1 cannot supply required helper confinement. Backup/migration/restore tooling and read/download audit records are also unimplemented plan capabilities. |

## Remaining core acceptance and expanded qualification

1. **Required core workflows:** complete selected-channel retrieval and personal-chat MCP, retaining matched tool/data/room traces. Complete user-named native Save after exact signed-app access approval. The helper regression now passes; its historical EPERM and the earlier model syntax/idempotency/empty-completion failures remain separate. Existing consent/navigation, GUI read and API boundary positives do not establish these full workflows. Carol's rendered task-local `qwen3:8b` processing/attachment passed, with passing independent corroboration; Settings inventory mismatch alone is not a Crew selection defect.
2. **Required correction/final integration:** complete W's final native per-hop product acceptance and integration, build the combined revision and rerun affected acceptance/gates. Record the actual `3145dfc5` hosted outcome when available, then the later final head separately. Retained platform-client omission remains source-reviewed only. Private/public and owner-grant enforcement must remain mandatory through these changes.
3. **Broader adversarial qualification:** extend storage tests beyond journal write/fsync EIO and write ENOSPC to physical exhaustion, attachment/directory fsync, interrupted acknowledgment, uncertain post-rename outcomes and power/server loss. Extend controlled queued/in-flight checks to live transport/provider races, aliases during work and all three owners. Live confirmed HTTP cancellation passes; unconfirmed revocation retry is unit-only, and remote process termination is not established.
4. **Scale/portability matrix:** assess Q's 1,500-message/top-level 30-minute exact-replay result against resource budgets, including 1,262,800 KiB peak broker RSS; add per-participant duration, mixed workloads and integrated GUI load. Carry CLI gateway/SFTP/PAM cases through desktop handoff, expiry and per-hop MFA, then Windows/Linux clients and installed packages. The aarch64 Debian 13 soak and emulated x86_64 Bullseye smoke prove different subsets.
5. **Explicit unsupported plan scope:** institutional compartments and approved local private persistence, NFS, scheduler integration, retention/compaction, read/download audit and operational backup/migration/restore remain unimplemented. These require scoped future work rather than more tests alone. Institutional capability denials and the AWS source-export block remain visible; cleaned AWS resources are not a product acceptance pass.

Use exact revision and artifact hashes from the linked reports. Do not promote
source review, an earlier fixture or one focused pass into full-invariant or
final-revision acceptance.


## Daemon-owned parity regressions

Current source is `a11ea7e9`: the shared-daemon conversation adapter, durable elicitation and safe SSH diagnostics are committed in `21ce28a2`, followed by test-only Clippy cleanup in `74190f6b` and the reviewed extension nonsecret-store guard plus three focused core test cases and UI await-order correction in `a11ea7e9`. The preceding full local gate passed on `74190f6b`; the extension-install suite now passes 31 tests and the serial action-required route suite passes 16. The latest full gate stopped on a Clippy boolean-style warning; its equivalent simplification is applied and awaits a fresh gate and native build. The full desktop suite now passes 551 files and 6,262 tests, with 19 skipped and zero failures; the current UI is unchanged since that pass. Earlier core manager 7 and pending 30 checks retain their scope. Wrong keychain lookup for blank nonsecret fields was confirmed and fixed, but causation of the original full-server hang is unproven: the bounded pre-fix reproduction stopped during compilation. The post-fix route suite passes; the complete gate and new artifacts remain pending.

The prior focused conversation checkpoint passes daemon client 16 (before the later diagnostic display change), shared CLI 4, adapter 5, provider binding 11, durable elicitation 4, UI form 2 and store 6, with clean typecheck, regenerated API, formatting and diff checks. Scopes overlap and are not summed. The selected diagnostic test passed, but its interrupted encompassing command is not an exit-0 result. [Validation](validation-report.md#shared-daemon-conversation-and-durable-elicitation-checkpoint) retains exact assertions and command scope.

Pinned `4a2e190b` remains the validated native/Linux production pair; its Linux UID 1101 lifecycle repeat passes, excluding the earlier test-feature daemon smoke. Fresh three-user bootstrap and a human roundtrip pass on that pair, awaiting rebuilt artifacts and the deterministic loopback provider fixture. Earlier `9a3957d6` downloads pass confirmation/proof/refusal/replay, post-confirm replacement, restart/resume and observed-partial cleanup with the same transfer implementation. [CLI evidence](evidence/crew-cli-observer-context-20260922.md) and [validation](validation-report.md) retain hashes, bounded MFA/helper/context/privacy/observer passes, invalid mixed-artifact attempts and fixture corrections. Those results do not establish the new conversation adapter's model/MCP behavior.

Full CLI-only and mixed GUI/CLI acceptance, observer slow-reader/fairness and broader fault/recovery/privacy matrices, actual GUI and native Windows remain open. Default local conversations still use their local manager; only explicit shared-daemon mode targets daemon conversation services. The historical broken-pipe cause remains unresolved; five model self-test attempts did not execute required assertions. Shared IPC is Unix-only. Source-and-text PR publication is authorized and being prepared through the safer alternative explicitly allowed by automatic review. Visuals remain local: back up unpublished history, remove screenshot-only `b90939b7` from pushed ancestry, and never publish the visual backup ref. AWS product export and native CUA approvals remain pending; visual export is excluded. Hosted checks cover older `3145dfc5` only. Historical evidence and unsupported institutional platforms remain scoped as recorded, not upgraded by current source checks.

Required ordinary-conversation regressions execute explicit shared-daemon run/session against an already connected daemon, with grants before and after startup, locked local vault, revocation and wrong-owner/privacy denial. The [plan contracts](implementation-plan.md#current-source-milestone-and-unresolved-review) and table below remain the acceptance requirements.

| Feature IDs | Required shared-service and interface assertions | Evidence boundary / gate |
|---|---|---|
| P01/P10 | Start/attach one canonical profile without Electron assets/processes; reject stale/mismatched discovery; serialize concurrent launch; authenticated exact-instance shutdown; both clients reuse the service | G10/G14/G15; API secret and discovery identity do not grant human authority |
| P02 | Missing/wrong/replayed/expired human proof refused; worker CLI invocation/model-created PTY cannot bootstrap a controller; secrets absent from argv, environments, logs, streams and durable receipts | G12/G14; accepted digest-only startup pipe and explicit secret input preserve Proven-only; no argv/env/descriptor/settings secret acquisition |
| P02 credentials | OS-keyring default; Linux vault requires explicit opt-in, proof-gated init/unlock, separate passphrase, fixed bounded Argon2id parameters, XChaCha20Poly1305 profile binding/fresh nonce, locked restart, zeroizing memory and atomic ciphertext; refuse corruption/substitution and plaintext fallback | G12/G14; source present and primitive review closed after fresh-profile guard, no runtime acceptance claim |
| P01/P02 reopening | Reopen GUI against the same daemon using explicit native prompt/confirmation for a human-held secret >=32 bytes; no secret persistence or replacement of existing attach key; wrong/missing secret refused | G12/G14; ASCII proof/Proven attach correction source present; reopening runtime acceptance pending |
| P01/P08 shutdown | Stop only the authenticated intended instance, cancel owned runs and bound drain with open WebSocket (10-second TCP and 2-second UDS source limits); detach does not stop daemon | G14; bounded shutdown and exact-ID cleanup source present; focused runtime contracts do not establish open-stream teardown acceptance |
| P03 | Same saved connection IDs and workspace identity; terminal and GUI input/resize/cancel use daemon auth session; per-hop changed keys fail closed; MFA reconnect and controller loss have explicit outcomes | G10/G11/G12/G14; native SSH-only fixture success is not Crew product success |
| P04 | CLI-created/joined rooms, invites, accepted ownership and profile changes appear in GUI and vice versa; stale/non-owner membership and transfer denied | G10–G12; three real Unix users, separate credentials, exact object IDs |
| P05 | Opaque cursor paging/search/watch/read parity; wrong-room/hidden/stale anchors refused; reconnect drains authorized pages; revocation clears stale context; exact seeded cross-channel fact retrieved only within granted scope | G10–G12; existing cursor tests and GUI scope grants do not prove successful retrieval/watch |
| P06 | Shared typed mutations preserve retry ID and payload; uncertain ack cannot create duplicate posts; reference/provenance and current membership rechecked on retry | G10–G12; compare broker receipts and message IDs across adapters |
| P07 | Large arbitrary binary transfer has bounded memory and exact digest; daemon restart/resume preserves metadata-only receipt; source/destination replacement, symlink/reparse, unauthorized overwrite and mid-transfer revocation denied; atomic final publication | G10/G11/G13; GUI localStorage/chunk tests and staged bytes are insufficient |
| P08 | Start/follow/inspect/approve/cancel same owner run through either interface; foreign run refused; detach leaves admitted work's specified state; restart reports unknown/interrupted outcomes without blind replay | G10–G12/G14; cancellation ledger state is not remote process termination |
| P09 | Grant/list/revoke and privacy changes use one authoritative service; private→public retains restrictions; both clients refuse public sink disclosure, stale grants and unauthorized source channels before effects | G10–G12; API negatives and GUI consent are bounded historical evidence |
| P10/all | Generated schema, CLI help, typed client/error semantics and shared limits agree; command handlers and GUI contain no duplicate authorization/transfer/task state machines; final hashes and required checks recorded | G15; source review and design inventory alone are not implementation or acceptance |

Run CLI-only acceptance with every Crew Electron client fully closed first, then mixed GUI/CLI collaboration among three real Unix users. Keep per-OS limitations, signed-app native Save approval, institutional kernel/storage refusals and AWS source-export approval separate. No approval blocker permits a weaker gate or a transfer workaround.

Current shared IPC is Unix-only; shared Windows CLI is unavailable and its optional first-release scope is undecided. Windows desktop functionality remains required. The new Windows transfer helper is fully integrated in source, but native directory-durability acceptance remains unresolved. Unix checks cannot qualify that path. AWS source-export approval is still pending; no product AWS validation has occurred.
